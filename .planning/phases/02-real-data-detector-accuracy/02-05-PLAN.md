---
phase: 02-real-data-detector-accuracy
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - docs/DETECTOR_EVAL.md
  - data/eval_la/data.yaml
  - README.md
autonomous: true
requirements: [REQ-real-data-accuracy]
requirements_addressed: [REQ-real-data-accuracy]
tags: [docs, ml, eval, readme]

user_setup: []

must_haves:
  truths:
    - "docs/DETECTOR_EVAL.md exists at the documented path (D-10) and is ≥ 150 lines with the seven required sections"
    - "docs/DETECTOR_EVAL.md contains placeholder tables for precision/recall/mAP with `[95% CI: x, y]` syntax (D-08) — operator substitutes real numbers after eval run"
    - "docs/DETECTOR_EVAL.md security section documents pickle ACE risk + mitigations (T-02-01 from Plan 01)"
    - "docs/DETECTOR_EVAL.md licensing section documents CC-BY-SA 4.0 chain (Mapillary images → dataset → fine-tuned weights AGPL-3.0)"
    - "docs/DETECTOR_EVAL.md reproduction-instructions section links to scripts/fetch_eval_data.py, scripts/finetune_detector.py, scripts/eval_detector.py with concrete one-liner each"
    - "data/eval_la/data.yaml exists at repo root with single-class 'pothole' definition (nc=1, names:[0:pothole]) per D-05 + RESEARCH Pattern 4"
    - "README.md contains a new '## Detector Accuracy' section linking to docs/DETECTOR_EVAL.md (ROADMAP SC #4)"
  artifacts:
    - path: "docs/DETECTOR_EVAL.md"
      provides: "LA Detector Evaluation Report with methodology, numbers placeholders, caveats, reproduction, security, licensing"
      min_lines: 150
    - path: "data/eval_la/data.yaml"
      provides: "Single-class YOLO data.yaml seed committed for reproducibility (regenerated by scripts/fetch_eval_data.py --build)"
      contains: "nc: 1"
    - path: "README.md"
      provides: "Detector section linking to eval writeup (ROADMAP SC #4)"
      contains: "DETECTOR_EVAL.md"
  key_links:
    - from: "README.md"
      to: "docs/DETECTOR_EVAL.md"
      via: "markdown link under new '## Detector Accuracy' section"
      pattern: "docs/DETECTOR_EVAL\\.md"
    - from: "docs/DETECTOR_EVAL.md"
      to: "scripts/eval_detector.py"
      via: "reproduction instructions with concrete command"
      pattern: "python scripts/eval_detector\\.py"
    - from: "docs/DETECTOR_EVAL.md"
      to: "docs/FINETUNE.md"
      via: "cross-ref for fine-tune reproduction"
      pattern: "FINETUNE\\.md"
---

<objective>
Deliver the user-facing eval writeup (ROADMAP SC #4) and wire it into README.md
so the public demo has a defensible, honest citation path. Also commit the
`data/eval_la/data.yaml` seed so downstream consumers (Plan 04 training, Plan
02 eval) have a stable schema file even before the dataset is fully populated.

This plan is **document-heavy** and depends on Plans 02/03/04 so it can reference
their CLI shapes accurately (which flags exist, which exit codes mean what,
which files are produced). No training or eval is run inside this plan — the
tables contain `TBD` placeholders that the operator substitutes after the first
real fine-tune + eval run (per the `After Training` section of
`docs/FINETUNE.md`).

Purpose:
- D-10: writeup at `docs/DETECTOR_EVAL.md`, linked from README
- ROADMAP SC #4: "a short writeup in `docs/` records the eval methodology and
  current numbers, honest enough to cite in the public demo"
- Security hand-off: pickle ACE risk (T-02-01) is surfaced to users here
- Licensing hand-off: CC-BY-SA 4.0 chain documented end-to-end

Output:
- `docs/DETECTOR_EVAL.md` — methodology + number placeholders + caveats +
  reproduction + security + licensing (≥ 150 lines, 7 sections)
- `data/eval_la/data.yaml` — committed YOLO data.yaml seed per D-05 + Pattern 4
- `README.md` — new "## Detector Accuracy" section linking to the writeup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-real-data-detector-accuracy/02-CONTEXT.md
@.planning/phases/02-real-data-detector-accuracy/02-RESEARCH.md
@.planning/phases/02-real-data-detector-accuracy/02-PATTERNS.md
@docs/PRD.md
@docs/SETUP.md
@README.md
@scripts/eval_detector.py
@scripts/finetune_detector.py
@scripts/fetch_eval_data.py
@data_pipeline/detector_factory.py
@.planning/phases/02-real-data-detector-accuracy/02-01-PLAN.md
@.planning/phases/02-real-data-detector-accuracy/02-02-PLAN.md
@.planning/phases/02-real-data-detector-accuracy/02-03-PLAN.md
@.planning/phases/02-real-data-detector-accuracy/02-04-PLAN.md

<interfaces>
<!-- Content contracts for DETECTOR_EVAL.md to reference exactly -->

From scripts/eval_detector.py (Plan 02):
- JSON report schema (what --json-out writes):
  `{model_path, split, precision, recall, map50, precision_ci_95: [lo,pt,hi], recall_ci_95: [lo,pt,hi], per_severity: {moderate:{count}, severe:{count}, dropped:{count}}, eval_config: {iou, bootstrap_resamples, ci_level, num_images}}`
- Exit codes (D-18): 0 OK, 1 other, 2 below floor, 3 missing data

From scripts/fetch_eval_data.py (Plan 03):
- Modes: --verify-only (default), --build
- Sequence-grouped 70/20/10 split (D-09 + Pitfall 7)

From scripts/finetune_detector.py (Plan 04):
- Default base: `_DEFAULT_HF_REPO = "keremberke/yolov8s-pothole-segmentation"`
- Seed=42, device=cpu default
- --push-to-hub requires HUGGINGFACE_TOKEN

Security/licensing handoffs:
- Pickle ACE (T-02-01): untrusted `.pt` load = RCE
- AGPL-3.0 on fine-tuned weights (inherits from keremberke base)
- CC-BY-SA 4.0 on Mapillary-derived training imagery
- Per-image attribution via source_mapillary_id in manifest.json

data/eval_la/data.yaml shape (RESEARCH.md Pattern 4):
```yaml
path: data/eval_la
train: images/train
val: images/val
test: images/test
nc: 1
names:
  0: pothole
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit data/eval_la/data.yaml seed (single-class YOLO schema)</name>
  <files>data/eval_la/data.yaml</files>
  <read_first>
    - .gitignore (from Plan 03 — verify `!data/eval_la/data.yaml` un-ignore line is present)
    - .planning/phases/02-real-data-detector-accuracy/02-RESEARCH.md:458-481 (Pattern 4 — YOLO data.yaml shape; single-class vs two-class decision)
    - scripts/fetch_eval_data.py (Plan 03 — `--build` will regenerate this file; our committed version is the seed used when dataset is not yet built)
    - data_pipeline/yolo_detector.py:22-24 (single-class is the chosen path; severity comes from confidence at inference)
  </read_first>
  <action>
    Create (or overwrite) `data/eval_la/data.yaml` at the repo root:

    ```yaml
    # LA pothole eval dataset — committed seed.
    #
    # Populated by scripts/fetch_eval_data.py --build (Phase 2 Plan 03).
    # Single-class per Phase 2 D-05 + RESEARCH.md Pattern 4 — severity
    # (moderate vs severe) is derived from confidence at inference time per
    # data_pipeline/yolo_detector.py::_map_severity:
    #     confidence >= 0.7  -> "severe"
    #     confidence >= 0.4  -> "moderate"
    #     below 0.4          -> dropped
    #
    # If fetch_eval_data.py --build regenerates this file, keep nc + names
    # identical — downstream eval depends on class_id 0 == "pothole".

    path: data/eval_la
    train: images/train
    val: images/val
    test: images/test

    nc: 1
    names:
      0: pothole
    ```

    Verify `git check-ignore data/eval_la/data.yaml` does NOT match (i.e., the file is NOT ignored) — Plan 03's `.gitignore` rule `!data/eval_la/data.yaml` must un-ignore it:
    ```bash
    git check-ignore -v data/eval_la/data.yaml  # Expected: no output (not ignored) or print the un-ignore rule
    ```

    If `git check-ignore` reports the file is ignored (rule `data/eval_la/*` matched but `!data/eval_la/data.yaml` missing), add the un-ignore line to `.gitignore` BEFORE the `data/eval_la/*` rule. Plan 03 should already have done this; this task double-checks.
  </action>
  <verify>
    <automated>test -f data/eval_la/data.yaml</automated>
    <automated>python3 -c "import yaml; d = yaml.safe_load(open('data/eval_la/data.yaml')); assert d['nc'] == 1; assert d['names'] == {0: 'pothole'}; assert d['train'] == 'images/train'; assert d['val'] == 'images/val'; assert d['test'] == 'images/test'"</automated>
    <automated>grep -q "^nc: 1$" data/eval_la/data.yaml</automated>
    <automated>grep -q "^  0: pothole$" data/eval_la/data.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f data/eval_la/data.yaml` exits 0
    - `python3 -c "import yaml; d = yaml.safe_load(open('data/eval_la/data.yaml')); assert d['nc']==1 and d['names'][0]=='pothole'"` exits 0
    - `grep -q "^train: images/train$" data/eval_la/data.yaml` exits 0
    - `grep -q "^val: images/val$" data/eval_la/data.yaml` exits 0
    - `grep -q "^test: images/test$" data/eval_la/data.yaml` exits 0
    - `git check-ignore data/eval_la/data.yaml` returns NON-zero (file NOT ignored) — NOTE: if `git check-ignore` is non-zero exit code that means "not ignored" which is what we want
    - Comment explicitly references the severity rules from yolo_detector.py: `grep -q "confidence >= 0.7" data/eval_la/data.yaml`
  </acceptance_criteria>
  <done>data/eval_la/data.yaml committed with single-class pothole schema, NOT git-ignored, properly documented with severity-from-confidence explanation.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/DETECTOR_EVAL.md — methodology + placeholders + caveats + reproduction + security + licensing</name>
  <files>docs/DETECTOR_EVAL.md</files>
  <read_first>
    - docs/PRD.md:1-40 (header style: title + Version + Last Updated)
    - docs/SETUP.md (operator-voice tone)
    - scripts/eval_detector.py (Plan 02 — CLI flags + JSON report schema)
    - scripts/fetch_eval_data.py (Plan 03 — --build vs --verify-only mode semantics)
    - scripts/finetune_detector.py (Plan 04 — multi-env recipes cross-reference)
    - docs/FINETUNE.md (Plan 04 — link target for reproduction)
    - .planning/phases/02-real-data-detector-accuracy/02-RESEARCH.md:438-456 (image-level bootstrap — A1 assumption to document)
    - .planning/phases/02-real-data-detector-accuracy/02-RESEARCH.md:857-895 (Security domain + Assumptions — pickle ACE, CC-BY-SA, A1/A7/A8 assumptions)
    - .planning/phases/02-real-data-detector-accuracy/02-PATTERNS.md:431-449 (DETECTOR_EVAL.md analog — docs/PRD.md header + section list)
  </read_first>
  <action>
    Create `docs/DETECTOR_EVAL.md`:

    ````markdown
    # Detector Accuracy — LA Evaluation Report

    **Version:** 0.1.0
    **Last Updated:** 2026-04-23
    **Status:** Methodology fixed; numbers populated after first eval run

    ---

    This document records the methodology and reported accuracy of the YOLOv8
    pothole detector used by road-quality-mvp, evaluated on hand-labelled Los
    Angeles street-level imagery sourced from Mapillary. It is the citation
    target for the public demo (ROADMAP M1 Phase 6).

    ## TL;DR

    - **Dataset:** ~300 hand-labelled LA images from Mapillary, single-class
      ("pothole"), sequence-grouped 70/20/10 train/val/test split (D-09)
    - **Base model:** [`keremberke/yolov8s-pothole-segmentation`](https://huggingface.co/keremberke/yolov8s-pothole-segmentation) fine-tuned on the LA train split (D-11)
    - **Metrics (test split):**

      | Metric | Value | 95% CI |
      |--------|-------|--------|
      | Precision | TBD | [TBD, TBD] |
      | Recall    | TBD | [TBD, TBD] |
      | mAP@0.5   | TBD | — |

      *Numbers are populated from `eval_report.json` produced by
      `scripts/eval_detector.py`. Update after re-training.*

    - **Severity breakdown (test split):**

      | Severity | Count |
      |----------|-------|
      | Severe (confidence ≥ 0.7) | TBD |
      | Moderate (0.4 ≤ confidence < 0.7) | TBD |
      | Dropped (confidence < 0.4) | TBD |

    ---

    ## 1. Methodology

    ### Dataset
    - **Source:** Mapillary street-level imagery, three Los Angeles zones
      (downtown DTLA, West LA residential, Hollywood freeway-adjacent) —
      configured in `scripts/fetch_eval_data.py::_DEFAULT_LA_BBOXES`. Each
      bounding box ≤ 0.01 deg² per Mapillary API guidance (Pitfall 3 in
      `.planning/phases/02-real-data-detector-accuracy/02-RESEARCH.md`).
    - **Labelling:** Hand-labelled by the project operator using a YOLO-
      compatible tool (CVAT 1.1 native export). Single-class: label = "pothole"
      regardless of severity; severity is derived at inference from detection
      confidence (`data_pipeline/yolo_detector.py::_map_severity`).
    - **Split:** 70% train / 20% val / 10% test (D-09). Splits grouped by
      Mapillary `sequence_id` so adjacent frames from the same drive never
      span the train/test boundary (Pitfall 7).
    - **Integrity:** Every file has a SHA256 pinned in
      `data/eval_la/manifest.json`. Re-running
      `python scripts/fetch_eval_data.py` (default `--verify-only`) hash-checks
      every file with constant-time compare (`hmac.compare_digest`).

    ### Metrics
    - **Primary (D-06):** Precision, Recall, mAP@0.5 — computed by
      `ultralytics.YOLO.val()` which is the COCO-style reference implementation.
    - **IoU threshold (D-07):** 0.5 — standard for YOLO. Not 0.3 (too lenient,
      weakens claim). Not 0.5:0.95 (overkill for a 300-image set).
    - **Confidence intervals (D-08):** Image-level bootstrap, 1000 resamples,
      95% percentile interval, seed=42 for reproducibility. Computed in
      `data_pipeline/eval.py::bootstrap_ci`.
    - **Per-severity breakdown (D-06):** Moderate (confidence ≥ 0.4 < 0.7) vs
      Severe (confidence ≥ 0.7) — mirrors
      `data_pipeline/yolo_detector.py::_map_severity` exactly so eval metrics
      map 1:1 to runtime behavior.

    ### Why image-level bootstrap?
    Resampling **images with replacement** (not individual detections) honors
    the unit of sampling variability — each Mapillary image is one independent
    "draw." Detections within a single image are not independent (a blurry
    frame causes clustered misses). This is the convention COCO-style eval
    tooling uses implicitly. See Assumption A1 in
    `.planning/phases/02-real-data-detector-accuracy/02-RESEARCH.md` — the
    planner chose image-level over detection-level; reviewers who prefer the
    latter should re-run with detection-level resampling and compare.

    ---

    ## 2. Results

    *Populated from `eval_report.json` after running
    `scripts/eval_detector.py --split test --json-out eval_report.json`.
    Replace TBD cells after the first real fine-tune + eval pass.*

    ### Headline (test split, held out from training per D-09)

    | Metric | Point Estimate | 95% CI (image-level bootstrap, 1000 resamples) |
    |--------|---------------:|-----------------------------------------------:|
    | Precision | TBD | [TBD, TBD] |
    | Recall    | TBD | [TBD, TBD] |
    | mAP@0.5   | TBD | — |

    ### Per-severity detection counts (test split)

    | Severity bucket | Predictions | Matched ground truth | Precision | Recall |
    |-----------------|-----------:|---------------------:|----------:|-------:|
    | Severe (conf ≥ 0.7) | TBD | TBD | TBD | TBD |
    | Moderate (0.4 ≤ conf < 0.7) | TBD | TBD | TBD | TBD |
    | Dropped (conf < 0.4) | TBD | — | — | — |

    ### Eval configuration

    - IoU threshold: 0.5
    - Bootstrap resamples: 1000
    - CI level: 95%
    - Split: test (30 images, sequence-grouped)
    - Model: `runs/detect/la_pothole/weights/best.pt` (local fine-tune) OR
      `<user>/road-quality-la-yolov8@<revision>` (HF Hub pinned)

    ---

    ## 3. Caveats & Limitations

    - **Small test set.** The held-out test split is ~30 images (10% of 300).
      Bootstrap CIs are wide by design; treat point estimates as indicative,
      not definitive. A production detector should be re-evaluated on a
      larger held-out set as more LA imagery is labelled.
    - **LA-specific.** The detector is proven on LA streets only. Generalization
      to other cities, lighting conditions, camera heights, or Mapillary
      vintage is untested. Do not claim global road-damage performance.
    - **Single-operator labelling.** All labels come from one annotator; no
      inter-rater agreement captured. Class boundary ("is this a pothole or
      just a shadow?") is judgment-dependent.
    - **Mapillary vintage and device mix.** Mapillary aggregates images from
      multiple capture devices (smartphones, dash cams, 360° rigs) across
      multiple years. The training distribution is mixed.
    - **Severity bucketing via confidence proxy.** Moderate vs severe is
      derived from detector confidence, not from labelled severity. This is
      a proxy — a severe pothole at an unusual angle may score low confidence
      and bucket as moderate. If severity accuracy becomes important, revisit
      the single-class-vs-two-class decision (see D-05 + A8 in RESEARCH.md).
    - **Deferred:** generalization comparison against RDD2020 (CONTEXT.md
      deferred_ideas; nice-to-have footnote); paper-grade statistical rigor
      (explicitly rejected as MVP overkill); confidence calibration; CI gate
      on detector accuracy.

    ---

    ## 4. Reproduction

    Full reproduction from a clean checkout:

    ```bash
    # 1. Install deps
    pip install -r data_pipeline/requirements.txt
    pip install -r requirements-train.txt   # only if you plan to fine-tune

    # 2. Set tokens (both free; skip HF if you only plan to eval, not publish)
    export MAPILLARY_ACCESS_TOKEN=...        # https://www.mapillary.com/dashboard/developers
    export HUGGINGFACE_TOKEN=hf_...          # https://huggingface.co/settings/tokens

    # 3. Fetch + hash-verify the eval dataset
    #    (--verify-only is default; it confirms data/eval_la/manifest.json matches local files)
    python scripts/fetch_eval_data.py

    #    If manifest missing OR files missing/corrupt: exit code 3, rebuild with:
    #      python scripts/fetch_eval_data.py --build --count 100
    #    then hand-label images/*/ under a YOLO-compatible tool (CVAT recommended).

    # 4. (Optional) Fine-tune on the LA train split — see docs/FINETUNE.md for multi-env recipes
    python scripts/finetune_detector.py \
        --data data/eval_la/data.yaml \
        --epochs 50 \
        --device cpu \                       # or 0 for CUDA, see docs/FINETUNE.md
        --push-to-hub <user>/road-quality-la-yolov8

    # 5. Run the eval
    YOLO_MODEL_PATH=runs/detect/la_pothole/weights/best.pt \
    python scripts/eval_detector.py \
        --data data/eval_la/data.yaml \
        --split test \
        --json-out eval_report.json

    # eval_detector.py exit codes (D-18):
    #   0 = OK
    #   1 = other error (missing dep, unexpected failure)
    #   2 = below --min-precision or --min-recall floor
    #   3 = missing dataset (data.yaml path does not exist)

    # 6. Substitute numbers from eval_report.json into this document's tables.
    ```

    Full multi-env fine-tune reproduction (laptop CPU, Colab T4, EC2 g5.xlarge):
    see [`docs/FINETUNE.md`](FINETUNE.md).

    ---

    ## 5. Security

    **Pickle ACE risk (T-02-01).** The YOLO `.pt` weights file is a pickled
    PyTorch state dict. `ultralytics.YOLO(path).load()` calls `torch.load`
    which executes arbitrary code during deserialization. **Loading an
    untrusted `.pt` file is remote code execution.** HuggingFace marks `.pt`
    files "Unsafe" on the repo UI for exactly this reason.

    Mitigations:
    - The default `YOLO_MODEL_PATH` in `data_pipeline/detector_factory.py`
      (`_DEFAULT_HF_REPO`) points to a known reviewed publisher
      (`keremberke/yolov8s-pothole-segmentation`).
    - Pin to a specific revision in production:
      `YOLO_MODEL_PATH=user/repo@<commit_sha>`. Supported by
      `_resolve_model_path` via the `@<revision>` suffix. Prevents silent
      weight swap by a compromised HF account (Pitfall 8).
    - Future: upgrade to `weights_only=True` when ultralytics supports it
      upstream (currently tracked in their issue queue).

    **Token handling.** `HUGGINGFACE_TOKEN` and `MAPILLARY_ACCESS_TOKEN` are
    read at module top in their respective files, never logged, and only used
    as `Authorization` headers. `.env.example` documents both with empty
    defaults; `.env` is gitignored. Review `.env.example` before committing if
    you add new vars.

    **SHA256 manifest verification.** `scripts/fetch_eval_data.py` uses
    `hmac.compare_digest` for constant-time hash comparison (Security V6) and
    rejects path-traversal entries and malformed hex strings in
    `data/eval_la/manifest.json` (Security V5).

    ---

    ## 6. Licensing & Attribution

    The eval dataset, fine-tuned weights, and downstream route-quality service
    involve three separate license tiers:

    | Artifact | License | Source | Propagation |
    |----------|---------|--------|-------------|
    | Mapillary open imagery | CC-BY-SA 4.0 | https://help.mapillary.com/hc/en-us/articles/115001770409 | Attribution per image via `source_mapillary_id` in `data/eval_la/manifest.json` |
    | Labels (YOLO `.txt` files) | CC-BY-SA 4.0 | Derivative of CC-BY-SA imagery | Inherits — dataset published under CC-BY-SA 4.0 if mirrored on HF Datasets |
    | Base model weights (keremberke) | AGPL-3.0 | Ultralytics base license | Fine-tuned output inherits AGPL-3.0 |
    | Fine-tuned weights | AGPL-3.0 | Derivative | Published under AGPL-3.0 on HF with CC-BY-SA data attribution in model card |
    | road-quality-mvp service code | Project license | This repo | Does NOT link model as a library — loads `.pt` as data at runtime; license chain scope limited to the weights file itself |

    **Risk flag (A8).** AGPL-3.0 is viral for derivative *code*; its reach over
    ML weights loaded at runtime is legally ambiguous. For the public demo,
    weights are used in a backend service that produces detection outputs; the
    service does not statically link the model. Worth a lawyer review if the
    project ever commercializes. For now: document, attribute, and ship.

    **Attribution requirements when publishing:**
    - Mapillary's CC-BY-SA: credit Mapillary + preserve source image IDs.
    - Keremberke's AGPL-3.0 (base model): credit
      `keremberke/yolov8s-pothole-segmentation`.
    - Include both in the fine-tuned weights' HF model card (`README.md`
      uploaded by `scripts/finetune_detector.py::_build_model_card`).

    ---

    ## 7. Changelog

    - **2026-04-23** (v0.1.0): Initial scaffolding. Methodology fixed; numbers
      are TBD pending first fine-tune + eval run.

    ---

    *This report is authored by the project operator. Numbers are regenerated
    from `eval_report.json` on every eval run; substitute them into Section 2
    tables manually (or pipe through a small formatter as future work).*
    ````

    Ensure the file is ≥ 150 lines. Use exact header format matching `docs/PRD.md` style.
  </action>
  <verify>
    <automated>test -f docs/DETECTOR_EVAL.md</automated>
    <automated>[ $(wc -l < docs/DETECTOR_EVAL.md) -ge 150 ]</automated>
    <automated>grep -q "^## 1. Methodology" docs/DETECTOR_EVAL.md && grep -q "^## 2. Results" docs/DETECTOR_EVAL.md && grep -q "^## 3. Caveats" docs/DETECTOR_EVAL.md && grep -q "^## 4. Reproduction" docs/DETECTOR_EVAL.md && grep -q "^## 5. Security" docs/DETECTOR_EVAL.md && grep -q "^## 6. Licensing" docs/DETECTOR_EVAL.md</automated>
    <automated>grep -q "Precision" docs/DETECTOR_EVAL.md && grep -q "Recall" docs/DETECTOR_EVAL.md && grep -q "mAP@0.5" docs/DETECTOR_EVAL.md</automated>
    <automated>grep -q "pickle" docs/DETECTOR_EVAL.md && grep -q "CC-BY-SA" docs/DETECTOR_EVAL.md && grep -q "AGPL" docs/DETECTOR_EVAL.md</automated>
    <automated>grep -q "python scripts/eval_detector.py" docs/DETECTOR_EVAL.md && grep -q "python scripts/fetch_eval_data.py" docs/DETECTOR_EVAL.md && grep -q "docs/FINETUNE.md\|FINETUNE.md" docs/DETECTOR_EVAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/DETECTOR_EVAL.md` exits 0
    - Line count ≥ 150: `[ $(wc -l < docs/DETECTOR_EVAL.md) -ge 150 ]`
    - All 7 sections present: `grep -c "^## [1-7]\\." docs/DETECTOR_EVAL.md` returns 7
    - Three headline metrics mentioned: `grep -c "Precision\\|Recall\\|mAP@0.5" docs/DETECTOR_EVAL.md` ≥ 3
    - 95% CI syntax present: `grep -q "\\[95% CI\\|\\[95%" docs/DETECTOR_EVAL.md` exits 0
    - Security section covers pickle ACE + token handling + SHA256: `grep -q "pickle" docs/DETECTOR_EVAL.md && grep -q "HUGGINGFACE_TOKEN\\|MAPILLARY_ACCESS_TOKEN" docs/DETECTOR_EVAL.md && grep -q "hmac.compare_digest\\|SHA256" docs/DETECTOR_EVAL.md`
    - Licensing section covers CC-BY-SA + AGPL chain: `grep -q "CC-BY-SA" docs/DETECTOR_EVAL.md && grep -q "AGPL" docs/DETECTOR_EVAL.md`
    - Reproduction section references all 3 CLIs: `grep -q "eval_detector.py" docs/DETECTOR_EVAL.md && grep -q "fetch_eval_data.py" docs/DETECTOR_EVAL.md && grep -q "finetune_detector.py" docs/DETECTOR_EVAL.md`
    - Cross-link to FINETUNE.md: `grep -q "FINETUNE.md" docs/DETECTOR_EVAL.md`
    - Header format matches project style: `grep -q "^\\*\\*Version:\\*\\*" docs/DETECTOR_EVAL.md && grep -q "^\\*\\*Last Updated:\\*\\*" docs/DETECTOR_EVAL.md`
    - Deferred items acknowledged: `grep -q "RDD2020\\|calibration\\|CI gate\\|Paper-grade" docs/DETECTOR_EVAL.md`
    - Image-level bootstrap choice documented (A1): `grep -q "image-level\\|image level" docs/DETECTOR_EVAL.md`
  </acceptance_criteria>
  <done>docs/DETECTOR_EVAL.md exists ≥ 150 lines with 7 sections (TL;DR + 1-7), methodology fixed, metric placeholders, caveats, reproduction, security (pickle ACE + tokens + SHA256), licensing (CC-BY-SA + AGPL chain), changelog.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with "## Detector Accuracy" section linking to the writeup</name>
  <files>README.md</files>
  <read_first>
    - README.md (full — 148 lines; understand existing section ordering + heading styles)
    - docs/DETECTOR_EVAL.md (just authored — confirm relative link path)
    - docs/FINETUNE.md (from Plan 04 — also linked in reproduction-oriented line)
  </read_first>
  <action>
    Edit `README.md`. Insert a new section **`## Detector Accuracy`** immediately AFTER the `## API Endpoints` section and BEFORE `## Frontend Pages`. Do not reorder or edit any other section. The new section must match the README's existing heading style (`##` level, blank line above and below):

    ```markdown
    ## Detector Accuracy

    The YOLOv8 pothole detector is evaluated on a hand-labelled LA eval set
    (~300 Mapillary images, 70/20/10 sequence-grouped split) with precision,
    recall, mAP@0.5, and image-level bootstrap 95% CIs. Full methodology and
    numbers: [`docs/DETECTOR_EVAL.md`](docs/DETECTOR_EVAL.md).

    Reproduce from a clean checkout — see
    [`docs/FINETUNE.md`](docs/FINETUNE.md) for laptop/Colab/EC2 fine-tuning
    recipes, then:

    ```bash
    # verify dataset integrity
    python scripts/fetch_eval_data.py

    # run eval on the held-out test split
    python scripts/eval_detector.py --data data/eval_la/data.yaml --split test
    ```

    Configuration: set `YOLO_MODEL_PATH` in `.env` to either a HuggingFace repo
    id (e.g. `<user>/road-quality-la-yolov8@<revision>`) or a local `.pt`
    file path. Default falls back to `keremberke/yolov8s-pothole-segmentation`.

    ```

    The exact insertion point is after line containing
    `## Frontend Pages` — insert the new section BEFORE it. In the final
    result, section ordering is:
    1. Title + Status + Quick Start + How It Works
    2. API Endpoints
    3. **Detector Accuracy (NEW)**
    4. Frontend Pages
    5. Tests
    6. Tech Stack
    7. Documentation

    Update the Documentation section at the bottom (after `## Documentation`
    heading, as the first bullet after existing bullets) to include:

    ```markdown
    - [Detector Accuracy Report](docs/DETECTOR_EVAL.md) — evaluation methodology, numbers, reproduction
    - [Fine-Tuning Guide](docs/FINETUNE.md) — laptop / Colab / EC2 recipes
    ```

    Do NOT change existing bullets, heading text, or ordering anywhere else.
  </action>
  <verify>
    <automated>grep -q "^## Detector Accuracy$" README.md</automated>
    <automated>grep -q "docs/DETECTOR_EVAL.md" README.md</automated>
    <automated>grep -q "docs/FINETUNE.md" README.md</automated>
    <automated>grep -q "python scripts/eval_detector.py" README.md</automated>
    <automated>grep -q "YOLO_MODEL_PATH" README.md</automated>
    <automated>awk '/^## /' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Detector Accuracy$" README.md` returns 1
    - Section order preserved: `awk '/^## /' README.md | head -7` output contains "Quick Start", "How It Works", "API Endpoints", "Detector Accuracy", "Frontend Pages", "Tests", "Tech Stack" in that order (other top-level sections intact)
    - Link to DETECTOR_EVAL.md present: `grep -c "docs/DETECTOR_EVAL.md" README.md` ≥ 2 (once in section + once in Documentation list)
    - Link to FINETUNE.md present: `grep -c "docs/FINETUNE.md" README.md` ≥ 2
    - `YOLO_MODEL_PATH` configuration note present: `grep -q "YOLO_MODEL_PATH" README.md`
    - Existing sections untouched: `grep -q "^### 1. Start the database$" README.md && grep -q "^### GET /health$" README.md && grep -q "^## Tech Stack$" README.md`
    - Documentation section extended with 2 new links (total ≥ 6 bullets): `awk '/^## Documentation/,/^##[^#]/ { if (/^- /) print }' README.md | wc -l` returns ≥ 6
  </acceptance_criteria>
  <done>README.md gains "## Detector Accuracy" section between API Endpoints and Frontend Pages; Documentation section gains links to DETECTOR_EVAL.md and FINETUNE.md; no existing content modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/DETECTOR_EVAL.md reader → linked artifacts | Public-facing document references internal CLIs and HF repos |
| README.md reader → docs/ | First-stop link graph for new project visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28 | Information Disclosure | DETECTOR_EVAL.md reveals internal architecture details to attackers | accept | Public demo is open-source by design. Architecture docs are already in `.planning/`. No secrets or endpoints exposed. |
| T-02-29 | Tampering | Reader follows DETECTOR_EVAL.md instructions and runs `scripts/finetune_detector.py --push-to-hub <attacker/repo>` by copy-paste | accept | Code blocks show placeholder `<user>/road-quality-la-yolov8`; operator substitutes own repo. Standard docs-as-instructions risk. |
| T-02-30 | Legal | DETECTOR_EVAL.md under-documents CC-BY-SA attribution, downstream user violates license | mitigate | Section 6 explicitly tables CC-BY-SA + AGPL chain with propagation rules. Attribution call-out in callable workflow (`_build_model_card` in Plan 04). |
| T-02-31 | Information Disclosure | README.md links to a private HF repo URL if operator substitutes prematurely | accept | Documentation template uses placeholder; operator responsibility. |
</threat_model>

<verification>
```bash
# Artifacts
test -f data/eval_la/data.yaml
test -f docs/DETECTOR_EVAL.md
[ $(wc -l < docs/DETECTOR_EVAL.md) -ge 150 ]
grep -q "^## Detector Accuracy$" README.md

# data.yaml shape
python3 -c "import yaml; d = yaml.safe_load(open('data/eval_la/data.yaml')); assert d['nc']==1 and d['names'][0]=='pothole'"

# DETECTOR_EVAL.md sections
grep -c "^## [1-7]\\." docs/DETECTOR_EVAL.md  # expect 7

# Linkage
grep -q "docs/DETECTOR_EVAL.md" README.md
grep -q "docs/FINETUNE.md" README.md
grep -q "scripts/eval_detector.py" docs/DETECTOR_EVAL.md
grep -q "FINETUNE.md" docs/DETECTOR_EVAL.md

# Security + licensing content
grep -q "pickle" docs/DETECTOR_EVAL.md
grep -q "CC-BY-SA" docs/DETECTOR_EVAL.md
grep -q "AGPL" docs/DETECTOR_EVAL.md

# Final Phase-2 smoke: all tests pass (aggregates Plans 01-04)
python3 -m pytest backend/tests/ -q
```
</verification>

<success_criteria>
- `data/eval_la/data.yaml` committed with single-class pothole schema (nc=1, names:[0:pothole], train/val/test paths)
- `docs/DETECTOR_EVAL.md` exists ≥ 150 lines with all 7 sections (Methodology, Results placeholders, Caveats, Reproduction, Security, Licensing, Changelog) + TL;DR
- Pickle ACE risk explicitly documented with concrete mitigation (pin `@<revision>` in production)
- CC-BY-SA (Mapillary) + AGPL-3.0 (base model) license chain tabled
- Reproduction section references all three CLIs from Plans 02/03/04 with concrete one-liner invocations
- Image-level bootstrap choice (A1) documented with explanation
- Deferred items (RDD2020, calibration, CI gate, paper-grade) explicitly acknowledged
- `README.md` gains `## Detector Accuracy` section between API Endpoints and Frontend Pages, linking both to DETECTOR_EVAL.md and FINETUNE.md
- `README.md` Documentation section lists both new docs
- No existing README content modified; section ordering preserved
- Phase 2 aggregate: `pytest backend/tests/ -q` still green across all 5 plans' contributions
</success_criteria>

<deferred>
- Auto-regeneration of Results tables from `eval_report.json` via a small Python formatter (operator does manual substitution for now)
- Colab notebook stub (`docs/finetune_colab.ipynb`) — D-16 marks as optional; embedded commands in FINETUNE.md sufficient
- RDD2020 cross-dataset comparison footnote (Claude's discretion per CONTEXT.md — not added; flagged for future revision if demo reviewers ask)
- Model-card continuous regeneration from eval_report.json (operator manually updates on publish)
- Continuous-eval CI gate on detector accuracy (deferred per CONTEXT.md)
</deferred>

<output>
After completion, create `.planning/phases/02-real-data-detector-accuracy/02-05-SUMMARY.md` summarizing:
- data.yaml committed + NOT gitignored
- DETECTOR_EVAL.md section map (TL;DR + 7 sections with line ranges)
- README.md diff (one new section + 2 new Documentation bullets)
- Hand-off to Phase 6 (public demo): ROADMAP SC #4 satisfied, writeup is the citation target for the demo landing page
- Operator runbook: after the first real fine-tune, substitute TBD cells in DETECTOR_EVAL.md Section 2 from `eval_report.json`; update `_DEFAULT_HF_REPO` in `data_pipeline/detector_factory.py` to point at the published fine-tune with a pinned revision
</output>
