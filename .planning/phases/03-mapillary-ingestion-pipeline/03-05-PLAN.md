---
phase: 03-mapillary-ingestion-pipeline
plan: 05
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - docs/MAPILLARY_INGEST.md
  - README.md
autonomous: true
requirements:
  - REQ-mapillary-pipeline
tags:
  - documentation
  - operator-runbook
  - sc4
  - phase-6-handoff

must_haves:
  truths:
    - "`docs/MAPILLARY_INGEST.md` exists and documents the full operator workflow: prerequisites, target modes, snap radius, idempotency, --wipe-synthetic semantics, --source toggle, manifest layout"
    - "Trust model for `--where` is documented (RESEARCH Pattern 6 line 710 — operator-trusted, regex defends against typos, max_segments cap is the DoS bound)"
    - "Migration apply procedure for existing dev DBs is documented"
    - "SC #4 ranking-comparison workflow is documented step-by-step (RESEARCH Validation Architecture lines 1119-1129)"
    - "Phase 6 deploy cutover is documented as a forward-flag (`--wipe-synthetic` + manual `POST /cache/clear` + auto-recompute)"
    - "CC-BY-SA attribution requirement is documented"
    - "RESEARCH Pitfalls 1, 2, 4, 5, 7, 8, 10 are surfaced as 'Common gotchas'"
    - "README.md gains a new top-level section heading linking to docs/MAPILLARY_INGEST.md"
  artifacts:
    - path: "docs/MAPILLARY_INGEST.md"
      provides: "operator runbook + SC #4 demo workflow + Phase 6 cutover + trust model"
      contains: "MAPILLARY_ACCESS_TOKEN"
      min_lines: 150
    - path: "README.md"
      provides: "section + link to docs/MAPILLARY_INGEST.md"
      contains: "MAPILLARY_INGEST.md"
  key_links:
    - from: "README.md"
      to: "docs/MAPILLARY_INGEST.md"
      via: "markdown link in a 'Real-Data Ingest' section"
      pattern: "MAPILLARY_INGEST\\.md"
---

<objective>
Ship `docs/MAPILLARY_INGEST.md` — the single operator runbook for `scripts/ingest_mapillary.py` end-to-end, the SC #4 ranking-comparison demo, the Phase 6 deploy cutover sequence, and the operational pitfalls operators are most likely to hit. Add a section in README.md linking to it.

Purpose: Phase 6 will cite this document for the public-demo data-source paragraph. Plan 04 wired the auto-recompute and `--wipe-synthetic` mechanics — without an operator-readable runbook, the SC #4 demo can be performed by an executor but cannot be reproduced by anyone else.

Output: One new markdown doc (~150-250 lines) + an additive section in README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-CONTEXT.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-RESEARCH.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-PATTERNS.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-01-SUMMARY.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-02-SUMMARY.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-03-SUMMARY.md
@.planning/phases/03-mapillary-ingestion-pipeline/03-04-SUMMARY.md
@scripts/ingest_mapillary.py
@scripts/compute_scores.py
@db/migrations/002_mapillary_provenance.sql
@README.md
@docs/DETECTOR_EVAL.md
@docs/SETUP.md

<interfaces>
The README's existing top-level section structure (extracted by grep `^## `):
  ## Current Status (2026-02-23)
  ## Quick Start
  ## How It Works
  ## API Endpoints
  ## Detector Accuracy
  ## Frontend Pages
  ## Tests
  ## Tech Stack
  ## Documentation

Recommended insertion point: between `## Detector Accuracy` and `## Frontend Pages`, mirroring the way `## Detector Accuracy` was added in Phase 2. New section title: `## Real-Data Ingest`.

Final flag set of scripts/ingest_mapillary.py after plans 03 + 04 (13 flags):
  --segment-ids, --segment-ids-file, --where (mutex group, required),
  --snap-meters (default 25.0), --pad-meters (default 50.0),
  --limit-per-segment (default 20), --cache-root (default data/ingest_la),
  --no-keep, --json-out, -v/--verbose,
  --wipe-synthetic, --force-wipe, --no-recompute

Existing analog: docs/DETECTOR_EVAL.md (Phase 2 markdown style template for cite-able demo docs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/MAPILLARY_INGEST.md — operator runbook covering prerequisites, all CLI modes, SC #4 demo workflow, Phase 6 cutover, trust model, and pitfalls (D-15, D-18, RESEARCH Validation Architecture)</name>
  <files>docs/MAPILLARY_INGEST.md</files>
  <read_first>
    - scripts/ingest_mapillary.py (FINAL state after plans 03 + 04 — extract the exact flag list, exit codes, and auto-recompute behavior)
    - scripts/compute_scores.py (modified version after plan 02 — `--source` flag)
    - db/migrations/002_mapillary_provenance.sql (the apply command for existing dev DBs)
    - docs/DETECTOR_EVAL.md (analog markdown style — section structure, table conventions, code-block style)
    - .planning/phases/03-mapillary-ingestion-pipeline/03-RESEARCH.md (Pattern 6 trust model lines 710-715; Validation Architecture SC #4 workflow lines 1119-1129; Pitfalls 1-10)
    - .planning/phases/03-mapillary-ingestion-pipeline/03-CONTEXT.md (D-13/D-14/D-15 — Phase 6 forward-flag expectations; `<deferred>` block — what NOT to document)
    - .planning/phases/03-mapillary-ingestion-pipeline/03-PATTERNS.md (lines 509-543 — section structure mirrored from scripts/fetch_eval_data.py docstring)
  </read_first>
  <action>
Create `docs/MAPILLARY_INGEST.md` with the structure below. Mirror `docs/DETECTOR_EVAL.md`'s markdown style (ATX `#` headings, fenced code blocks, comparison tables for flag references and gotchas).

The document MUST include — in this order, with these exact heading texts (Phase 6's plan will grep for some of these):

1. **Title + frontmatter (no YAML, just intro lines)**: H1 `# Mapillary Ingestion Pipeline — Operator Runbook`. Then bold lines: `**Phase:** 3 (M1)`, `**Script:** scripts/ingest_mapillary.py`, `**Migration:** db/migrations/002_mapillary_provenance.sql`, `**Requirement:** REQ-mapillary-pipeline`. Then 1-paragraph summary saying this is the operator runbook for the pipeline.

2. **`## What this pipeline does`** — numbered steps 1-6 covering: pad bbox per target segment (default 50 m), pull Mapillary imagery within bbox, run YOLOv8 detector per image, snap-match to nearest segment within snap_meters (default 25 m, drop if outside), write idempotent rows tagged `source='mapillary'` with `source_mapillary_id`, auto-run `compute_scores.py`.

3. **`## Prerequisites`** — table with rows: `MAPILLARY_ACCESS_TOKEN` env (link <https://www.mapillary.com/dashboard/developers>, "NEVER commit"); `DATABASE_URL` env (default + cloud override note); `YOLO_MODEL_PATH` env optional (HF default + pin to `user/repo@sha` for prod, link to docs/FINETUNE.md); migration 002 applied (link to next section); `pip install -r data_pipeline/requirements.txt`; `seed_data.py` already run.

4. **`## Applying the migration`** — TWO subsections:
   - **Fresh dev DB**: `docker compose up` picks it up automatically because docker-compose.yml mounts it.
   - **Existing dev DB**: provide the literal command:
     `docker compose exec -T db psql -U rq -d roadquality < db/migrations/002_mapillary_provenance.sql`
     plus the verification query:
     `docker compose exec db psql -U rq -d roadquality -c "SELECT source, COUNT(*) FROM segment_defects GROUP BY source"`
     and the expected result narration ("all existing rows under source='synthetic'").

5. **`## CLI reference`** with subsections:
   - `### Target modes (mutually exclusive, one required)` — table with rows for `--segment-ids 1,2,3`, `--segment-ids-file path.txt`, `--where "..."`. Include a one-line "When to use" column.
   - `### Options` — table with all 10 non-target flags (`--snap-meters`, `--pad-meters`, `--limit-per-segment`, `--cache-root`, `--no-keep`, `--json-out`, `--wipe-synthetic`, `--force-wipe`, `--no-recompute`, `-v/--verbose`) with default + purpose columns. Make `--wipe-synthetic` row explicitly say "Destructive" and "Aborts (exit 2) if zero detections will be written, unless --force-wipe is also passed".
   - `### Exit codes` — table 0/1/2/3 mirroring D-18 conventions (0 OK, 1 generic/token-missing/DB-down, 2 validation/empty-target/wipe-without-replacement, 3 missing-resource).

6. **`## Trust model for --where`** — explain it's NOT a security boundary (operator already has DB credentials), but defends against typos/copy-paste accidents. List the rejected token classes verbatim from RESEARCH Pattern 6: DDL/DML keywords (DELETE, UPDATE, INSERT, DROP, ALTER, CREATE, GRANT, REVOKE, EXECUTE, TRUNCATE, COPY, EXEC), system catalogs (`pg_*`, `information_schema`), statement separators (`;`), comment markers (`--`, `/*`, `*/`). Document the `max_segments=1000` cap and the `SET statement_timeout = '30s'` bound. Tell operators with > 1000 segments to chunk via `LIMIT N OFFSET M`.

7. **`## Idempotency`** — 1-2 paragraphs explaining: every row carries `source_mapillary_id` + `source='mapillary'`; UNIQUE index `uniq_defects_segment_source_severity`; `INSERT ... ON CONFLICT DO NOTHING`; second run produces zero new rows; counters `rows_inserted` vs `rows_skipped_idempotent` in the run summary report this.

8. **`## Provenance and licensing`** — explicitly cite Mapillary's CC-BY-SA 4.0 (link <https://help.mapillary.com/hc/en-us/articles/115001770409>). Two attribution mechanisms: `segment_defects.source_mapillary_id` per row, and per-run `data/ingest_la/manifest-<timestamp>.json` (image_id, local cache path, SHA256, matched segment id). Warn against redistributing bytes from `data/ingest_la/` without preserving these attributions; note the directory is gitignored for this reason (only `.gitkeep` committed).

9. **`## SC #4 ranking-comparison demo workflow`** — the canonical 5-step demo:
   - Step 1: ingest with a high-IRI `--where` predicate (e.g., `iri_norm > 0.6 ORDER BY iri_norm DESC LIMIT 50`).
   - Step 2: `python scripts/compute_scores.py --source synthetic` → POST a `/route` request → `jq '.best_route.total_cost'` → `/tmp/synthetic-cost.txt`.
   - Step 3: `python scripts/compute_scores.py --source mapillary` → same POST → `/tmp/mapillary-cost.txt`.
   - Step 4: `diff /tmp/synthetic-cost.txt /tmp/mapillary-cost.txt` (non-empty diff = SC #4 satisfied; archive both files for the demo).
   - Step 5: restore default with `--source all`.
   Provide a literal `curl -s -X POST http://localhost:8000/route -H 'Content-Type: application/json' -d '{...}'` template with realistic LA coordinates so operators can paste-and-run.
   Note that an empty diff means either a uniform set of dominant segments OR Pitfall 7 (no mapillary rows yet — see Common gotchas).

10. **`## Phase 6 public-demo cutover`** — DO NOT rename this heading; plan 06-NN-PLAN.md will grep for it. The 4-step cutover:
    - Step 1: apply migration to cloud DB (`psql "$DATABASE_URL_CLOUD" < db/migrations/002_mapillary_provenance.sql`).
    - Step 2: `python scripts/ingest_mapillary.py --where "iri_norm > 0.5 ORDER BY iri_norm DESC LIMIT 200" --limit-per-segment 15 --wipe-synthetic`. Explain the wipe is gated on having real detections.
    - Step 3: `curl -X POST "$DEMO_API_URL/cache/clear"` to invalidate cachetools.
    - Step 4: smoke a `/route` request and verify `pothole_score_total > 0` along the route.
    Note: after cutover, `seed_data.py` MUST NOT be re-run on the demo DB (would re-introduce synthetic rows).

11. **`## Common gotchas`** — table mapping symptom → cause → fix, covering at minimum these 8 cases (all from RESEARCH Pitfalls):
    - "Detections on neighbor segment" → Pitfall 5 (D-04 attribute-to-closest is intentional) → check `matched_to_neighbor` counter.
    - "`--source mapillary` shows zeros" → Pitfall 7 (no Mapillary rows yet) → ingest first or fall back to `--source all`.
    - "`/segments` still shows synthetic after wipe" → Pitfall 4 (cachetools TTL) → `POST /cache/clear`.
    - "Re-running ingest doubles row counts" → Pitfall 1 (NULL `source_mapillary_id`) → verify migration applied; if NULL appears on mapillary rows, file a bug.
    - "Migration apply errors `constraint already exists`" → Pitfall 8 (Postgres 16 has no `ADD CONSTRAINT IF NOT EXISTS`) → migration uses DROP-then-ADD pattern; re-fetch from git if you see this.
    - "`validate_bbox` rejects `exceeds 0.01 deg²`" → Pitfall 2 (long arterial) → CLI auto-subdivides; lower `--pad-meters` if subdivide still fails.
    - "Mapillary HTTP 429" → rate limit (10k/min search) → CLI has hand-rolled exponential backoff; lower `--limit-per-segment` or split runs.
    - "`data/ingest_la/` accidentally committed" → Pitfall 10 (.gitignore not pulled) → `git rm --cached` and re-pull.

12. **`## What this pipeline does NOT do (out of scope)`** — bullet list of deferred items from CONTEXT.md `<deferred>`: per-class confidence calibration, heading/orientation filter, `--bbox` auto-tile-sweep mode, named target sets, `segment_defects_synthetic` separate table, soft-delete via `deleted_at`, continuous-CI ingestion gate. Phase 4 auth gate explicitly noted as N/A (CLI not HTTP).

13. **`## References`** — bullet list with relative-path links to: 03-CONTEXT.md (decisions), 03-RESEARCH.md (patterns + pitfalls), `db/migrations/002_mapillary_provenance.sql`, `scripts/ingest_mapillary.py`, `scripts/compute_scores.py`. Plus external links: Mapillary developer docs <https://www.mapillary.com/developer/api-documentation>, CC-BY-SA <https://help.mapillary.com/hc/en-us/articles/115001770409>.

Style notes:
- Use ATX headings (`#`, `##`, `###`) matching `docs/DETECTOR_EVAL.md`.
- Tables for flag references and gotchas — readers scan, they don't read line-by-line.
- Fenced bash/sql/json code blocks for every command an operator might paste; include the leading `$ ` only if `docs/DETECTOR_EVAL.md` does (it does NOT — match its convention).
- The "Phase 6 public-demo cutover" heading text is load-bearing for plan 06; do NOT rename or remove it.

DO NOT introduce features not present in CONTEXT.md (no per-image bbox in segment_defects, no deleted_at column, no `--bbox` mode). DO NOT speculate about Phase 4/5 — only forward-flag what CONTEXT.md D-15 mandates (Phase 6 cutover documentation).
  </action>
  <verify>
    <automated>test -f docs/MAPILLARY_INGEST.md && test $(wc -l < docs/MAPILLARY_INGEST.md) -gt 100 && grep -q "MAPILLARY_ACCESS_TOKEN" docs/MAPILLARY_INGEST.md && grep -q "002_mapillary_provenance.sql" docs/MAPILLARY_INGEST.md && grep -q "wipe-synthetic" docs/MAPILLARY_INGEST.md && grep -q "source synthetic" docs/MAPILLARY_INGEST.md && grep -q "source mapillary" docs/MAPILLARY_INGEST.md && grep -q "Phase 6 public-demo cutover" docs/MAPILLARY_INGEST.md && grep -q "SC #4 ranking-comparison demo workflow" docs/MAPILLARY_INGEST.md && grep -q "CC-BY-SA" docs/MAPILLARY_INGEST.md && grep -q "Trust model" docs/MAPILLARY_INGEST.md && grep -q "Common gotchas" docs/MAPILLARY_INGEST.md && grep -q "data/ingest_la" docs/MAPILLARY_INGEST.md && grep -q "max_segments" docs/MAPILLARY_INGEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MAPILLARY_INGEST.md` exists, is at least 100 lines
    - Contains `MAPILLARY_ACCESS_TOKEN` env-var reference (D-19)
    - Contains the migration apply command for existing dev DBs (`002_mapillary_provenance.sql`)
    - Contains `--wipe-synthetic` documentation (D-14)
    - Documents `--source synthetic` AND `--source mapillary` toggle (D-16)
    - Contains heading `## Phase 6 public-demo cutover` (forward-flag for D-15)
    - Contains heading `## SC #4 ranking-comparison demo workflow` (D-18)
    - Cites CC-BY-SA license (provenance requirement)
    - Documents the `--where` trust model (RESEARCH Pattern 6)
    - Has a `Common gotchas` section
    - References `data/ingest_la/` cache layout
    - References `max_segments` cap
    - Markdown lints cleanly: `python3 -c "import re; t=open('docs/MAPILLARY_INGEST.md').read(); assert t.count('```') % 2 == 0"` succeeds (balanced fences)
  </acceptance_criteria>
  <done>Operator can clone the repo, install deps, set the token, apply the migration, run the ingest, run the SC #4 demo, and execute the Phase 6 cutover by following only this document. All 13 verification flags pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add a "Real-Data Ingest" section to README.md linking to docs/MAPILLARY_INGEST.md (REQ-readme-docs reaffirmed for Phase 3)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current section structure — find where the `## Detector Accuracy` section ends and `## Frontend Pages` begins; insert the new section between them)
    - docs/MAPILLARY_INGEST.md (the document being linked — read AFTER Task 1 lands)
  </read_first>
  <action>
EDIT `README.md`:

Insert a new top-level section between the existing `## Detector Accuracy` and `## Frontend Pages` sections. The section should mirror the brevity of `## Detector Accuracy` (~5-10 lines).

Section content:

```markdown
## Real-Data Ingest

The `scripts/ingest_mapillary.py` CLI pulls Mapillary imagery for a target list of road segments, runs the YOLOv8 detector, and writes detections back into `segment_defects` with full provenance (`source = 'mapillary'`, `source_mapillary_id = <image_id>`). Re-runs are idempotent.

Three target modes — explicit ids, ids-file, or `--where` SQL predicate. After each ingest, the CLI auto-runs `scripts/compute_scores.py` so `/segments` and `/route` reflect the new detections.

See [`docs/MAPILLARY_INGEST.md`](docs/MAPILLARY_INGEST.md) for the operator runbook: prerequisites, all flags, the SC #4 ranking-comparison demo workflow, the Phase 6 public-demo cutover sequence, and the trust model for `--where`.

Quick start (after `MAPILLARY_ACCESS_TOKEN` is set):
```bash
python scripts/ingest_mapillary.py --segment-ids 1,2,3 --limit-per-segment 5
```
```

Place it AFTER the closing line of `## Detector Accuracy` and BEFORE `## Frontend Pages`. Preserve all surrounding content exactly. Do NOT add to the `## Documentation` section unless `docs/DETECTOR_EVAL.md` is also listed there (if `docs/DETECTOR_EVAL.md` IS listed in `## Documentation`, also add `docs/MAPILLARY_INGEST.md` adjacent to it for consistency — read the existing list first to decide).

Do NOT modify the `## Quick Start` section. Do NOT modify the `## Tech Stack` or `## Tests` sections. Phase 3 only adds the new section + (conditionally) a doc-list entry.
  </action>
  <verify>
    <automated>grep -q "## Real-Data Ingest" README.md && grep -q "docs/MAPILLARY_INGEST.md" README.md && grep -q "MAPILLARY_ACCESS_TOKEN" README.md && grep -c "^## " README.md | awk '{ if ($1 < 9) { print "FAIL: too few sections"; exit 1 } }' && grep -B 1 "## Real-Data Ingest" README.md | head -1 | grep -E "(Detector Accuracy|^$|^[^#])"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the heading `## Real-Data Ingest`
    - README.md contains a link to `docs/MAPILLARY_INGEST.md` (e.g., `[...](docs/MAPILLARY_INGEST.md)` markdown link)
    - README.md mentions `MAPILLARY_ACCESS_TOKEN` (so users know what env var is needed before clicking through)
    - README.md still has at least 9 top-level sections (8 original + 1 new — counted by `^## ` lines)
    - The new `## Real-Data Ingest` section is positioned AFTER `## Detector Accuracy` (verified by grep+sort or by visual inspection)
    - No prior README content is removed (verified by `git diff README.md` showing only insertions, not deletions)
    - If `docs/DETECTOR_EVAL.md` was listed in the `## Documentation` section before this plan, `docs/MAPILLARY_INGEST.md` is now also listed there (consistency)
  </acceptance_criteria>
  <done>README has a single new section pointing to the operator runbook; existing structure preserved; users find the runbook from the canonical entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented procedure → operator | The runbook is what the operator runs against production. If the runbook documents an unsafe procedure (e.g., committing tokens, unsafe `--where` examples), the operator may execute it verbatim. |
| README link → external readers | Public-facing entry point; the link must point at the runbook (not at internal planning docs) and must not embed secrets in examples. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Information Disclosure | Documentation includes a real Mapillary token in an example | mitigate | All token examples use the placeholder `mly_xxx` or `MAPILLARY_ACCESS_TOKEN=...`. The acceptance criterion grep verifies `MAPILLARY_ACCESS_TOKEN` appears (the env-var name is fine to publish), but the runbook never publishes a live token. The "Prerequisites" table explicitly says "NEVER commit this." |
| T-03-24 | Tampering | Phase 6 plan-author can't find the cutover sequence | mitigate | The heading `## Phase 6 public-demo cutover` is the load-bearing forward-flag. Acceptance criterion greps for it verbatim. |
| T-03-25 | Tampering | Operator tries to run `--wipe-synthetic` on a fresh DB and loses all data | mitigate | The runbook documents the wipe-guard semantics ("Aborts (exit 2) if zero detections will be written, unless --force-wipe is also passed") and explicitly cautions about `--force-wipe`. The `## Phase 6 public-demo cutover` step also documents the proper sequence. |
| T-03-26 | Tampering | Operator copy-pastes a `--where` predicate from internet that contains DDL | mitigate | The runbook's "Trust model for --where" section enumerates the rejected token classes so operators learn what the regex blocks. RESEARCH Pattern 6 is the reference. |
</threat_model>

<verification>
After both tasks land:
1. `test -f docs/MAPILLARY_INGEST.md` and Task 1's grep gauntlet (13 strings) all return matches.
2. `grep -c "^## " docs/MAPILLARY_INGEST.md` reports >= 12 sections (13 if the operator includes the title H1 in the count).
3. `grep -c "^## " README.md` is exactly 1 higher than before this plan landed.
4. `python3 -c "import re; t=open('docs/MAPILLARY_INGEST.md').read(); assert t.count('` + chr(96) + chr(96) + chr(96) + `') % 2 == 0"` passes (balanced fences).
5. Manual scan: every flag from `python scripts/ingest_mapillary.py --help` is mentioned in the runbook (a missed flag is a doc gap).
6. Manual scan: `docs/MAPILLARY_INGEST.md` does NOT mention any deferred feature (no per-image bbox column, no `--bbox` mode, no soft-delete).
</verification>

<success_criteria>
- `docs/MAPILLARY_INGEST.md` documents the entire operator surface end-to-end
- README.md has a clear entry-point section linking to the runbook
- The `## Phase 6 public-demo cutover` heading is preserved verbatim (forward-flag for plan 06)
- Trust model for `--where` is captured (RESEARCH Pattern 6)
- SC #4 ranking-comparison demo is reproducible from the runbook alone
- CC-BY-SA attribution mechanism is explicit
- Common gotchas surface 8 RESEARCH pitfalls so operators self-debug
- No deferred features documented (CONTEXT.md `<deferred>` block respected)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mapillary-ingestion-pipeline/03-05-SUMMARY.md` documenting:
- Final section list of `docs/MAPILLARY_INGEST.md` (heading text only — Phase 6 plan will reference these)
- Where in README.md the new section was inserted (heading-anchor reference)
- Confirmation that the `## Phase 6 public-demo cutover` heading is the forward-flag for plan 06
- Manual-only verifications still outstanding for Phase 3 (per `.planning/phases/03-mapillary-ingestion-pipeline/03-VALIDATION.md` Manual-Only Verifications table): live Mapillary smoke run + SC #4 archival
</output>
