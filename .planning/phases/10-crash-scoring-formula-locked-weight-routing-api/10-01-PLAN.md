---
phase: 10-crash-scoring-formula-locked-weight-routing-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/scoring.py
  - backend/tests/test_scoring.py
autonomous: true
requirements:
  - REQ-crash-scoring-formula
  - REQ-route-api-locked-weights
must_haves:
  truths:
    - "`backend/app/scoring.py` exports module-level constants `W_IRI=0.40`, `W_POT=0.35`, `W_CRASH=0.25` (exact float literals, no env var overrides per D-10-01)"
    - "`backend/app/scoring.py` exports severity weight constants `FATAL_WEIGHT=8`, `INJURY_WEIGHT=3`, `PDO_WEIGHT=1`, plus `FATAL_CAP_K=3` and `FATAL_CAP=24` (per D-10-04, D-10-05; researcher recommendation K=3)"
    - "`compute_segment_cost(travel_time_s, iri_norm, pothole_total, crash_norm)` returns `travel_time_s + W_IRI*iri_norm + W_POT*pothole_total + W_CRASH*crash_norm` with no normalization step (D-10-02)"
    - "`normalize_weights()` is still importable from `app.scoring`, marked `# DEPRECATED v0.4.0 — remove after one milestone of confidence` in a docstring/comment, and all 6 existing `TestNormalizeWeights` tests in `backend/tests/test_scoring.py` continue to pass unchanged (D-10-03, D-10-21)"
    - "`backend/tests/test_scoring.py` contains ≥8 NEW unit tests in `TestLockedConstants` + `TestCrashScoringMath` + `TestComputeSegmentCostV4` classes covering: locked outer constants, severity weight ratios, fatal cap value, compute_segment_cost arithmetic and signature, deprecated-but-importable normalize_weights (D-10-19)"
    - "Test command `cd backend && DATABASE_URL='postgresql://rq:rqpass@127.0.0.1:5432/roadquality' AUTH_SIGNING_KEY='test_secret_do_not_use_in_production_padding_padding' PYTHONPATH=. /tmp/rq-venv/bin/python -m pytest tests/test_scoring.py -x -q` exits 0 with all tests pass (existing 9 + new ≥8)"
  artifacts:
    - path: backend/app/scoring.py
      provides: "Module constants (W_IRI, W_POT, W_CRASH, FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT, FATAL_CAP_K, FATAL_CAP), 4-arg compute_segment_cost, deprecated-but-importable normalize_weights"
      exports:
        - "W_IRI"
        - "W_POT"
        - "W_CRASH"
        - "FATAL_WEIGHT"
        - "INJURY_WEIGHT"
        - "PDO_WEIGHT"
        - "FATAL_CAP_K"
        - "FATAL_CAP"
        - "compute_segment_cost"
        - "normalize_weights"
      contains: "DEPRECATED v0.4.0"
      min_lines: 60
    - path: backend/tests/test_scoring.py
      provides: "≥8 new unit tests in TestLockedConstants + TestCrashScoringMath + TestComputeSegmentCostV4 classes, plus the 6 preserved TestNormalizeWeights tests, plus 3 preserved TestComputeSegmentCost tests adapted to old-signature import path (or deleted in favor of V4 class — see Task 2)"
      contains: "class TestLockedConstants"
  key_links:
    - from: "backend/tests/test_scoring.py"
      to: "backend/app/scoring.py"
      via: "from app.scoring import W_IRI, W_POT, W_CRASH, FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT, FATAL_CAP, FATAL_CAP_K, compute_segment_cost, normalize_weights"
      pattern: "from app.scoring import"
    - from: "backend/app/scoring.py (W_IRI, W_POT, W_CRASH, severity constants, compute_segment_cost)"
      to: "scripts/compute_scores.py (Plan 10-02) and backend/app/routes/routing.py (Plan 10-03)"
      via: "module-level import"
      pattern: "from app.scoring import"
---

<objective>
Build the scoring foundation: replace v0.2.0's user-tunable weight normalization with locked module constants, introduce severity weight constants for the crash formula, and rewrite `compute_segment_cost` to a 4-argument signature that includes `crash_norm`. Both downstream plans (10-02 correlated subquery, 10-03 routing.py swap) import from this file — it MUST land first.

Purpose: This is the single source of truth for all scoring constants in v0.4.0 — both the offline `compute_scores.py` UPDATE pipeline and the online `routing.py` cost computation. Severity weights (8:3:1, NOT academic 100:10:1) live here per Pitfall 5; the single-fatal cap (K=3 → 24 raw severity units) lives here per D-10-05. `normalize_weights()` is intentionally retained-but-deprecated so the existing 6 v0.2.0 unit tests in `TestNormalizeWeights` continue to pass unchanged (D-10-03, D-10-21).

Output: Updated `backend/app/scoring.py` with new constants + new `compute_segment_cost` signature; extended `backend/tests/test_scoring.py` with ≥8 new tests pinning the constants and behavior (RED→GREEN per TDD).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-crash-scoring-formula-locked-weight-routing-api/10-CONTEXT.md
@.planning/phases/10-crash-scoring-formula-locked-weight-routing-api/10-RESEARCH.md
@backend/app/scoring.py
@backend/tests/test_scoring.py

<interfaces>
<!-- Existing exports from backend/app/scoring.py (35 LOC, pre-Phase-10). -->
<!-- These are the contracts callers currently rely on. -->

```python
# backend/app/scoring.py — current state (35 LOC)

def normalize_weights(
    include_iri: bool,
    include_potholes: bool,
    weight_iri: float,
    weight_potholes: float,
) -> tuple[float, float]:
    """Returns (w_iri, w_pot) summing to 1.0 (or both 0.0)."""
    # ... existing logic unchanged ...

def compute_segment_cost(
    travel_time_s: float,
    iri_norm: float,
    pothole_score_total: float,
    w_iri: float,
    w_pot: float,
) -> float:
    """OLD signature — Plan 10-01 REPLACES this with the 4-arg form."""
    return travel_time_s + w_iri * iri_norm + w_pot * pothole_score_total
```

<!-- Existing TestNormalizeWeights class (6 tests in test_scoring.py:5-52) — MUST keep passing. -->
<!-- Existing TestComputeSegmentCost class (3 tests in test_scoring.py:55-86) — uses OLD 5-arg signature; will be REPLACED with new TestComputeSegmentCostV4 class that exercises the new 4-arg signature. -->
<!-- The 3 OLD TestComputeSegmentCost tests are TIGHTLY COUPLED to the OLD signature; they CANNOT be preserved. Delete them and replace with the new V4 class. This is the only "breaking" intra-file test change in this phase — verified safe because no other file imports from TestComputeSegmentCost (grep confirms). -->
</interfaces>

<callers_who_will_break_until_their_plan_lands>
<!-- IMPORTANT: After Plan 10-01 lands, these two callers WILL FAIL because they still pass the OLD 5-arg signature. -->
<!-- This is INTENTIONAL — they get fixed in Plans 10-02 and 10-03 in Wave 2. -->
<!-- DO NOT touch these files in Plan 10-01 — they belong to other plans' file_modified scope. -->

- backend/app/routes/routing.py:8 — imports `normalize_weights, compute_segment_cost` (still works; normalize_weights still exists, compute_segment_cost is REDEFINED with new signature)
- backend/app/routes/routing.py:245-248 — calls `normalize_weights(...)` (still works, deprecated import)
- backend/app/routes/routing.py:424 — calls `compute_segment_cost(t, iri, pot, w_iri, w_pot)` (THIS WILL TypeError after 10-01 lands; Plan 10-03 fixes by switching to the new 4-arg form)

Therefore: between the end of 10-01 and the start of 10-03, the integration tests in `test_route.py` will fail with TypeError. This is acceptable — Plan 10-03 lands in the same Wave 2 and runs in parallel with 10-02. The phase verification gate runs only after BOTH 10-02 and 10-03 are merged.

Smoke test that proves 10-01 didn't break unit-level scoping: `pytest tests/test_scoring.py -x -q` passes (this plan's own gate).
</callers_who_will_break_until_their_plan_lands>
</context>

<prereqs>
Files that must exist BEFORE this plan runs (verified 2026-05-08):
- `backend/app/scoring.py` (35 LOC, exports `normalize_weights` + old-signature `compute_segment_cost`)
- `backend/tests/test_scoring.py` (87 LOC, 9 tests across `TestNormalizeWeights` + `TestComputeSegmentCost`)
- `backend/tests/conftest.py` (registers `integration` marker — irrelevant for this pure-python plan but referenced for shared fixtures down the line)
- `/tmp/rq-venv` Python 3.12 host venv with `pytest` installed (per project runtime memory)

DB state required: NONE. This plan is pure-python; no live PostGIS connection required for any test in this plan.
</prereqs>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED then GREEN — Add scoring constants + new 4-arg compute_segment_cost; deprecate normalize_weights</name>
  <files>backend/app/scoring.py, backend/tests/test_scoring.py</files>
  <behavior>
    Per CONTEXT.md decisions D-10-01 through D-10-05 + RESEARCH.md Code Example 1, this task introduces:
    - Outer weight constants: `W_IRI = 0.40`, `W_POT = 0.35`, `W_CRASH = 0.25` (sum to 1.0 by construction; D-10-01).
    - Severity constants: `FATAL_WEIGHT = 8`, `INJURY_WEIGHT = 3`, `PDO_WEIGHT = 1` (D-10-04 — literature-converged 8:3:1, NOT academic 100:10:1; Pitfall 5).
    - Single-fatal cap: `FATAL_CAP_K = 3`, `FATAL_CAP = FATAL_CAP_K * FATAL_WEIGHT  # = 24` (D-10-05; researcher recommendation per RESEARCH §Alternatives Considered "whole-sum cap with K=3").
    - New 4-arg `compute_segment_cost(travel_time_s, iri_norm, pothole_total, crash_norm) -> float` returns `travel_time_s + W_IRI*iri_norm + W_POT*pothole_total + W_CRASH*crash_norm` with NO normalization step (D-10-02 — constants already sum to 1.0).
    - `normalize_weights()` is preserved with `# DEPRECATED v0.4.0 — remove after one milestone of confidence (D-10-03)` in the function docstring AND a sibling module-level comment line above it. Function body UNCHANGED so the existing 6 `TestNormalizeWeights` tests pass without edits.

    TDD test plan (write tests FIRST in test_scoring.py, confirm RED, then implement; ≥8 NEW tests required by D-10-19):

    Test 1 (`TestLockedConstants::test_outer_weights_exact_floats`): `assert W_IRI == 0.40` AND `assert W_POT == 0.35` AND `assert W_CRASH == 0.25` — exact literal float comparison (no `abs(... - 0.40) < 1e-9` softening; the constants are exact floats per D-10-01).

    Test 2 (`TestLockedConstants::test_outer_weights_sum_to_one`): `assert abs((W_IRI + W_POT + W_CRASH) - 1.0) < 1e-9`. (Floating-point safety belt; 0.40 + 0.35 + 0.25 = 1.0 exactly in IEEE 754, but the assertion documents the invariant.)

    Test 3 (`TestCrashScoringMath::test_severity_weight_ratios`): `assert FATAL_WEIGHT == 8 * PDO_WEIGHT` AND `assert INJURY_WEIGHT == 3 * PDO_WEIGHT` AND `assert FATAL_WEIGHT == (8.0/3.0) * INJURY_WEIGHT - 0.0  # i.e. INJURY_WEIGHT * 8/3 == FATAL_WEIGHT exactly` — use `assert FATAL_WEIGHT * 3 == INJURY_WEIGHT * 8` to dodge floats. Pins Pitfall 5.

    Test 4 (`TestCrashScoringMath::test_severity_constants_are_named_integers`): `from app.scoring import FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT` does not raise; `assert isinstance(FATAL_WEIGHT, int)` (or float — D-10-04 says "named constants"; pin whichever the implementation chooses, but D-10-19 + RESEARCH Code Example 1 uses `int`).

    Test 5 (`TestCrashScoringMath::test_fatal_cap_value`): `from app.scoring import FATAL_CAP_K, FATAL_CAP` — `assert FATAL_CAP_K == 3` AND `assert FATAL_CAP == 24` AND `assert FATAL_CAP == FATAL_CAP_K * FATAL_WEIGHT`. Pins D-10-05 + researcher's K=3 recommendation; recalibration is a one-line constant change.

    Test 6 (`TestComputeSegmentCostV4::test_basic_arithmetic`): `cost = compute_segment_cost(travel_time_s=100.0, iri_norm=0.5, pothole_total=2.0, crash_norm=0.3)`. Expected: `100 + 0.40*0.5 + 0.35*2.0 + 0.25*0.3 = 100 + 0.20 + 0.70 + 0.075 = 100.975`. `assert abs(cost - 100.975) < 1e-9`.

    Test 7 (`TestComputeSegmentCostV4::test_signature_is_four_args_no_kwargs`): Use `inspect.signature(compute_segment_cost).parameters` to assert the parameter names are exactly `["travel_time_s", "iri_norm", "pothole_total", "crash_norm"]` and the count is 4 (NOT the old 5 with `w_iri, w_pot`). Pins D-10-02 — guards against accidental signature drift.

    Test 8 (`TestComputeSegmentCostV4::test_zero_inputs_equal_travel_time`): `cost = compute_segment_cost(travel_time_s=200.0, iri_norm=0.0, pothole_total=0.0, crash_norm=0.0)` — `assert abs(cost - 200.0) < 1e-9`. Sanity belt: zero-quality-data segments cost exactly travel time.

    Test 9 (`TestComputeSegmentCostV4::test_no_normalization_step`): `cost = compute_segment_cost(travel_time_s=0.0, iri_norm=1.0, pothole_total=1.0, crash_norm=1.0)` — `assert abs(cost - 1.0) < 1e-9`. Pins D-10-02's "no normalization step" — if the implementer accidentally renormalized over `(W_IRI + W_POT + W_CRASH)` the value would still be 1.0; this test alone doesn't catch that, BUT combined with Test 6 (which has heterogeneous values that would diverge under double-normalization) it does.

    Test 10 (`TestComputeSegmentCostV4::test_iri_only_contribution`): `cost = compute_segment_cost(travel_time_s=0.0, iri_norm=10.0, pothole_total=0.0, crash_norm=0.0)` — `assert abs(cost - 4.0) < 1e-9` (10 * 0.40 = 4.0). Confirms `W_IRI` is multiplied not added. Symmetric variants for pothole_total (W_POT=0.35) and crash_norm (W_CRASH=0.25) are encouraged but not required.

    Test 11 (`TestNormalizeWeightsDeprecated::test_still_importable`): `from app.scoring import normalize_weights; assert callable(normalize_weights)`. Pins D-10-03 — function MUST remain importable. The body docstring or comment must contain the literal substring "DEPRECATED v0.4.0" (`assert "DEPRECATED v0.4.0" in (normalize_weights.__doc__ or "") or "DEPRECATED v0.4.0" in inspect.getsource(normalize_weights)`).

    EXISTING tests preservation:
    - `TestNormalizeWeights` (6 tests, lines 5-52 of test_scoring.py): MUST PASS UNCHANGED. Do NOT delete these tests; they pin the deprecated function's behavior so it can be safely removed in a future milestone.
    - `TestComputeSegmentCost` (3 tests, lines 55-86 of test_scoring.py): These use the OLD 5-arg signature. They CANNOT be preserved because the function body changes signature. DELETE this class entirely. The 3 deleted tests are SUPERSEDED by the new `TestComputeSegmentCostV4` class above (Tests 6-10) which provide stronger and broader coverage of the new contract.

    RED phase verification: After writing the new tests but BEFORE editing `scoring.py`, the new tests must fail with ImportError (constants don't exist) or TypeError (function signature mismatch). Document the RED state in the task commit message body.

    GREEN phase verification: After editing `scoring.py`, all tests pass.
  </behavior>
  <action>
    1. RED phase. Edit `backend/tests/test_scoring.py`:
       (a) Update the import line at the top to: `from app.scoring import (normalize_weights, compute_segment_cost, W_IRI, W_POT, W_CRASH, FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT, FATAL_CAP_K, FATAL_CAP)` and `import inspect` for Test 7.
       (b) DELETE the `TestComputeSegmentCost` class (lines 55-86) — these 3 tests are tightly coupled to the OLD 5-arg signature and are superseded by `TestComputeSegmentCostV4`.
       (c) PRESERVE the `TestNormalizeWeights` class (lines 5-52) UNCHANGED.
       (d) ADD three new test classes at the end of the file: `TestLockedConstants` (Tests 1-2), `TestCrashScoringMath` (Tests 3-5), `TestComputeSegmentCostV4` (Tests 6-10), `TestNormalizeWeightsDeprecated` (Test 11). Use the test bodies described in `<behavior>`.
       (e) Run `cd backend && DATABASE_URL='postgresql://rq:rqpass@127.0.0.1:5432/roadquality' AUTH_SIGNING_KEY='test_secret_do_not_use_in_production_padding_padding' PYTHONPATH=. /tmp/rq-venv/bin/python -m pytest tests/test_scoring.py -x -q` and confirm RED — collection itself will fail with `ImportError: cannot import name 'W_IRI'`. (Note: this is an import-time failure, so all tests in the file appear failed at the collection stage. That's fine — the RED state is documented; proceed to GREEN.)
       (f) Commit (atomic): `test(10-01): add failing unit tests for locked weights + 4-arg compute_segment_cost + deprecated normalize_weights`. Commit message body must include "RED state: import fails because W_IRI / W_POT / W_CRASH / FATAL_WEIGHT / INJURY_WEIGHT / PDO_WEIGHT / FATAL_CAP_K / FATAL_CAP / new compute_segment_cost signature do not yet exist in app.scoring."

    2. GREEN phase. Edit `backend/app/scoring.py` (replace contents wholesale; the file is only 35 LOC). Reference RESEARCH.md §Code Examples → Example 1 for the canonical shape:
       (a) Add module docstring describing v0.4.0 transition (locked weights replacing user sliders) — verbatim from RESEARCH Example 1's docstring.
       (b) Define 8 module-level constants: `W_IRI: float = 0.40`, `W_POT: float = 0.35`, `W_CRASH: float = 0.25`, `FATAL_WEIGHT: int = 8`, `INJURY_WEIGHT: int = 3`, `PDO_WEIGHT: int = 1`, `FATAL_CAP_K: int = 3`, `FATAL_CAP: int = FATAL_CAP_K * FATAL_WEIGHT  # = 24`. Each MUST have an inline comment citing the relevant decision ID (D-10-01, D-10-04, D-10-05) AND, for the severity constants, the Pitfall-5 anti-pattern reference.
       (c) Define new `compute_segment_cost(travel_time_s: float, iri_norm: float, pothole_total: float, crash_norm: float) -> float` with body `return travel_time_s + W_IRI * iri_norm + W_POT * pothole_total + W_CRASH * crash_norm`. Docstring must mention "no normalization step (D-10-02)" because constants sum to 1.0.
       (d) Preserve `normalize_weights(include_iri, include_potholes, weight_iri, weight_potholes) -> tuple[float, float]` body UNCHANGED. Update the docstring to:
           ```
           [DEPRECATED v0.4.0] Use module constants W_IRI / W_POT / W_CRASH instead.
           Kept-but-unused for one milestone of confidence (D-10-03). Removal deferred.
           ```
           AND add a sibling module-level comment line `# DEPRECATED v0.4.0 — remove after one milestone of confidence (D-10-03)` immediately above the `def normalize_weights(...)` line.
       (e) Run the same pytest command; confirm GREEN — all ≥8 new tests pass + all 6 existing `TestNormalizeWeights` tests pass.
       (f) Commit (atomic): `feat(10-01): lock outer weights (40/35/25), severity (8:3:1) + cap K=3, deprecate normalize_weights per D-10-01..D-10-05`.

    Total commits this task: 2 (test → feat); each is a single atomic commit per project convention.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL='postgresql://rq:rqpass@127.0.0.1:5432/roadquality' AUTH_SIGNING_KEY='test_secret_do_not_use_in_production_padding_padding' PYTHONPATH=. /tmp/rq-venv/bin/python -m pytest tests/test_scoring.py -x -q</automated>
  </verify>
  <done>
    - File `backend/app/scoring.py` exports W_IRI, W_POT, W_CRASH, FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT, FATAL_CAP_K, FATAL_CAP, the new 4-arg compute_segment_cost, and the deprecated-but-importable normalize_weights.
    - All 6 existing `TestNormalizeWeights` tests pass UNCHANGED.
    - All ≥8 new tests across `TestLockedConstants`, `TestCrashScoringMath`, `TestComputeSegmentCostV4`, `TestNormalizeWeightsDeprecated` pass.
    - Old `TestComputeSegmentCost` class is deleted (its tests are superseded by `TestComputeSegmentCostV4`).
    - Two atomic commits exist on main: `test(10-01): ...` and `feat(10-01): ...`.
    - The string `DEPRECATED v0.4.0` appears in `scoring.py` (verifiable via `grep -c 'DEPRECATED v0.4.0' backend/app/scoring.py` ≥ 1).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Cap-arithmetic helper assertion + per-segment formula sanity tests (extends TestCrashScoringMath)</name>
  <files>backend/tests/test_scoring.py</files>
  <behavior>
    Pure-Python verification of the cap math, length-floor math, and p95-clip math that compute_scores.py (Plan 10-02) will execute in SQL. These are unit tests of the FORMULAS — not of the SQL — so they live in test_scoring.py with no DB dependency. They pin the numerical contract before Plan 10-02 implements the SQL form, so any drift between researcher's recommendation and what gets shipped is caught here.

    Test 12 (`TestCrashScoringMath::test_single_fatal_cap_clamps_raw_sum`): A helper `_capped(raw_sum)` that returns `min(raw_sum, FATAL_CAP)` should clamp `raw_sum=24` to 24 (no clamp at boundary), `raw_sum=25` to 24 (clamp), `raw_sum=100` to 24 (severe clamp), `raw_sum=10` to 10 (below cap, no-op). Define the helper inline in the test (1-line lambda or function) — it mirrors the SQL `LEAST(raw_sum, FATAL_CAP)` expression researcher recommends. This is a contract-level test: it documents that the cap is a max-clamp, not a scale.

    Test 13 (`TestCrashScoringMath::test_length_floor_50m_in_km_units`): A helper `_floor_km(length_m)` that returns `max(length_m / 1000.0, 0.05)` should return 0.05 for `length_m=10` (below floor), 0.05 for `length_m=49` (below floor), 0.05 for `length_m=50` (boundary; `50/1000 == 0.05`), `0.06` for `length_m=60` (above floor; `60/1000 == 0.06`), `1.5` for `length_m=1500` (typical block). Pins D-10-07 + Pitfall 5's column-name fix (`length_m` divided by 1000.0, NOT `length_km`).

    Test 14 (`TestCrashScoringMath::test_p95_clip_to_one`): A helper `_p95_normalize(raw_per_km, p95)` that returns `min(1.0, raw_per_km / p95)` should return 1.0 for `raw_per_km=20, p95=10` (clip), 0.5 for `raw_per_km=5, p95=10` (no clip), 1.0 for `raw_per_km=10, p95=10` (boundary), 0.0 for `raw_per_km=0, p95=10` (no crashes). Pins D-10-08 + RESEARCH §Pattern 1 `LEAST(1.0, raw_per_km / p95)` clamp.

    Test 15 (`TestCrashScoringMath::test_zero_p95_yields_zero_or_safety_net`): For the degenerate case `p95 == 0` (no crashes anywhere — all zeros — Pitfall 6), the helper should NOT divide by zero. Define `_p95_normalize_safe(raw_per_km, p95)` returning `0.0 if p95 == 0 else min(1.0, raw_per_km / p95)`. Test asserts `_p95_normalize_safe(5, 0) == 0.0`. Pins D-10-09 (zero-crash segments default to 0) and the SQL `NULLIF(p95, 0)` defensive idiom that Plan 10-02 will use.

    Test 16 (`TestCrashScoringMath::test_zero_crash_segment_default_norm`): Compose the three helpers end-to-end: a segment with `raw_sum=0, length_m=200` → `_floor_km(200) = 0.2`, `_capped(0) = 0`, `raw_per_km = 0/0.2 = 0`, `_p95_normalize_safe(0, 5) = 0.0`. Final crash_norm == 0.0. Pins D-10-09 — the migration-default 0.0 stays 0.0 for any zero-crash segment.

    Test 17 (`TestCrashScoringMath::test_canonical_segment_below_p95`): Compose helpers for a realistic segment: `raw_sum = 2*INJURY_WEIGHT + 1*PDO_WEIGHT = 2*3 + 1 = 7`, `length_m = 500` → `_floor_km(500) = 0.5`, `_capped(7) = 7` (below cap), `raw_per_km = 7/0.5 = 14`, with `p95 = 30` → `crash_norm = _p95_normalize_safe(14, 30) = 14/30 ≈ 0.467`. `assert abs(crash_norm - 14.0/30.0) < 1e-9`. Pins the realistic, non-bimodal target distribution — exactly the kind of value Pitfall 5's "≥50% in [0.05, 0.5]" check is designed to find.

    These 6 new tests bring the new-test count to ≥17 (well above the D-10-19 floor of 8) and provide a Python-level sanity belt that Plan 10-02's SQL must reproduce numerically — if the SQL drifts, the histogram smoke test in 10-02 catches it; if these helpers drift, the SQL diverges from spec. Both directions are guarded.
  </behavior>
  <action>
    1. Edit `backend/tests/test_scoring.py`:
       (a) Inside the existing `TestCrashScoringMath` class (created in Task 1), add tests 12-17 from `<behavior>`. Keep the inline helpers (`_capped`, `_floor_km`, `_p95_normalize_safe`) co-located inside the class as static methods OR module-level helpers above the class — pick whichever pytest discovery style the existing test file already uses. Either is acceptable.
       (b) Add the line `import math` at the top of the file if any of the assertions need `math.isclose` (optional; the `abs(... - expected) < 1e-9` pattern from existing tests is preferred for consistency).
    2. Run `cd backend && DATABASE_URL='postgresql://rq:rqpass@127.0.0.1:5432/roadquality' AUTH_SIGNING_KEY='test_secret_do_not_use_in_production_padding_padding' PYTHONPATH=. /tmp/rq-venv/bin/python -m pytest tests/test_scoring.py -x -q`. Confirm GREEN — all 6 new tests pass plus the 11 prior tests from Task 1 plus the 6 preserved `TestNormalizeWeights` tests = 23 total in this file.
    3. Commit (atomic): `test(10-01): add cap/length-floor/p95-clip helper tests pinning the SQL formula contract for plan 10-02`.

    Total commits this task: 1 (test-only — no production code change here, so no separate "feat" commit).
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL='postgresql://rq:rqpass@127.0.0.1:5432/roadquality' AUTH_SIGNING_KEY='test_secret_do_not_use_in_production_padding_padding' PYTHONPATH=. /tmp/rq-venv/bin/python -m pytest tests/test_scoring.py -x -q</automated>
  </verify>
  <done>
    - `backend/tests/test_scoring.py` contains ≥17 NEW unit tests (well above D-10-19 floor of 8) plus the 6 preserved `TestNormalizeWeights` tests = 23 total tests in this file.
    - All 23 tests pass.
    - One atomic commit exists on main: `test(10-01): add cap/length-floor/p95-clip helper tests ...`.
    - Plan 10-02's executor can copy these helper formulas directly into its SQL with confidence that the numerical contract is pinned.
  </done>
</task>

</tasks>

<verification>
**Per-task verification (executor runs these on its own as it implements):**
- After RED commit of Task 1: `pytest tests/test_scoring.py -x -q` should FAIL at collection (ImportError on the new constants). Document the failure in the commit body. Move to GREEN.
- After GREEN commit of Task 1: `pytest tests/test_scoring.py -x -q` should PASS — 17 tests (6 preserved + 11 new).
- After Task 2 commit: `pytest tests/test_scoring.py -x -q` should PASS — 23 tests (6 preserved + 17 new).

**Plan-level gate (executor runs at end of plan):**

```
cd backend && \
  DATABASE_URL='postgresql://rq:rqpass@127.0.0.1:5432/roadquality' \
  AUTH_SIGNING_KEY='test_secret_do_not_use_in_production_padding_padding' \
  PYTHONPATH=. \
  /tmp/rq-venv/bin/python -m pytest tests/test_scoring.py -x -q
```

Expected: `23 passed in <1.0s` (pure-python; no DB hit).

**Constants spot-check (sanity):**

```
cd backend && PYTHONPATH=. /tmp/rq-venv/bin/python -c "from app.scoring import W_IRI, W_POT, W_CRASH, FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT, FATAL_CAP_K, FATAL_CAP, compute_segment_cost; print(W_IRI, W_POT, W_CRASH, FATAL_WEIGHT, INJURY_WEIGHT, PDO_WEIGHT, FATAL_CAP_K, FATAL_CAP); print(compute_segment_cost(100.0, 0.5, 2.0, 0.3))"
```

Expected output:
```
0.4 0.35 0.25 8 3 1 3 24
100.975
```

**Deprecation marker spot-check:**
`grep -c 'DEPRECATED v0.4.0' backend/app/scoring.py` should print ≥ 1.

**Out-of-scope gate (Plan 10-01 does NOT validate these — they belong to Plans 10-02 / 10-03):**
- `tests/test_route.py` will FAIL after this plan lands (TypeError on routing.py:424 calling old 5-arg signature). This is INTENTIONAL — Plan 10-03 fixes routing.py. Do not panic; do not partial-fix routing.py here.
- `tests/test_compute_scores_source.py` is unaffected by this plan (no SQL change yet).
</verification>

<success_criteria>
- [x] `backend/app/scoring.py` exports W_IRI=0.40, W_POT=0.35, W_CRASH=0.25 as module-level float constants (D-10-01).
- [x] `backend/app/scoring.py` exports FATAL_WEIGHT=8, INJURY_WEIGHT=3, PDO_WEIGHT=1, FATAL_CAP_K=3, FATAL_CAP=24 as module-level constants (D-10-04, D-10-05).
- [x] `compute_segment_cost(travel_time_s, iri_norm, pothole_total, crash_norm)` is a 4-arg function returning the locked-weight cost without any normalization step (D-10-02). The signature is verified via `inspect.signature` in Test 7.
- [x] `normalize_weights()` is still importable from `app.scoring`, has `DEPRECATED v0.4.0` in its docstring or a sibling comment, and the existing 6 `TestNormalizeWeights` tests pass UNCHANGED (D-10-03, D-10-21).
- [x] `backend/tests/test_scoring.py` contains ≥8 new unit tests (actual: 17) covering the locked constants, severity ratios, fatal cap, length floor, p95 clip, zero-cap safety, and full per-segment formula sanity (D-10-19).
- [x] All 23 tests in `tests/test_scoring.py` pass via the host-venv command above (D-10-22).
- [x] Three atomic commits on main:
  1. `test(10-01): add failing unit tests for locked weights + 4-arg compute_segment_cost + deprecated normalize_weights`
  2. `feat(10-01): lock outer weights (40/35/25), severity (8:3:1) + cap K=3, deprecate normalize_weights per D-10-01..D-10-05`
  3. `test(10-01): add cap/length-floor/p95-clip helper tests pinning the SQL formula contract for plan 10-02`
- [x] No file outside `files_modified` is touched (no overlap with Plans 10-02 / 10-03 — verifiable via `git diff --name-only`).
</success_criteria>

<output>
After completion, create `.planning/phases/10-crash-scoring-formula-locked-weight-routing-api/10-01-SUMMARY.md` per the GSD summary template.

The summary MUST include:
- Final test count by class (TestNormalizeWeights, TestNormalizeWeightsDeprecated, TestLockedConstants, TestCrashScoringMath, TestComputeSegmentCostV4) and total pass count.
- Confirmation that the 6 v0.2.0 `TestNormalizeWeights` tests passed unchanged (Phase-10 backward-compat gate).
- The exact commit SHAs for each of the 3 atomic commits.
- A "Known broken" note flagging that `tests/test_route.py` will TypeError until Plan 10-03 lands — for awareness during Wave 2.
- A pointer to Plan 10-02 + Plan 10-03 noting that both depend on the constants exported by this plan (`W_IRI`, `W_POT`, `W_CRASH`, `FATAL_WEIGHT`, `INJURY_WEIGHT`, `PDO_WEIGHT`, `FATAL_CAP`, `compute_segment_cost`).
</output>
