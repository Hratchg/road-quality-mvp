---
phase: 01-mvp-integrity-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: true
requirements: [REQ-mvp-integrity-cleanup]

must_haves:
  truths:
    - "A .env.example file exists at repo root"
    - ".env.example lists DATABASE_URL with a non-secret placeholder value"
    - ".env.example lists VITE_API_URL with a non-secret placeholder value"
    - ".env.example lists VITE_MAPBOX_TOKEN with an empty-string placeholder (matches current operational default)"
    - "Every env var actually read by a committed source file in this repo appears in .env.example"
    - "No real secret (no actual Mapbox token, no production DB password) is committed in .env.example"
    - ".env.example is tracked by git (not matched by .gitignore's .env pattern)"
  artifacts:
    - path: ".env.example"
      provides: "Repo-root template listing every environment variable the stack reads, with placeholder (non-secret) defaults and one-line descriptions"
      contains: "DATABASE_URL"
  key_links:
    - from: "source files that read env vars (backend/app/db.py, scripts/*.py, frontend/src/api.ts, docker-compose.yml)"
      to: ".env.example (repo root)"
      via: "every env var name in source MUST appear in .env.example with a safe placeholder"
      pattern: "DATABASE_URL|VITE_API_URL|VITE_MAPBOX_TOKEN"
---

<objective>
Create a `.env.example` file at repo root that documents every environment variable the stack reads, with safe placeholder values. Resolves `.planning/codebase/CONCERNS.md` "No .env.example File" (Configuration Smells section) and addresses Phase 1 Success Criterion #5 from the ROADMAP.

**Current state (verified during planning):**
- No `.env.example` exists at repo root (`ls .env.example` returned "No such file").
- `.gitignore` line 25 excludes `.env` but NOT `.env.example` — so the template WILL be committable.
- Env vars actually read by committed source files (verified via grep):
  - `DATABASE_URL` — read by `backend/app/db.py:5`, `scripts/seed_data.py:15`, `scripts/compute_scores.py:6`, `scripts/ingest_iri.py:41`.
  - `VITE_API_URL` — read by `frontend/src/api.ts:1`.
  - `VITE_MAPBOX_TOKEN` — declared in `docker-compose.yml:38` (empty default) and documented in `docs/SETUP.md:392` as an optional Mapbox tile token. Not yet consumed by frontend source code, but documented as an operational env var, so it belongs in .env.example.
- No other env vars are read from Python `os.environ.*` / `os.getenv` or TypeScript `import.meta.env.*` / `process.env.*` in committed source (confirmed via grep).

**Phase 1 Success Criterion #5 (ROADMAP, authoritative):** "A `.env.example` lives at repo root listing every env var the stack reads (DATABASE_URL, VITE_API_URL, VITE_MAPBOX_TOKEN, and any new ones added in later phases)." Also: "Grep for other `os.environ[...]`, `os.getenv(...)`, `import.meta.env.*`, `process.env.*` usages and add any missing vars with placeholder values (not real secrets)."

**Purpose:** New contributors should be able to copy `.env.example` to `.env`, fill in any secrets they need, and run the stack. Future M1 phases (esp. Phase 3 Mapillary token, Phase 4 auth signing key, Phase 5 CORS origin) will extend this file — this plan establishes the template so those extensions are additive.

**Output:** A single new file at `/Users/hratchghanime/road-quality-mvp/.env.example` containing documented placeholders for the three current env vars, plus a SUMMARY entry.

**Non-goals:**
- Do NOT create a `.env` file (secrets are per-developer; `.env.example` is the TEMPLATE).
- Do NOT modify `.gitignore` — `.env.example` is not `.env` so it is already trackable.
- Do NOT move or refactor any existing env-var reads. `backend/app/db.py` still has its hardcoded localhost default; that is a Phase 5 concern.
- Do NOT add env vars that are NOT read by any committed source (no speculative "future" vars). Phase 2+ plans will append as they introduce new vars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Every env-var read in committed source, with the file and line where it is consumed. -->
<!-- The executor uses this list verbatim to assemble .env.example. Task 1 reruns the grep to confirm the list is still accurate at execution time. -->

Backend Python — os.environ.get calls:
- backend/app/db.py:5            -> DATABASE_URL
- scripts/seed_data.py:15        -> DATABASE_URL
- scripts/compute_scores.py:6    -> DATABASE_URL
- scripts/ingest_iri.py:41       -> DATABASE_URL

Frontend TypeScript — import.meta.env:
- frontend/src/api.ts:1          -> VITE_API_URL (const API_BASE = import.meta.env.VITE_API_URL || "/api";)

docker-compose.yml declarations (env-vars passed into frontend container):
- docker-compose.yml:37          -> VITE_API_URL: http://localhost:8000
- docker-compose.yml:38          -> VITE_MAPBOX_TOKEN: ""

docker-compose.yml also sets POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD inside the db service block (lines 5-7). These are Docker-service env vars used by the postgres image itself — NOT read by application code via os.environ. They are intentionally EXCLUDED from .env.example because:
(a) the app never reads them — they initialize the postgres container;
(b) they are the hardcoded dev defaults (rq / rqpass / roadquality) and Phase 5 will replace them with proper secret management;
(c) surfacing them in .env.example would normalize committing the dev password pattern.
If the executor believes these should be included, STOP and flag — do not unilaterally add DB-service credentials to .env.example.

Safe placeholder values to write (exact strings):
- DATABASE_URL       -> postgresql://rq:rqpass@localhost:5432/roadquality  (the current dev default, already in source as a fallback, not a secret)
- VITE_API_URL       -> http://localhost:8000  (the docker-compose default, already in source)
- VITE_MAPBOX_TOKEN  -> empty string  (matches docker-compose.yml's "" default; Mapbox is optional and not yet wired)

Gitignore state (DO NOT edit .gitignore — shown here so the executor understands .env.example is trackable):
```
# From .gitignore lines 24-26:
# Environment
.env
.env.local
```
`.env.example` is not matched by these patterns, so it will be added by `git add`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify env-var inventory is still accurate</name>
  <read_first>(none — this task is pure grep)</read_first>
  <files>(none — read-only)</files>
  <action>
    Re-run the grep commands below to confirm the env-var inventory in the `<interfaces>` block above is still accurate at execution time. The expected result is exactly three unique env-var names used in source code:
    - DATABASE_URL (in 4 Python files)
    - VITE_API_URL (in frontend/src/api.ts)
    - VITE_MAPBOX_TOKEN (declared in docker-compose.yml, not yet consumed by frontend source)

    If the grep reveals ADDITIONAL env vars that were added between planning and execution (e.g., a new `os.getenv("SOMETHING_NEW")` somewhere), STOP. Do NOT silently add the new var to .env.example — it may be incomplete or wrong. Instead, record the discovered additional vars in the SUMMARY under "Newly discovered env vars (out of scope for this plan)" and let the planner reassess. The whole point of SC #5 is a complete and correct inventory.

    If the grep reveals FEWER vars than expected (e.g., someone deleted a script between planning and execution), that is fine — build .env.example from whatever vars are actually present. But STILL record the discrepancy in the SUMMARY so the audit trail is intact.
  </action>
  <verify>
    <automated>grep -rnE "os\.environ\.get|os\.environ\[|os\.getenv" --include="*.py" --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.planning /Users/hratchghanime/road-quality-mvp/ &amp;&amp; grep -rnE "import\.meta\.env|process\.env" --include="*.ts" --include="*.tsx" --include="*.js" --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.planning /Users/hratchghanime/road-quality-mvp/</automated>
  </verify>
  <acceptance_criteria>
    - Python grep returns exactly 4 hits, all reading `DATABASE_URL` (one each in backend/app/db.py, scripts/seed_data.py, scripts/compute_scores.py, scripts/ingest_iri.py).
    - TypeScript/JS grep returns exactly 1 hit, reading `VITE_API_URL` in frontend/src/api.ts.
    - docker-compose.yml (not re-grepped in the verify because it uses YAML syntax not matched by the above patterns) is known via the `<interfaces>` block to declare VITE_MAPBOX_TOKEN.
    - The executor has a confirmed list of three env-var names: DATABASE_URL, VITE_API_URL, VITE_MAPBOX_TOKEN.
  </acceptance_criteria>
  <done>Env-var inventory for .env.example is confirmed: three vars (DATABASE_URL, VITE_API_URL, VITE_MAPBOX_TOKEN), no others.</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example at repo root</name>
  <read_first>
    - /Users/hratchghanime/road-quality-mvp/.gitignore (just lines 24-26 to confirm .env is ignored but .env.example is not — do NOT modify this file)
    - /Users/hratchghanime/road-quality-mvp/docker-compose.yml (just lines 25-40 to confirm current VITE_API_URL / VITE_MAPBOX_TOKEN defaults — do NOT modify)
  </read_first>
  <files>.env.example</files>
  <action>
    Create `.env.example` at repo root with the exact content below. Use the Write tool, not a heredoc.

    File content (everything between the BEGIN and END markers, exclusive; the markers themselves must NOT appear in the file):

    --- BEGIN FILE CONTENT ---
    # .env.example — environment variable template for road-quality-mvp
    #
    # Copy this file to .env and fill in any values you want to override.
    # .env is git-ignored; .env.example is committed.
    #
    # Variables listed here match what the codebase currently reads (backend, frontend, scripts).
    # Future M1 phases will extend this file as new env vars are introduced.

    # ----- Database -----
    # Consumed by: backend/app/db.py, scripts/seed_data.py, scripts/compute_scores.py, scripts/ingest_iri.py
    # The dev default below matches docker-compose.yml. Replace with your cloud DB URL in production (Phase 5).
    DATABASE_URL=postgresql://rq:rqpass@localhost:5432/roadquality

    # ----- Frontend -> Backend API -----
    # Consumed by: frontend/src/api.ts (import.meta.env.VITE_API_URL)
    # In local Vite dev, the dev-server proxies /api to the backend — leave blank or set to an absolute URL.
    # In Docker Compose, docker-compose.yml sets this to http://localhost:8000.
    # In production (Phase 5), set this to the deployed backend origin.
    VITE_API_URL=http://localhost:8000

    # ----- Mapbox (optional, Phase 5+ frontend tile upgrade) -----
    # Consumed by: frontend (not yet wired; currently declared empty in docker-compose.yml)
    # Leave blank to use the default Leaflet+OSM tiles. Set to a real Mapbox token to enable premium tiles.
    # NEVER commit a real token to git — this template stays empty.
    VITE_MAPBOX_TOKEN=
    --- END FILE CONTENT ---

    Exact placement: `/Users/hratchghanime/road-quality-mvp/.env.example` (dot-prefixed, at repo root, sibling to docker-compose.yml).

    Constraints:
    - All three var names appear with trailing `=` and either a safe dev default (DATABASE_URL, VITE_API_URL) or empty string (VITE_MAPBOX_TOKEN).
    - No real Mapbox token.
    - No production DB credentials.
    - Comments explain what reads each var and where to override.
    - File ends with a single newline.

    Implements Success Criterion #5 of Phase 1 (ROADMAP) and resolves `.planning/codebase/CONCERNS.md` "No .env.example File".
  </action>
  <verify>
    <automated>test -f /Users/hratchghanime/road-quality-mvp/.env.example &amp;&amp; grep -qE "^DATABASE_URL=" /Users/hratchghanime/road-quality-mvp/.env.example &amp;&amp; grep -qE "^VITE_API_URL=" /Users/hratchghanime/road-quality-mvp/.env.example &amp;&amp; grep -qE "^VITE_MAPBOX_TOKEN=" /Users/hratchghanime/road-quality-mvp/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/hratchghanime/road-quality-mvp/.env.example` exists.
    - `grep -cE "^DATABASE_URL=" .env.example` returns 1.
    - `grep -cE "^VITE_API_URL=" .env.example` returns 1.
    - `grep -cE "^VITE_MAPBOX_TOKEN=" .env.example` returns 1.
    - `grep -cE "^[A-Z_]+=" .env.example` returns exactly 3 (three env-var assignments, no extras).
    - The VITE_MAPBOX_TOKEN line's value is empty (the line matches `^VITE_MAPBOX_TOKEN=$`).
    - The file is NOT matched by `.gitignore` — `git check-ignore .env.example` exits non-zero.
    - No line in the file contains a plausible real secret pattern (no hex strings of length >= 32, no `sk_live_*`, no `pk_live_*`). A conservative check: `grep -cE "[a-f0-9]{32,}|sk_live|pk_live" .env.example` returns 0.
  </acceptance_criteria>
  <done>.env.example exists at repo root with exactly three documented env vars and no real secrets, trackable by git.</done>
</task>

<task type="auto">
  <name>Task 3: Write plan SUMMARY</name>
  <read_first>(none)</read_first>
  <files>.planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md</files>
  <action>
    Create the plan SUMMARY recording:
    - **Env-var inventory (Task 1):** DATABASE_URL, VITE_API_URL, VITE_MAPBOX_TOKEN (three vars, confirmed via grep at execution time).
    - **File created (Task 2):** `.env.example` at repo root, documented with comments explaining each var and where it is consumed.
    - **Intentionally excluded:** POSTGRES_DB / POSTGRES_USER / POSTGRES_PASSWORD (Docker-service env vars, not app-read; Phase 5 will handle proper secret management for these).
    - **No secrets committed:** VITE_MAPBOX_TOKEN is empty; DATABASE_URL and VITE_API_URL use the known dev defaults.
    - **CONCERNS.md "No .env.example File" — RESOLVED.**
    - **Forward compatibility:** future phases (Phase 3 Mapillary token, Phase 4 auth signing key, Phase 5 CORS origin / production DB URL) extend this file additively; do not edit existing entries.
    - **Evidence:** the grep output from Task 1, and the new-file ls+grep from Task 2.
  </action>
  <verify>
    <automated>test -f /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md &amp;&amp; grep -qE "\.env\.example" /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md &amp;&amp; grep -qE "CONCERNS.*RESOLVED" /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md` exists.
    - File contains the literal strings "DATABASE_URL", "VITE_API_URL", and "VITE_MAPBOX_TOKEN".
    - File contains "CONCERNS.md" and "RESOLVED".
    - File contains an explicit "Files modified" section listing `.env.example` (one file).
    - File contains an explicit "Forward compatibility" note naming Phase 3 / 4 / 5 as the phases that will extend .env.example.
  </acceptance_criteria>
  <done>SUMMARY exists, records the env-var inventory, the new file, and the closure of CONCERNS.md "No .env.example File".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

New-file plan. One trust boundary: `.env.example` is committed to the public repo, read by every future contributor and by CI pipelines. If it contained a real secret, the secret would be leaked on first push. Mitigation is the plan's explicit "no real secrets" constraint and acceptance checks that pattern-match for common secret signatures.

No runtime code changes, no network surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | .env.example content | mitigate | Task 2 acceptance criteria include a `grep -cE "[a-f0-9]{32,}\|sk_live\|pk_live" .env.example` check that MUST return 0. VITE_MAPBOX_TOKEN line MUST be empty (pattern `^VITE_MAPBOX_TOKEN=$`). The DATABASE_URL placeholder uses the well-known dev default already committed in source — not a secret. |
| T-01-04-02 | Information Disclosure | Accidental commit of .env | accept | .gitignore line 25 already ignores `.env`. This plan does not create `.env`, only `.env.example`. |
| T-01-04-03 | Tampering | .gitignore | mitigate | The plan forbids modifying `.gitignore`. Task 2 acceptance verifies `.env.example` is NOT ignored (`git check-ignore` exits non-zero), proving .gitignore was not altered to exclude the new template. |
| T-01-04-04 | Integrity (incomplete inventory) | Future debugging when an env var is "missing" | mitigate | Task 1 grep enumerates all env-var reads in committed source; Task 3 SUMMARY records the inventory and the intentional exclusions. Future phases inherit a clear contract: if you add a new `os.getenv` or `import.meta.env.*` read, you must also add it to `.env.example` in the same plan. |
| T-01-04-05 | Elevation of Privilege | Someone using `.env.example` values as production secrets | accept | The file's comment header and per-var comments explicitly tell the reader to override in production. Mitigation is documentation; enforcement happens in Phase 5 (prod deploy) which will check for dev-default credentials. |
</threat_model>

<verification_criteria>

```bash
# Full plan verification — run from repo root

# File exists, at the right path
test -f .env.example

# Exactly three env-var assignments, no more, no fewer
[ "$(grep -cE '^[A-Z_]+=' .env.example)" = "3" ]

# All three required vars present with correct names
grep -qE "^DATABASE_URL=" .env.example
grep -qE "^VITE_API_URL=" .env.example
grep -qE "^VITE_MAPBOX_TOKEN=" .env.example

# VITE_MAPBOX_TOKEN is empty (no committed token)
grep -qE "^VITE_MAPBOX_TOKEN=$" .env.example

# No plausible secret patterns (hex >= 32 chars, Stripe-style keys)
[ "$(grep -cE '[a-f0-9]{32,}|sk_live|pk_live' .env.example)" = "0" ]

# .env.example is NOT git-ignored (template must be committable)
! git check-ignore .env.example

# No unintended additional files modified (the only new file is .env.example)
[ "$(git status --porcelain -- .env.example | wc -l | tr -d ' ')" -ge "1" ]

# SUMMARY written with correct closures
test -f .planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md
grep -qE "CONCERNS.*RESOLVED" .planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md
```

</verification_criteria>

<success_criteria>
- `.env.example` exists at `/Users/hratchghanime/road-quality-mvp/.env.example`.
- It declares exactly three env vars — DATABASE_URL, VITE_API_URL, VITE_MAPBOX_TOKEN — with placeholder (non-secret) values.
- VITE_MAPBOX_TOKEN's value is an empty string.
- The file is not git-ignored (can be `git add`-ed and committed).
- No plausible real secret appears anywhere in the file.
- `.gitignore` is unchanged (no new lines, no removed lines).
- SUMMARY documents the env-var inventory, lists the one modified file (`.env.example`), and explicitly closes CONCERNS.md "No .env.example File".
</success_criteria>

<output>
After completion, `.planning/phases/01-mvp-integrity-cleanup/01-04-SUMMARY.md` must exist and document the new `.env.example` (repo-root template with three env vars), along with forward-compatibility notes for Phases 3/4/5 that will extend the file. Modified files: `.env.example` (one new file).
</output>
