---
phase: 01-mvp-integrity-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/2026-02-23-pothole-tracker-design.md
autonomous: true
requirements: [REQ-mvp-integrity-cleanup]

must_haves:
  truths:
    - "Zero occurrences of REACT_APP_MAPBOX_TOKEN remain in the repo (excluding .git, node_modules, .planning, and dist)"
    - "VITE_MAPBOX_TOKEN is the only Mapbox env-var identifier referenced in code and docs"
    - "The historical design doc docs/plans/2026-02-23-pothole-tracker-design.md still describes the Mapbox-via-env-var design decision, just with the correct Vite-era env-var name"
  artifacts:
    - path: "docs/plans/2026-02-23-pothole-tracker-design.md"
      provides: "Historical design doc updated to reference VITE_MAPBOX_TOKEN, matching operational reality"
      contains: "VITE_MAPBOX_TOKEN"
  key_links:
    - from: "operational code/config (docker-compose.yml line 38, docs/SETUP.md line 392)"
      to: "historical design doc (docs/plans/2026-02-23-pothole-tracker-design.md line 156)"
      via: "env-var identifier alignment — Vite prefix wins over CRA prefix"
      pattern: "VITE_MAPBOX_TOKEN"
---

<objective>
Retire the stale `REACT_APP_MAPBOX_TOKEN` identifier from the repo so `VITE_MAPBOX_TOKEN` is the only documented Mapbox env-var name. This closes ingest-conflict INFO row 2 (`INGEST-CONFLICTS.md`): the SPEC predates the project's Vite migration and uses the Create-React-App prefix, which does not match any implemented code path.

**Current state (verified during planning by grep):**
- `REACT_APP_MAPBOX_TOKEN` appears in the repo exactly once: `docs/plans/2026-02-23-pothole-tracker-design.md:156`.
- `VITE_MAPBOX_TOKEN` is the operational identifier, used in `docker-compose.yml:38`, `docs/SETUP.md:392`, and `docs/plans/2026-02-23-implementation-plan.md:132`.
- No frontend source file reads either identifier today (Mapbox tiles are not yet wired — the Vite var is declared empty in docker-compose).

**Phase 1 Success Criterion #3 (ROADMAP, authoritative):** "No reference to `REACT_APP_MAPBOX_TOKEN` anywhere in code or docs. `VITE_MAPBOX_TOKEN` is the only documented name. Grep the whole repo (excluding `.git`, `node_modules`) and replace every hit."

**Scope note:** The single remaining hit is in a historical design doc under `docs/plans/`. The planner's default is "don't rewrite history," but SC #3 explicitly says "replace every hit" across code and docs. The design doc's line 156 is describing a configuration mechanism that future readers will try to apply — leaving `REACT_APP_MAPBOX_TOKEN` there actively misleads. So this hit is in-scope per SC #3.

**Purpose:** A reader of the design doc in Phase 2+ planning must not be confused about which env-var name to use. One identifier, one source of truth.

**Output:**
- `docs/plans/2026-02-23-pothole-tracker-design.md` line 156 says `VITE_MAPBOX_TOKEN` instead of `REACT_APP_MAPBOX_TOKEN`.
- Repo-wide grep for `REACT_APP_MAPBOX_TOKEN` returns zero hits.
- SUMMARY records the find/replace and closes INGEST-CONFLICTS INFO row 2.

**Non-goals:**
- Do NOT rewrite any other part of the design doc (it is dated 2026-02-23 and is a historical artifact; only this one env-var identifier is being corrected to match operational reality).
- Do NOT touch `docker-compose.yml`, `docs/SETUP.md`, or `docs/plans/2026-02-23-implementation-plan.md` — they already use `VITE_MAPBOX_TOKEN`.
- Do NOT add or modify any frontend code to actually READ `VITE_MAPBOX_TOKEN` — that is future work for whenever Mapbox tiles are actually integrated. This plan is purely about identifier consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/INGEST-CONFLICTS.md
@.planning/intel/constraints.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Exact current text of the line to change. -->

From docs/plans/2026-02-23-pothole-tracker-design.md (line 156):
```
- Full-screen Leaflet map (Mapbox if token set via `REACT_APP_MAPBOX_TOKEN`)
```

Already-correct references (DO NOT edit, shown for context):
- docker-compose.yml:38 → `      VITE_MAPBOX_TOKEN: ""`
- docs/SETUP.md:392 → `| \`VITE_MAPBOX_TOKEN\` | *(empty)* | Optional Mapbox token for premium map tiles |`
- docs/plans/2026-02-23-implementation-plan.md:132 → `      VITE_MAPBOX_TOKEN: ""`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-edit grep to confirm scope</name>
  <read_first>(none — this is a discovery grep, not a file read)</read_first>
  <files>(none — read-only)</files>
  <action>
    Run a repo-wide grep for `REACT_APP_MAPBOX_TOKEN` (excluding `.git`, `node_modules`, `.planning`, and `dist`) and confirm the only hit is the single line at `docs/plans/2026-02-23-pothole-tracker-design.md:156`.

    If the grep returns MORE hits than expected (e.g., the repo has drifted since planning), STOP — do NOT proceed to Task 2. Record the unexpected hits in the SUMMARY and let the planner reassess. Specifically: the scope of Task 2 is a single-line surgical edit; if there are multiple hits, they need individual scoped edits.

    If the grep returns ZERO hits (meaning someone already fixed it), skip Tasks 2 and just write the SUMMARY in Task 3 noting that SC #3 was pre-satisfied.
  </action>
  <verify>
    <automated>grep -rnE "REACT_APP_MAPBOX_TOKEN" --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.planning --exclude-dir=dist /Users/hratchghanime/road-quality-mvp/</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one hit is returned.
    - The hit's file path ends with `docs/plans/2026-02-23-pothole-tracker-design.md`.
    - The hit's line number is approximately 156 (content match matters more than exact line number — use the matched content, not the line number, as the edit anchor).
  </acceptance_criteria>
  <done>Pre-edit scope confirmed: exactly one REACT_APP_MAPBOX_TOKEN reference in the design doc.</done>
</task>

<task type="auto">
  <name>Task 2: Replace REACT_APP_MAPBOX_TOKEN with VITE_MAPBOX_TOKEN in design doc</name>
  <read_first>
    - /Users/hratchghanime/road-quality-mvp/docs/plans/2026-02-23-pothole-tracker-design.md (read just lines 150-165 — the "Frontend Overview" or similar section containing the Mapbox bullet; do NOT read the whole file, it's a long design doc)
  </read_first>
  <files>docs/plans/2026-02-23-pothole-tracker-design.md</files>
  <action>
    Edit exactly one line in the design doc, preserving all surrounding markdown/bullet structure verbatim.

    **Exact replacement** (surgical, one identifier only):
    - FROM: `- Full-screen Leaflet map (Mapbox if token set via \`REACT_APP_MAPBOX_TOKEN\`)`
    - TO:   `- Full-screen Leaflet map (Mapbox if token set via \`VITE_MAPBOX_TOKEN\`)`

    Only the env-var identifier inside the backticks changes. Do NOT edit anything else on the line. Do NOT add footnotes, rationale blurbs, or "updated 2026-04-23" markers to the line itself (historical doc semantics are preserved — the correction is a factual identifier alignment, not a narrative rewrite).

    Rationale for commit: ingest-conflict INFO row 2 — SPEC predates project's Vite migration; VITE prefix is operational reality per docker-compose.yml and docs/SETUP.md.

    Implements Success Criterion #3 of Phase 1 (ROADMAP).
  </action>
  <verify>
    <automated>! grep -rnE "REACT_APP_MAPBOX_TOKEN" --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.planning --exclude-dir=dist /Users/hratchghanime/road-quality-mvp/ &amp;&amp; grep -nE "VITE_MAPBOX_TOKEN" /Users/hratchghanime/road-quality-mvp/docs/plans/2026-02-23-pothole-tracker-design.md</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide grep for `REACT_APP_MAPBOX_TOKEN` (excluding .git, node_modules, .planning, dist) returns ZERO hits.
    - `grep -nE "VITE_MAPBOX_TOKEN" docs/plans/2026-02-23-pothole-tracker-design.md` returns exactly one hit — the line that was just edited.
    - `git diff docs/plans/2026-02-23-pothole-tracker-design.md` shows exactly one line changed, and the only change is the identifier inside backticks (CRA prefix → Vite prefix).
  </acceptance_criteria>
  <done>REACT_APP_MAPBOX_TOKEN is retired repo-wide; VITE_MAPBOX_TOKEN is the only Mapbox env-var identifier in code and docs.</done>
</task>

<task type="auto">
  <name>Task 3: Write plan SUMMARY</name>
  <read_first>(none)</read_first>
  <files>.planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md</files>
  <action>
    Create the plan SUMMARY recording:
    - **Pre-edit grep:** one hit found at docs/plans/2026-02-23-pothole-tracker-design.md (line ~156).
    - **Edit applied:** single-line replacement, REACT_APP_MAPBOX_TOKEN → VITE_MAPBOX_TOKEN, inside backticks on the Mapbox bullet.
    - **Post-edit grep:** zero hits for REACT_APP_MAPBOX_TOKEN repo-wide.
    - **INGEST-CONFLICTS INFO #2 (Mapbox env-var name) — RESOLVED.**
    - **Files modified:** docs/plans/2026-02-23-pothole-tracker-design.md (one file, one-line diff).
    - **Files intentionally NOT modified:** docker-compose.yml, docs/SETUP.md, docs/plans/2026-02-23-implementation-plan.md (all already correct); no frontend source file was touched because no frontend source file read either identifier.
    - **Evidence:** grep outputs pre and post edit.
  </action>
  <verify>
    <automated>test -f /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md &amp;&amp; grep -iE "INFO #2.*RESOLVED" /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md` exists.
    - File contains the literal string "INFO #2" AND "RESOLVED".
    - File contains both identifiers verbatim: "REACT_APP_MAPBOX_TOKEN" and "VITE_MAPBOX_TOKEN" (pre and post state).
    - File lists exactly one modified file: docs/plans/2026-02-23-pothole-tracker-design.md.
  </acceptance_criteria>
  <done>SUMMARY exists, records pre/post grep evidence, closes INGEST-CONFLICTS INFO row 2, and enumerates the single modified file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only plan. One trust boundary: the design doc is read by future planners (Phase 2-6 humans and Claude agents) to understand the Mapbox configuration contract. A drifted env-var identifier in this doc would cause future planners to wire the wrong var name, breaking the Mapbox integration when it eventually ships.

No runtime code changes, no network surface, no user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | docs/plans/2026-02-23-pothole-tracker-design.md (over-edit) | mitigate | Task 2 is defined as a single-line surgical find/replace. Acceptance criteria require `git diff` to show exactly one line changed, catching any out-of-scope edit. |
| T-01-03-02 | Integrity (drift regression) | Future references to the Mapbox env var | mitigate | Task 1 runs a pre-edit repo-wide grep and Task 2's verify runs a post-edit repo-wide grep — both with the same exclude set. A zero post-edit count proves full retirement. |
| T-01-03-03 | Repudiation | The historical doc edit | accept | This is a factual correction to a historical doc (the SPEC's env-var name was wrong at authoring time relative to the Vite migration). The SUMMARY file records what was changed and why, providing an audit trail. No signature/PGP controls needed for a repo-internal doc at this scope. |
</threat_model>

<verification_criteria>

```bash
# Full plan verification — run from repo root

# Zero REACT_APP_MAPBOX_TOKEN hits in code or docs
! grep -rnE "REACT_APP_MAPBOX_TOKEN" --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.planning --exclude-dir=dist .

# The design doc now has VITE_MAPBOX_TOKEN in the Mapbox bullet
grep -nE "Mapbox.*VITE_MAPBOX_TOKEN" docs/plans/2026-02-23-pothole-tracker-design.md

# The already-correct files are untouched — they still say VITE_MAPBOX_TOKEN
grep -nE "VITE_MAPBOX_TOKEN" docker-compose.yml
grep -nE "VITE_MAPBOX_TOKEN" docs/SETUP.md

# SUMMARY written
test -f .planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md
grep -qE "INFO #2.*RESOLVED" .planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md
```

</verification_criteria>

<success_criteria>
- Repo-wide grep (excluding .git, node_modules, .planning, dist) for `REACT_APP_MAPBOX_TOKEN` returns zero hits.
- `docs/plans/2026-02-23-pothole-tracker-design.md` line ~156 now uses `VITE_MAPBOX_TOKEN`.
- `git diff` on the design doc shows exactly one line changed (the env-var identifier inside backticks).
- No frontend source file was modified (the plan did not try to wire up Mapbox tiles — that is future work).
- SUMMARY records pre/post grep evidence and closes INGEST-CONFLICTS INFO row 2.
</success_criteria>

<output>
After completion, `.planning/phases/01-mvp-integrity-cleanup/01-03-SUMMARY.md` must exist with pre/post grep evidence and closure of INGEST-CONFLICTS INFO row 2. Modified files: docs/plans/2026-02-23-pothole-tracker-design.md (one line).
</output>
