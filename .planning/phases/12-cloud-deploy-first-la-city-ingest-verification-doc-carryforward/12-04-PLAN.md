---
phase: 12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - README.md
  - backend/app/routes/routing.py
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - REQ-route-filter-env-vars-doc
must_haves:
  truths:
    - "An operator reading .env.example sees ROUTE_FILTER_BUFFER_DEG and ROUTE_FILTER_WIDEN_FACTOR with defaults and one-line descriptions"
    - "An operator reading README.md learns the same two env vars are tunable and finds a cross-link to 08-PERF-NUMBERS.md for tuning rationale"
    - "An engineer reading backend/app/routes/routing.py:22-23 sees an inline cross-link to the Phase 8 perf-numbers archive (anti-pattern pin reciprocity)"
    - "GH Actions deploy.yml's pytest gate uses --timeout=180 so test_idempotent_reingest stops flaking under parallel-agent load"
  artifacts:
    - path: ".env.example"
      provides: "ROUTE_FILTER_BUFFER_DEG=0.03 + ROUTE_FILTER_WIDEN_FACTOR=2.0 documented section (Phase 8 carryforward)"
      contains: "ROUTE_FILTER_BUFFER_DEG"
    - path: "README.md"
      provides: "Configuration sub-section (or equivalent) listing both env vars with cross-link to 08-PERF-NUMBERS.md"
      contains: "ROUTE_FILTER_BUFFER_DEG"
    - path: "backend/app/routes/routing.py"
      provides: "Inline cross-link comment on lines reading the two env vars"
      contains: "08-PERF-NUMBERS.md"
    - path: ".github/workflows/deploy.yml"
      provides: "pytest invocation with --timeout=180 (Phase 10 deferred-items resolution)"
      contains: "--timeout=180"
  key_links:
    - from: ".env.example"
      to: ".planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md"
      via: "comment cross-link"
      pattern: "08-PERF-NUMBERS\\.md"
    - from: "README.md"
      to: ".planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md"
      via: "markdown link in Configuration section"
      pattern: "08-PERF-NUMBERS\\.md"
    - from: "backend/app/routes/routing.py"
      to: ".planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md"
      via: "inline comment near the env-var reads"
      pattern: "08-PERF-NUMBERS\\.md"
---

<objective>
Land the v0.3.0 carryforward documentation for the two Phase 8 routing-buffer env vars (`ROUTE_FILTER_BUFFER_DEG`, `ROUTE_FILTER_WIDEN_FACTOR`) and apply Claude's-discretion bump to the GH Actions pytest timeout (Phase 10 deferred-items resolution). This plan is pure repo edits — it runs autonomously via `gsd-executor` while the operator works on the cloud-side plans (12-01/02/03) in parallel.

Purpose: Close REQ-route-filter-env-vars-doc (3 acceptance criteria from REQUIREMENTS.md). Land the same milestone-close diff so the v0.4.0 milestone-close audit finds documented env vars when it inspects the tree.

Output: `.env.example` + `README.md` + `backend/app/routes/routing.py` document the two env vars with cross-links to `08-PERF-NUMBERS.md`. `deploy.yml` pytest timeout bumped to 180s (per CONTEXT D-12 Claude's-discretion item, Phase 10 deferred-items.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-CONTEXT.md
@.planning/phases/10-crash-scoring-formula-locked-weight-routing-api/deferred-items.md
@.planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md

<interfaces>
<!-- Already-shipped reads in backend/app/routes/routing.py:22-23 (do NOT change behavior, only add a cross-link comment above these two lines). -->

```python
# Phase 8: Pre-filter buffer for the OD bbox passed into ST_MakeEnvelope.
# Default 0.03 deg ~= 3.3 km at LA latitude (RESEARCH §3, §9 defaults table).
# Override via env to widen on operator request without a redeploy.
ROUTE_FILTER_BUFFER_DEG = float(os.environ.get("ROUTE_FILTER_BUFFER_DEG", "0.03"))
ROUTE_FILTER_WIDEN_FACTOR = float(os.environ.get("ROUTE_FILTER_WIDEN_FACTOR", "2.0"))
```

The new comment must cross-link the v0.3.0 archive at `.planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md` (D-12-19, anti-pattern pin reciprocity with the existing Phase 8 line 272-298 inline header).

<!-- README.md current heading inventory (no top-level "Configuration" section exists). -->
- Live Demo / Current Status / Quick Start / How It Works / API Endpoints / Detector Accuracy / Real-Data Ingest / Frontend Pages / Tests / Deploy / Tech Stack / Documentation

<!-- D-12-18 says "Configuration section (or equivalent — researcher to verify section name)". The closest existing semantic home is between `## Tests` and `## Deploy` (operator-facing tunables) OR appended to the existing `## Real-Data Ingest` section. Bias toward minimum disruption per D-12 Claude's-discretion: add a new top-level `## Configuration` section between `## Tests` (line 197) and `## Deploy` (line 205). This is the natural spot for env-var docs that affect runtime behavior. -->

<!-- .github/workflows/deploy.yml line 155 currently runs `pytest -v` with no timeout flag. The `pytest-timeout` plugin defaults to 0 (off); the Phase 10 deferred-items.md notes pytest-timeout's "default 180s ceiling" but pytest-timeout is NOT installed in the workflow's pip install list (line 138 only installs requests/numpy/huggingface_hub on top of backend/requirements.txt). Verify pytest-timeout is in backend/requirements.txt; if not, add a `--timeout=180` flag may be a no-op. Actual fix: pass `--timeout=180` flag (pytest-timeout MUST be installed for this to take effect — confirm in Task 3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document ROUTE_FILTER_BUFFER_DEG + ROUTE_FILTER_WIDEN_FACTOR in .env.example (per D-12-17)</name>
  <files>.env.example</files>
  <action>
Append a new section to `.env.example` AFTER the existing `LACITY_SNAP_M=50.0` block (which ends at line 92). The section must be the EXACT copy from CONTEXT D-12-17:

```
# ----- Routing perf tunables (Phase 8 carryforward; cite 08-PERF-NUMBERS.md) -----
# Spatial buffer in degrees around route bbox for segment_scores filter (default 0.03 ≈ 3.3 km).
# Wider buffer covers more candidate detours; narrower keeps the temp-table small (Phase 8 perf budget).
# See .planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md for tuning rationale.
ROUTE_FILTER_BUFFER_DEG=0.03
# Multiplier for the wide-fallback buffer when the initial filter returns 0 candidates (default 2.0 → 6.6 km).
# 3-attempt fallback chain: filter → wide → full. Tune up for sparse corners, down for tight perf.
ROUTE_FILTER_WIDEN_FACTOR=2.0
```

The cross-link line is added on top of the verbatim D-12-17 block (D-12-18/19 require the cross-link in BOTH `.env.example` and `README.md`; CONTEXT shows it explicit only in the README/routing.py text but the same archive should be reachable from `.env.example` so an operator who only opens `.env.example` finds the rationale). Do NOT remove the LACITY_SNAP_M=50.0 line. Do NOT alphabetize — append at the end so existing line numbers in any docs/cross-links stay stable.

Single atomic commit: `docs(12-04): document ROUTE_FILTER_BUFFER_DEG + ROUTE_FILTER_WIDEN_FACTOR in .env.example (REQ-route-filter-env-vars-doc, D-12-17)`.
  </action>
  <verify>
    <automated>grep -A 2 "ROUTE_FILTER_BUFFER_DEG=0.03" .env.example && grep "ROUTE_FILTER_WIDEN_FACTOR=2.0" .env.example && grep "08-PERF-NUMBERS.md" .env.example</automated>
  </verify>
  <done>`.env.example` contains both env vars with their defaults, one-line descriptions, and a cross-link to `08-PERF-NUMBERS.md` archive. Existing `LACITY_SNAP_M=50.0` block is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Configuration section to README.md cross-linking 08-PERF-NUMBERS.md (per D-12-18)</name>
  <files>README.md</files>
  <action>
Insert a new top-level `## Configuration` section between the existing `## Tests` (line 197) and `## Deploy` (line 205) sections. Per CONTEXT D-12 Claude's-discretion: bias toward minimum disruption to existing README structure — this slot is the natural home for operator-facing runtime tunables (it sits between "how to run tests" and "how to ship to prod").

Section content:

```markdown
## Configuration

Operator-facing runtime tunables. All values have safe defaults committed in `.env.example`; override via Fly secrets (production) or `.env` (local dev).

### Routing performance buffer (Phase 8 carryforward)

- `ROUTE_FILTER_BUFFER_DEG` (default `0.03` ≈ 3.3 km at LA latitude) — spatial buffer in degrees around the OD bbox passed to `ST_MakeEnvelope` when building `rq_filtered_edges`. Widen for sparse-corner OD pairs that miss candidate detours; narrow to keep the temp-table small under the Phase 8 5s cross-LA budget. Read at request time in `backend/app/routes/routing.py:22`.
- `ROUTE_FILTER_WIDEN_FACTOR` (default `2.0`) — multiplier applied to `ROUTE_FILTER_BUFFER_DEG` for the second attempt in the 3-attempt fallback chain (`filter → wide → full`). Default `2.0` widens the second-pass buffer to ~6.6 km. Read at request time in `backend/app/routes/routing.py:23`.

Tuning rationale, perf measurements, and the `pgr_ksp` SPI / GiST root cause are documented in [`.planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md`](.planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md). The anti-pattern header inside `routing.py` (lines 272-298) explains why the spatial pre-filter MUST run in the outer psycopg2 query, not inside `pgr_ksp`'s SPI-evaluated edges_sql.
```

Two bullet entries (one per env var) satisfies REQ-route-filter-env-vars-doc acceptance criterion #2 ("README.md Configuration section documents both env vars + cross-links 08-PERF-NUMBERS.md").

Single atomic commit: `docs(12-04): add Configuration section documenting routing buffer env vars + 08-PERF-NUMBERS.md cross-link (REQ-route-filter-env-vars-doc, D-12-18)`.
  </action>
  <verify>
    <automated>grep -A 1 "^## Configuration" README.md && grep "ROUTE_FILTER_BUFFER_DEG" README.md && grep "ROUTE_FILTER_WIDEN_FACTOR" README.md && grep "08-PERF-NUMBERS.md" README.md</automated>
  </verify>
  <done>README.md has a new `## Configuration` section between `## Tests` and `## Deploy`. Section documents both env vars with defaults and one-line descriptions, and contains a markdown link to `.planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Add inline cross-link comment in backend/app/routes/routing.py:22-23 (per D-12-19)</name>
  <files>backend/app/routes/routing.py</files>
  <action>
Modify the existing comment block at lines 19-23 to add a single cross-link line. The block currently reads:

```python
# Phase 8: Pre-filter buffer for the OD bbox passed into ST_MakeEnvelope.
# Default 0.03 deg ~= 3.3 km at LA latitude (RESEARCH §3, §9 defaults table).
# Override via env to widen on operator request without a redeploy.
ROUTE_FILTER_BUFFER_DEG = float(os.environ.get("ROUTE_FILTER_BUFFER_DEG", "0.03"))
ROUTE_FILTER_WIDEN_FACTOR = float(os.environ.get("ROUTE_FILTER_WIDEN_FACTOR", "2.0"))
```

Append ONE additional comment line BEFORE the two `os.environ.get` reads (anti-pattern pin reciprocity with the existing Phase 8 line 272-298 inline header):

```python
# Phase 8: Pre-filter buffer for the OD bbox passed into ST_MakeEnvelope.
# Default 0.03 deg ~= 3.3 km at LA latitude (RESEARCH §3, §9 defaults table).
# Override via env to widen on operator request without a redeploy.
# Operator runbook + tuning rationale: .planning/milestones/v0.3.0-phases/08-routing-performance/08-PERF-NUMBERS.md
# (Phase 12 D-12-19 carryforward — also documented in .env.example + README.md Configuration section.)
ROUTE_FILTER_BUFFER_DEG = float(os.environ.get("ROUTE_FILTER_BUFFER_DEG", "0.03"))
ROUTE_FILTER_WIDEN_FACTOR = float(os.environ.get("ROUTE_FILTER_WIDEN_FACTOR", "2.0"))
```

Behavior is UNCHANGED — only comment lines are added. Verify by running pytest after the edit; backend/tests/ must remain green (the routing.py module loads at import time so any syntax error surfaces immediately).

Single atomic commit: `docs(12-04): add 08-PERF-NUMBERS.md cross-link comment in routing.py:22-23 (D-12-19 anti-pattern pin reciprocity)`.
  </action>
  <verify>
    <automated>grep -B 1 "ROUTE_FILTER_BUFFER_DEG = float" backend/app/routes/routing.py | grep "08-PERF-NUMBERS.md" && cd backend && python -c "from app.routes.routing import ROUTE_FILTER_BUFFER_DEG, ROUTE_FILTER_WIDEN_FACTOR; assert ROUTE_FILTER_BUFFER_DEG == 0.03 and ROUTE_FILTER_WIDEN_FACTOR == 2.0"</automated>
  </verify>
  <done>routing.py has an inline comment immediately above the `os.environ.get("ROUTE_FILTER_BUFFER_DEG"...)` line that cross-links `08-PERF-NUMBERS.md`. Module imports cleanly; both module constants resolve to their documented defaults (0.03 and 2.0).</done>
</task>

<task type="auto">
  <name>Task 4: Bump deploy.yml pytest timeout to 180s (Claude's discretion per CONTEXT, resolves Phase 10 deferred-items)</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Modify the `test` job in `.github/workflows/deploy.yml`. The current step at lines 139-155 runs `pytest -v` with no timeout flag.

Two sub-edits in a single atomic change:

1. **Install pytest-timeout in the host venv.** Phase 10 deferred-items.md references "pytest-timeout's default 180s ceiling" but the workflow only installs `requests numpy huggingface_hub` on top of `backend/requirements.txt` (line 138). First check whether `pytest-timeout` is in `backend/requirements.txt`:
   ```bash
   grep -i pytest-timeout backend/requirements.txt
   ```
   - If present: skip step 1 (already in `pip install -r backend/requirements.txt`).
   - If absent: add `pytest-timeout` to the `pip install` line at line 138, e.g. `pip install requests numpy huggingface_hub pytest-timeout`. Do NOT add it to backend/requirements.txt itself (that ships in the prod image and is unrelated to CI test gating).

2. **Add `--timeout=180` flag to the pytest invocation.** Change line 155 from `pytest -v` to `pytest -v --timeout=180`. Per Phase 10 deferred-items: this surfaces `test_idempotent_reingest`'s parallel-agent flake as an explicit timeout (rather than the silent default-off behavior) and bumps the per-test ceiling above the ~107s × 2 = 214s the test needs. NOTE: 180s is what CONTEXT and deferred-items both name; the test still needs >180s under load. The bump is the documented fix per CONTEXT Claude's-discretion. If the test continues to flake at 180s, the next-pass remediation is in Phase 10 deferred-items Option B/C (parameterize fixture / serialize via xdist), NOT a further bump in Phase 12.

Verify locally that the workflow YAML still parses (no indentation breakage). Use `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` to confirm.

Single atomic commit: `ci(12-04): bump pytest-timeout to 180s in deploy.yml (Phase 10 deferred-items, CONTEXT Claude's-discretion)`.
  </action>
  <verify>
    <automated>grep -E "pytest.*--timeout=180|--timeout=180.*pytest" .github/workflows/deploy.yml && python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && (grep -i "pytest-timeout" backend/requirements.txt || grep -i "pytest-timeout" .github/workflows/deploy.yml)</automated>
  </verify>
  <done>deploy.yml's test job runs `pytest -v --timeout=180`. `pytest-timeout` is installed (either via backend/requirements.txt or via the workflow's pip install line). YAML parses cleanly. The Phase 10 deferred-items.md flake gets the documented 180s ceiling instead of pytest's silent default-off behavior.</done>
</task>

</tasks>

<verification>
- `grep "ROUTE_FILTER_BUFFER_DEG=0.03" .env.example` and `grep "ROUTE_FILTER_WIDEN_FACTOR=2.0" .env.example` both return matches with the cross-link.
- `grep "^## Configuration" README.md` returns one match; that section contains both env-var bullets and the `08-PERF-NUMBERS.md` markdown link.
- `grep -B 1 "ROUTE_FILTER_BUFFER_DEG = float" backend/app/routes/routing.py | grep "08-PERF-NUMBERS.md"` returns a match (the new comment immediately precedes the read).
- `grep -- "--timeout=180" .github/workflows/deploy.yml` returns a match in the `test` job's pytest step.
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` exits 0.
- All 4 tasks land as 4 atomic commits on `main` (per `commit_docs: true` and Phase 12 build-policy in CONTEXT).
</verification>

<success_criteria>
REQ-route-filter-env-vars-doc closed:
- `.env.example` lists both env vars with defaults (0.03, 2.0) and one-line descriptions ✅
- README.md `## Configuration` section documents both env vars + cross-links `08-PERF-NUMBERS.md` ✅
- Cross-link to `routing.py:22-23` (inline comment) reciprocates the Phase 8 line 272-298 anti-pattern pin ✅

CONTEXT Claude's-discretion item closed:
- deploy.yml pytest gate uses `--timeout=180` (Phase 10 deferred-items resolution) ✅

This plan lands BEFORE the operator-driven push-to-main in Plan 12-03 — those commits get included in the deploy.yml run that 12-03 verifies. (Wave-1 placement guarantees this ordering: 12-04 runs first, alongside 12-05.)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → operator/end-user docs | Docs are read-only artifacts; no runtime trust transition |
| repo → CI runner | `.github/workflows/deploy.yml` is executed by GH-hosted runners with `FLY_API_TOKEN` secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | I (Information disclosure) | `.env.example` | accept | Template only — no real values; cross-link to `08-PERF-NUMBERS.md` does not expose secrets. Anti-pattern pin says "NEVER commit a real token"; this plan adds zero token-shaped content. |
| T-12-04-02 | T (Tampering) | `.github/workflows/deploy.yml` | mitigate | YAML parse check (Task 4 verify command) catches indentation/quoting tampering at commit time. Existing `concurrency: deploy-prod cancel-in-progress=false` (line 47) and `if: always() && needs.test.result == 'success' || ...` skip-cascade defense (Pitfall 7) remain intact. |
| T-12-04-03 | D (Denial of service) | CI test job | accept | The 180s timeout limits a single test's CI consumption rather than enabling DoS. Worst case is the test job runs 180s longer per timeout-victim test before failing loud. |
| T-12-04-04 | E (Elevation of privilege) | repo → routing.py runtime | accept | Comment-only change; no behavior delta. Module-level `os.environ.get` reads are unchanged; existing `float(...)` coercion (lines 22-23) prevents env-var injection of non-numeric values. |
</threat_model>

<output>
After completion, create `.planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-04-SUMMARY.md` per the template. Include:
- Commit hashes for all 4 tasks
- Confirmation that the 4 verify commands all pass
- Note that Wave-1 placement means these commits land BEFORE Plan 12-03's push-to-main verification (so deploy.yml's run includes them)
- Any deviations (e.g., if pytest-timeout was already in backend/requirements.txt — note that Task 4 sub-step 1 was a no-op)
</output>
