---
phase: 12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward
plan: 03
type: human-action
wave: 4
depends_on:
  - 12-02
  - 12-04
files_modified:
  - .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md
autonomous: false
requirements:
  - REQ-crash-cloud-deploy
user_setup:
  - service: fly.io
    why: "Push to main triggers deploy.yml; operator monitors GH Actions run + verifies live deploy"
    env_vars:
      - name: FLY_API_TOKEN
        source: "GH Actions secret (already configured per v0.3.0 Phase 5 deploy.yml)"
    dashboard_config:
      - task: "Operator monitors run at https://github.com/<owner>/road-quality-mvp/actions"
        location: "GitHub Actions UI"
must_haves:
  truths:
    - "Push-to-main triggers GH Actions deploy.yml; the run goes green for the test job (under 180s pytest timeout per Plan 12-04 Task 4) and for deploy-backend + deploy-frontend"
    - "https://road-quality-frontend.fly.dev/ returns HTTP 200; bundle contains the locked Phase 11 disclaimer copy with em dash U+2014"
    - "POST /route returns 200 with the locked `Deprecation: weight_iri,weight_potholes ignored as of v0.4.0` header (Phase 10 contract)"
    - "GET /segments returns features whose properties dict includes `crash_norm` (Phase 10 contract; non-zero for ≥1 feature given Plan 12-02 populated ≥100 segments)"
    - "3-route spot check (DTLA-local + cross-LA + Wilshire) produces routes that pass the Phase 8 5s perf budget and the Pitfall 5 Wilshire sanity gate"
    - "Disclaimer + caption render live; no new map layer / heatmap / per-segment crash markers (Phase 11 anti-feature check)"
  artifacts:
    - path: ".planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md"
      provides: "Sections 6 (GH Actions run URL + per-job status) + 7 (live API contract + 3-route spot check + disclaimer/caption visibility) + 8 (deviations + rollback decisions)"
      contains: "Pershing Square"
  key_links:
    - from: "operator's git push origin main"
      to: ".github/workflows/deploy.yml"
      via: "GH Actions auto-trigger on push to main"
      pattern: "push: branches: \\[main\\]"
    - from: "deploy.yml deploy-backend / deploy-frontend"
      to: "Fly.io road-quality-backend / road-quality-frontend"
      via: "flyctl deploy --remote-only"
      pattern: "flyctl deploy"
    - from: "live frontend bundle"
      to: "Phase 11 disclaimer string"
      via: "served JS asset contains 'always drive defensively'"
      pattern: "always drive defensively"
---

<objective>
Operator-driven cloud verification: push the Wave-1 commits (Plan 12-04 env-var docs + pytest-timeout bump; Plan 12-05 delta-report scaffold; any Plan 12-01/12-02 delta-report paste commits) to main, monitor the GH Actions deploy.yml auto-run, then execute the live API + 3-route spot check from a browser/curl session against `https://road-quality-frontend.fly.dev/` and `https://road-quality-backend.fly.dev/`. Capture all results into Sections 6, 7, 8 of `12-DELTA-REPORT.md`.

Purpose: Close the remaining REQ-crash-cloud-deploy acceptance criteria (5, 6, 7, 8, 9): backend + frontend redeployed; GH Actions green; cold cross-LA <5s; 3-route spot check makes sense to a local; delta report committed. This is the milestone-close verification gate for v0.4.0 — after this plan, Phase 12 verification can run, then `gsd-complete-milestone v0.4.0` closes M2.

Output: Sections 6, 7, 8 of 12-DELTA-REPORT.md populated with verbatim captures (GH Actions run URL, per-job status, curl outputs, route URLs/screenshots, disclaimer-bytes grep result, Pitfall 5 sanity verdict, any deviations/rollback decisions).

> **This plan is `autonomous: false` `type: human-action`.** It cannot run via `gsd-executor`. The push itself, the GH Actions monitoring, the live curl checks, and the qualitative 3-route browser inspection ALL require operator interaction. The execute-phase agent presents this as a checklist; the operator works through Tasks 1-3 sequentially.

> **DEPENDS ON:** Plan 12-02 (live data populated; otherwise 3-route spot check has nothing to verify) AND Plan 12-04 (env-var docs + pytest-timeout bump committed; the push triggers the deploy.yml run that Task 1 verifies). Wave 4 placement enforces this: Plans 12-04/05 land in Wave 1, Plan 12-01 in Wave 2, Plan 12-02 in Wave 3, Plan 12-03 in Wave 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-CONTEXT.md
@.planning/phases/11-frontend-slider-removal-liability-disclaimer-data-vintage-caption/11-VERIFICATION.md
@.planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md
@.github/workflows/deploy.yml
@docs/API.md

<interfaces>
<!-- Phase 10/11 contracts the live deploy must satisfy: -->

POST /route response headers (Phase 10 D-10-15, locked):
```
HTTP/1.1 200 OK
Deprecation: weight_iri,weight_potholes ignored as of v0.4.0
Content-Type: application/json
```

GET /segments response shape (Phase 10 / 11 D-11-04):
```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": { "type": "LineString", "coordinates": [...] },
      "properties": {
        "id": <int>,
        "iri_norm": <float>,
        "moderate_score": <float>,
        "severe_score": <float>,
        "pothole_score_total": <float>,
        "crash_norm": <float>   /* NEW in v0.4.0 — REQ-frontend-slider-removal acceptance bullet 5 */
      }
    }
  ]
}
```

Frontend disclaimer string (Phase 11 D-11-04, EXACT-MATCH locked, em dash is U+2014):
```
Routes incorporate historical crash data from LA City open-data (through March 2024). This is informational, not a safety guarantee — always drive defensively.
```
(Confirmed live at frontend/src/pages/RouteFinder.tsx:138 + frontend/src/__tests__/disclaimer.test.tsx:6)

Frontend caption string (Phase 11 D-11-04, EXACT-MATCH):
```
Crash data: LA City open-data through March 2024. Single-segment attribution; intersection distribution to be added in a future release.
```

<!-- Phase 8 perf budget (locked, REQ-crash-cloud-deploy AC #6): -->
- Cold cross-LA route: <5s end-to-end (browser button click → route geometry rendered)
- DTLA-local route: well under budget (Phase 8 reported 0.385s for similar OD)

<!-- 3-route spot check from CONTEXT D-12-15: -->
- Route 1 (DTLA-local): Pershing Square → Bunker Hill (~1 mi) — sanity that small routes work
- Route 2 (Cross-LA): Westwood → Boyle Heights (~14 mi) — perf budget gate
- Route 3 (Pitfall 5 sanity): Wilshire Blvd Mid-Wilshire — known-safe arterial NOT routed onto if crash_norm > 0.5

<!-- deploy.yml trigger contract (verified v0.3.0 Phase 5 + per Plan 12-04 Task 4 update): -->
- changes job paths-filter: backend+frontend likely true (Phase 10 backend changes + Phase 11 frontend changes already on main from prior phases per CONTEXT line 10; this push only adds Plan 12-04 docs/CI changes which match the `db: false, backend: false, frontend: false` if .env.example/README only — but the .github/workflows/deploy.yml change itself is in `db: false, backend: false, frontend: false` paths-filter category)
- IMPORTANT: deploy.yml line 73-80 paths-filter only watches `deploy/db/**`, `db/**`, `backend/**`, `deploy/backend/**`, `frontend/**`, `deploy/frontend/**`. Plan 12-04 modifies `.env.example` (no path watched), `README.md` (no path watched), `backend/app/routes/routing.py` (matches `backend/**`), `.github/workflows/deploy.yml` (no path watched). So paths-filter outputs: backend=true (routing.py change), db=false, frontend=false. Test job will run (backend=true). deploy-backend will run (backend=true + test green). deploy-frontend will SKIP (frontend=false). This is the correct outcome — frontend bundle is unchanged so no redeploy needed.
- WAIT: per CONTEXT line 10, Phase 9-11 commits are already on main from prior phases. So the frontend (Phase 11) and backend (Phase 10) Docker images on Fly already carry those changes. Plan 12-03's push includes Plan 12-04's targeted backend/routing.py comment-only change (backend=true → redeploy backend) and the deploy.yml change (no path watched but the test job still runs). Frontend skips redeploy because no frontend/** changes — that's CORRECT, the existing live frontend already has Phase 11 disclaimer + caption + slider removal from the previous push.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push-to-main + monitor GH Actions deploy.yml run (D-12-11, D-12-12)</name>
  <what-to-run>
**Operator runs from credentialed shell.**

### 1a. Confirm staged commits

By the time this task runs, the local repo on `main` should contain (in chronological order):

- Plan 12-04 commits (4 atomic commits):
  - `docs(12-04): document ROUTE_FILTER_BUFFER_DEG + ROUTE_FILTER_WIDEN_FACTOR in .env.example ...`
  - `docs(12-04): add Configuration section ...`
  - `docs(12-04): add 08-PERF-NUMBERS.md cross-link comment in routing.py:22-23 ...`
  - `ci(12-04): bump pytest-timeout to 180s in deploy.yml ...`
- Plan 12-05 commit (1):
  - `docs(12-05): scaffold 12-DELTA-REPORT.md with all D-12-20 sections ...`
- Plan 12-01 commit (1, operator's own):
  - `docs(12-01): paste df rehearsal + migration 004 apply log + post-apply schema ...`
- Plan 12-02 commit (1, operator's own):
  - `docs(12-02): paste first LA City ingest run-summary + compute_scores --source all output ...`

Verify the log:
```bash
git log --oneline -10
git status
```

Expected: clean tree, all 7 commits ahead of `origin/main` (or already-pushed if operator pushed Plan 12-04/05 earlier; the design admits incremental pushes — no rule says all 7 must land in a single push).

### 1b. Push to main

```bash
git push origin main
```

This auto-triggers `.github/workflows/deploy.yml` (concurrency group `deploy-prod`,
cancel-in-progress=false per line 47 — a second push waits for the first to finish).

### 1c. Capture GH Actions run URL

Open https://github.com/<owner>/road-quality-mvp/actions in browser. Find the
run triggered by the push (top of the list, "Deploy to Fly" workflow).

```bash
# Optional: get the URL via gh CLI:
gh run list --workflow=deploy.yml --limit 1
gh run view <run-id> --web
```

Capture the run URL into 12-DELTA-REPORT.md Section 6 `Run URL:` line.

### 1d. Monitor per-job status

Watch the run progress through the 5 jobs. Capture pass/fail/skip status for
each into Section 6's per-job table. Expected (per the `<interfaces>` block):

| Job | Expected Outcome |
|-----|------------------|
| changes | green; outputs.backend=true (routing.py edit); outputs.db=false; outputs.frontend=false |
| test | green; pytest under `--timeout=180` (Plan 12-04 Task 4 bump); 19+ tests + Phase 5 + Phase 8 + Phase 9 + Phase 10 tests all pass |
| deploy-db | skipped (paths-filter db=false) — the manual migration was Plan 12-01, by design (D-12-13) |
| deploy-backend | green; new image carries the routing.py comment-only change |
| deploy-frontend | skipped (paths-filter frontend=false) — frontend is unchanged in this push (Phase 11 already on main from prior phase) |

If any job fails:
- **test fails**: investigate via `gh run view <run-id> --log`. Common cause: `test_idempotent_reingest` flaking even with `--timeout=180`. If so: deferred-items.md option B/C is the next-pass fix; for Phase 12 we accept and document under Section 8.
- **deploy-backend fails**: rare; v0.3.0 Phase 5 inline defects (PGDATA, dockerfile path) are already fixed. Most likely cause: Fly machine OOM if memory-pin sized wrong. Investigate before retrying.
- **deploy-frontend skipped**: this is EXPECTED for this push (no frontend/** changes). NOT a failure. Confirm in Section 6 row.

### 1e. Skip-cascade defense check (Pitfall 7 from Phase 5)

If `test` is skipped (paths-filter says no code changes — possible if operator
pushes a single commit that only touches non-watched paths like `.planning/`),
deploy-backend and deploy-frontend MUST inherit `skipped` not `failed`. Verify:
the `if: always() && (needs.test.result == 'success' || needs.test.result == 'skipped')`
guard on each deploy job (deploy.yml lines 169, 194, 244, 272) ensures this.
This was the Pitfall 7 fix from Phase 5.

### 1f. Deploy-time pin check (CR-02 defense)

If `test` FAILS (not skipped): deploy-backend AND deploy-frontend must NOT
deploy (the CR-02 fix from Phase 5 explicitly added `test` to the `needs:`
list of deploy-frontend, lines 239-244). If somehow a deploy job ran with
`test: failure`, that is a regression — STOP, file as v0.4.1 incident,
do NOT proceed past Plan 12-03.

### Anti-patterns (DO NOT)

- ❌ `git push --force` to main — never (PROJECT.md Git Safety Protocol).
- ❌ Manually run `flyctl deploy` to short-cut the deploy.yml gate — the GH Actions
  flow IS the contract (REQ-crash-cloud-deploy AC #7: "GH Actions deploy.yml passes
  for the v0.4.0 commit"). Skipping it leaves the audit gap.
- ❌ Re-run a failed deploy job blindly without reading logs — Phase 5 LESSONS-LEARNED
  shows the value of root-cause analysis (16 inline defects fixed there); blind
  retries are how those defects originally hid.
  </what-to-run>
  <expected-output>
- Section 6 Run URL: live GH Actions run URL captured
- Section 6 per-job table: 5 rows with pass/skip statuses; matches expected outcome (test green, deploy-backend green, deploy-db skipped, deploy-frontend skipped, changes green) OR documented variance with reason
- Pass box ticked
  </expected-output>
  <capture-into>12-DELTA-REPORT.md Section 6 — Run URL + per-job table.</capture-into>
  <resume-signal>Type "deploy-green" with the run URL + per-job summary, OR "deploy-failed" with the failing job name + log excerpt + decision (retry vs rollback vs accept-and-document).</resume-signal>
  <verify>
    <automated>grep -E "actions/runs/|github.com/.+/actions/runs/" .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md</automated>
  </verify>
  <done>Section 6 of 12-DELTA-REPORT.md captures the GH Actions run URL and per-job pass/skip status. The deploy.yml run is green (test + deploy-backend; deploy-db + deploy-frontend skip-by-design); pass box ticked.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Live API contract verification + 3-route spot check (D-12-14, D-12-15, D-12-16)</name>
  <what-to-run>
**Operator runs from a browser + credentialed shell.** Sub-steps 2a → 2b → 2c.

### 2a. Live API contract checks (D-12-14)

Run each curl from the operator's local machine (no flyctl proxy; these are
PUBLIC endpoints):

#### Frontend reachable
```bash
curl -I https://road-quality-frontend.fly.dev/
```
Expected: `HTTP/2 200`. Capture the status line into Section 7a row 1.

#### Backend `/route` Deprecation header
```bash
curl -i -X POST https://road-quality-backend.fly.dev/route \
  -H 'content-type: application/json' \
  -d '{
    "origin": {"lat": 34.0522, "lon": -118.2437},
    "destination": {"lat": 34.0407, "lon": -118.2466},
    "max_extra_minutes": 5
  }'
```
Expected:
- `HTTP/2 200`
- Header line: `deprecation: weight_iri,weight_potholes ignored as of v0.4.0`
  (HTTP/2 lowercases header names; the value matches Phase 10 D-10-15 EXACT-STRING locked)
- Body: JSON with `fastest_route`, `best_route`, `per_segment_metrics`

Capture the `Deprecation:` header line VERBATIM into Section 7a row 2.

#### Backend `/segments` exposes crash_norm
LA viewbox bbox covering downtown:
```bash
curl -s 'https://road-quality-backend.fly.dev/segments?bbox=-118.27,34.04,-118.23,34.07' \
  | python3 -c "
import sys, json
data = json.load(sys.stdin)
features = data.get('features', [])
print(f'Feature count: {len(features)}')
if features:
    keys = list(features[0]['properties'].keys())
    print(f'First feature property keys: {keys}')
    has_crash_norm = 'crash_norm' in keys
    print(f'crash_norm present: {has_crash_norm}')
    nonzero = sum(1 for f in features if f['properties'].get('crash_norm', 0) > 0)
    print(f'features with crash_norm > 0: {nonzero}')
"
```
Expected:
- Feature count > 0
- `crash_norm present: True`
- `features with crash_norm > 0:` ≥ 1 (since Plan 12-02 populated ≥100 segments with non-zero crash_norm; the downtown bbox should catch some)

Capture into Section 7a row 3.

#### Disclaimer bytes in served frontend bundle
The disclaimer copy contains an em dash (U+2014, UTF-8 bytes `e2 80 94`).
Verify it round-trips through the build pipeline:
```bash
curl -s https://road-quality-frontend.fly.dev/ | grep -oE 'assets/index-[a-zA-Z0-9_-]+\.js' | head -1
# Use the output to fetch the JS asset:
ASSET_PATH=$(curl -s https://road-quality-frontend.fly.dev/ | grep -oE 'assets/index-[a-zA-Z0-9_-]+\.js' | head -1)
curl -s "https://road-quality-frontend.fly.dev/${ASSET_PATH}" | grep -aoE '.{20}always drive defensively.{5}' | head -1
```
Expected: a snippet like `safety guarantee — always drive defensively.` with the
em dash byte-perfect (visible as `—` if your terminal is UTF-8). If you see
`?` or mojibake instead of `—`, the build pipeline corrupted the encoding —
ROLLBACK trigger (D-12-27 BREAKING — disclaimer bytes wrong is legal-liability).

Capture the grep output into Section 7a row 4.

### 2b. 3-Route Spot Check (D-12-15)

Open https://road-quality-frontend.fly.dev/ in a browser. For each of the 3
routes below: select origin/destination, click "find route", record the URL
(if the Phase 11 frontend exposes URL state) or take a screenshot. Note
fastest_route vs best_route divergence + cold response time.

**Cold response time:** ensure the route is not a cache hit. Either reload
the page first OR pick fresh coordinate jitter (e.g., add ±0.0001 to lat/lon).
Phase 8 perf budget: cold cross-LA <5s.

#### Route 1: DTLA-local — Pershing Square → Bunker Hill (~1 mi)

Origin: Pershing Square, ~`(34.0480, -118.2531)`
Destination: Bunker Hill, ~`(34.0540, -118.2531)`

Capture into Section 7b Route 1 block:
- fastest_route URL or screenshot
- best_route URL or screenshot
- Observation: best == fastest (acceptable) or best diverges (expected if
  any LA City crash from Plan 12-02 snapped to the involved segments)
- Cold response time: paste seconds (Phase 8 budget for DTLA-local: well
  under 1s — reported 0.385s in Phase 8)

#### Route 2: Cross-LA — Westwood → Boyle Heights (~14 mi)

Origin: Westwood, ~`(34.0640, -118.4452)`
Destination: Boyle Heights, ~`(34.0331, -118.2138)`

This is the MAIN perf budget gate (REQ-crash-cloud-deploy AC #6 explicitly:
"cold cross-LA route stays under 5s").

Capture:
- fastest_route URL or screenshot
- best_route URL or screenshot
- Cold response time: paste seconds (ACCEPTANCE GATE: <5s; Phase 8 baseline 2.47s)
- Observation: best swings onto Sunset/3rd over Wilshire/Olympic? Identical?
  Unexpected detour? (qualitative — "looks reasonable to a local")
- Pass/fail vs Phase 8 5s budget

#### Route 3: Wilshire Blvd Mid-Wilshire — Pitfall 5 sanity gate

Pick an OD pair that would NATURALLY pass through Wilshire Blvd in the Mid-
Wilshire portion (e.g., Origin: Beverly Hills `(34.0736, -118.4004)`,
Destination: Koreatown `(34.0631, -118.3088)` — fastest path goes via
Wilshire). The locked-weights routing should NOT push best_route off Wilshire
ONTO an unsafe alternative if Wilshire's crash_norm is reasonable (CONTEXT
D-12-15 Pitfall 5 gate: Wilshire's `crash_norm` should NOT exceed 0.5;
fatal weights at 8:3:1 + K=3 cap should prevent saturation).

Capture:
- fastest_route URL or screenshot (expected: Wilshire-heavy)
- best_route URL or screenshot
- Wilshire crash_norm spot value: query a representative Wilshire segment from
  the live `/segments` endpoint:
  ```bash
  curl -s 'https://road-quality-backend.fly.dev/segments?bbox=-118.39,34.06,-118.34,34.07' \
    | python3 -c "
import sys, json
data = json.load(sys.stdin)
crash_norms = sorted([f['properties'].get('crash_norm', 0) for f in data['features']], reverse=True)
print(f'Top-5 crash_norm in Mid-Wilshire bbox: {crash_norms[:5]}')
print(f'Median: {crash_norms[len(crash_norms)//2]}')
"
  ```
  Expected: top crash_norm in Mid-Wilshire is below 0.5 (Pitfall 5 gate).
  If above 0.5 on Wilshire's representative segments: route 3 likely
  diverges off Wilshire onto a side street — log under Section 8c as a
  v0.4.1 calibration hand-off (CONTEXT D-12-15 closing line: NOT a
  Phase 12 blocker; log and continue).

### 2c. Disclaimer + Caption visibility check (Phase 11 D-11-04 carryforward)

Operator visually inspects the live frontend at https://road-quality-frontend.fly.dev/:

- **Route Finder page** — open it and click somewhere to set up origin/dest.
  - Disclaimer text adjacent to the "find route" button: ☐ yes / ☐ no
  - Disclaimer copy MATCHES (exact-string against the locked Phase 11 D-11-04
    string from `<interfaces>`): ☐ yes / ☐ no
  - If NO on either: ROLLBACK trigger D-12-27 BREAKING (legal liability).

- **Map View page** — open it.
  - Caption rendered: ☐ yes / ☐ no
  - Caption copy MATCHES: ☐ yes / ☐ no
  - No new map layer / no heatmap / no per-segment crash markers (Phase 11
    locked anti-feature check): ☐ confirmed absent / ☐ found unexpected

Capture each check (with screenshot if discrepancy) into Section 7c.

### Anti-patterns (DO NOT)

- ❌ Use `localhost:3000` instead of the live `https://road-quality-frontend.fly.dev/` —
  Plan 12-03 verifies the LIVE deploy, not local stack.
- ❌ Skip Route 3 (Wilshire) "because route 1 + 2 looked fine" — the Pitfall 5
  sanity gate is the locked Phase 12 calibration check (CONTEXT D-12-15);
  skipping it leaves a verification gap.
- ❌ Paraphrase the disclaimer string match — Phase 11 D-11-04 is EXACT-STRING
  locked. The audit greps for the literal copy.
  </what-to-run>
  <expected-output>
- Section 7a (live API contract): 4 rows populated (frontend 200; Deprecation header VERBATIM; crash_norm present in /segments; disclaimer bytes round-trip through bundle)
- Section 7b (3-route check): all 3 routes with URLs/screenshots, response times, observations
  - Route 2 cold response time captured + pass/fail vs 5s budget ticked
  - Route 3 Wilshire crash_norm verdict captured + Pitfall 5 sanity gate ticked
- Section 7c (disclaimer + caption visibility): all 6 checks ticked + (if mismatch) screenshot captured
- Pass box ticked OR rollback decision recorded in Section 8b
  </expected-output>
  <capture-into>12-DELTA-REPORT.md Section 7 (7a + 7b + 7c). Tick `☐ pass` (or document failure with rollback decision in Section 8).</capture-into>
  <resume-signal>Type "live-pass" with the cross-LA cold time + Wilshire verdict + disclaimer-match status, OR "live-fail" with rollback decision (per D-12-27 trigger conditions in 12-DELTA-REPORT Section 8b).</resume-signal>
  <verify>
    <automated>grep -q "Pershing\|Westwood\|Wilshire" .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md && grep -q "Deprecation\|always drive defensively" .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md</automated>
  </verify>
  <done>Section 7 of 12-DELTA-REPORT.md is fully populated: live API contract checks pass, 3-route spot check captures URLs/screenshots/timings/observations, disclaimer + caption visibility verified. Cross-LA cold time <5s confirmed; Wilshire Pitfall 5 sanity gate verdict recorded. No rollback triggered (or if triggered: documented decision in Section 8b).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Final delta-report sign-off (Section 8 deviations + Section 9 timestamps; commit + close phase)</name>
  <what-to-run>
**Operator runs from credentialed shell.**

### 3a. Section 8a — Deviations from CONTEXT.md

Operator reviews their notes from Tasks 1-3 across all of Plans 12-01, 12-02, 12-03.
For each step that deviated from CONTEXT decisions, fill in a row in the Section 8a
table. Examples of legitimate deviations:

- D-12-08 chose Option A → operator confirmed used Option A; row optional or skip
- D-12-15 Wilshire crash_norm came in at 0.62 (above the 0.5 gate) → row REQUIRED
  with hand-off note for v0.4.1 calibration
- Plan 12-02 had to widen `--snap-meters` because the default 50m dropped >5%
  of crashes → row REQUIRED with the override value + rationale
- GH Actions test job failed first attempt due to flake → row REQUIRED with
  retry count + which test flaked

If NO deviations: write `*No deviations from CONTEXT.md decisions during Phase 12 execution.*` under Section 8a.

### 3b. Section 8b — Rollback decisions

For each of the 4 rollback-trigger conditions (D-12-27), operator ticks the
"Triggered? ☐ no / ☐ yes" box. If any are `yes`, the "Action taken" column
records what happened (e.g., `Rolled back via flyctl deploy --image-label v0.3.0-final at <timestamp>; investigation continues in v0.4.1`).

If ALL are `no`: confirm by ticking each `☐ no` box explicitly.

### 3c. Section 8c — v0.4.1 hand-off items

Bullet-list any items discovered live that should track into v0.4.1:
- Pitfall 5 calibration if Wilshire `crash_norm > 0.5`
- pytest-timeout option B/C (parameterize fixture / serialize via xdist) if
  test_idempotent_reingest still flakes at 180s
- `--snap-meters` widening if Plan 12-02 surfaced too many drops
- Histogram smoke test guard condition if it remained skipped against live data
- Anything else surfaced by the 3-route observations ("Route 3 best_route diverged
  unexpectedly through low-traffic side streets — investigate K=5 cap behavior")

### 3d. Section 9 — Sign-off

Fill in the operator's name and per-plan close timestamps:
- Plan 12-01 closed (Wave 2 — operator's 12-01 commit timestamp)
- Plan 12-02 closed (Wave 3 — operator's 12-02 commit timestamp)
- Plan 12-03 closed (this task's commit timestamp)
- Plan 12-04 closed (Wave 1 — gsd-executor's last 12-04 commit timestamp)
- Plan 12-05 closed (Wave 1 — gsd-executor's 12-05 scaffold commit timestamp)

Tick the "Milestone-close audit input ready" box: `☐ yes (all 8 sections populated)`.

### 3e. Commit + push

```bash
git add .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md
git commit -m "$(cat <<'EOF'
docs(12-03): close 12-DELTA-REPORT — GH Actions URL, 3-route spot check, deviations + sign-off (REQ-crash-cloud-deploy AC #5, AC #6, AC #7, AC #8, AC #9)

Phase 12 verification gate: GH Actions deploy.yml green; 3-route spot check
makes-sense-to-a-local (Pitfall 5 sanity); disclaimer + caption render live;
crash_norm exposed in /segments; cold cross-LA <5s.

Closes Phase 12. Milestone-close audit input ready.
EOF
)"
git push origin main
```

This is the FINAL push of Phase 12. After this, the milestone-close audit
(`gsd-complete-milestone v0.4.0`) reads `12-DELTA-REPORT.md` as its primary
input and marks v0.4.0 shipped.

NOTE: this final push also re-triggers deploy.yml. The auto-run will be
nearly all-skip (changes paths-filter sees only `.planning/**` which is not
watched) — paths-filter outputs `db=false, backend=false, frontend=false`,
the test job will skip-on-changes-empty (deploy.yml line 87-90: `test` runs
only if any of db/backend/frontend == true), and all deploy jobs will skip
via the Pitfall 7 skip-cascade defense. This is expected and correct.

### Anti-patterns (DO NOT)

- ❌ Skip Section 8a "because nothing deviated" — at minimum write the explicit
  "no deviations" line. The audit reads the absence of the row as ambiguous;
  the explicit line removes ambiguity.
- ❌ Force-push to main — never (Git Safety Protocol).
- ❌ Defer the milestone-close audit hand-off "to clean up the report later" —
  the report is the audit input AS-IS at commit time; cleanup is post-milestone
  scope.
  </what-to-run>
  <expected-output>
- Section 8a populated (deviations table or explicit "no deviations")
- Section 8b populated (4 rollback-trigger rows ticked)
- Section 8c populated (v0.4.1 hand-off bullets)
- Section 9 populated (operator name + 5 plan-close timestamps)
- Single final commit on main with the message above
- Final push triggers deploy.yml re-run that all-skips (expected)
  </expected-output>
  <capture-into>12-DELTA-REPORT.md Sections 8 + 9. Final commit closes Phase 12.</capture-into>
  <resume-signal>Type "phase-12-closed" with the final commit SHA, OR "phase-12-blocked" with the blocker description (rollback in progress; pytest-timeout flake; etc).</resume-signal>
  <verify>
    <automated>grep -q "Plan 12-01 closed\|Plan 12-02 closed\|Plan 12-03 closed" .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md && grep -q "Milestone-close audit input ready" .planning/phases/12-cloud-deploy-first-la-city-ingest-verification-doc-carryforward/12-DELTA-REPORT.md</automated>
  </verify>
  <done>12-DELTA-REPORT.md Sections 8 + 9 populated. Final commit on main. Phase 12 closed; v0.4.0 milestone-close audit can read the report as input.</done>
</task>

</tasks>

<verification>
- 12-DELTA-REPORT.md Section 6: GH Actions run URL captured + 5-row per-job table populated; test green, deploy-backend green, deploy-db skipped (out-of-band), deploy-frontend skipped-or-green
- Section 7a: 4 rows of live API contract checks (frontend 200; Deprecation header verbatim; crash_norm present in /segments; disclaimer bytes round-trip)
- Section 7b: 3 routes with URLs/screenshots, observations, response times; cross-LA cold time <5s; Wilshire Pitfall 5 sanity verdict recorded
- Section 7c: disclaimer + caption visibility checks ticked
- Section 8a: deviations table OR explicit "no deviations" line
- Section 8b: 4 rollback-trigger rows ticked (`☐ no` or `☐ yes` with action)
- Section 8c: v0.4.1 hand-off bullets (or empty if nothing surfaced)
- Section 9: operator name + 5 plan-close timestamps + "ready" box ticked
- Final commit on main with conventional message; deploy.yml re-trigger all-skips as expected
</verification>

<success_criteria>
REQ-crash-cloud-deploy fully closed (acceptance criteria 5-9):
- Backend redeployed with locked-weights code (deploy.yml deploy-backend green) ✅
- Frontend already-redeployed with slider-removal + disclaimer (Phase 11 push earlier; bundle bytes confirmed live) ✅
- Live POST /route returns 200 with Deprecation header ✅
- Cold cross-LA route <5s (Phase 8 perf budget intact) ✅
- 3-route manual spot-check makes-sense-to-a-local; Wilshire Pitfall 5 sanity gate verdict recorded ✅
- Live GET /segments returns features with crash_norm field ✅
- GH Actions deploy.yml passes for the v0.4.0 commit ✅
- First-deploy delta report committed under .planning/phases/12-*/ with all 8 D-12-20 sections ✅

Phase 12 closed. v0.4.0 milestone-close audit unblocked.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's git push → GitHub | HTTPS-authenticated git remote; main branch protection (if configured) enforces no-force-push |
| GitHub → GH Actions runner | FLY_API_TOKEN secret exposed only as job-scoped env-var; runner ephemeral |
| GH Actions runner → Fly | flyctl deploy via FLY_API_TOKEN; deploys signed by Fly's image-promotion pipeline |
| public browser → road-quality-frontend.fly.dev | TLS; CORS-restricted to allowed_origins (Phase 5 SC #2) |
| public browser → road-quality-backend.fly.dev /route, /segments | TLS; auth disabled per d0ef452 (public demo); rate-limited at Fly edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | T (Tampering) | git push to main | mitigate | Operator-authored commits; no force-push (Git Safety Protocol). The deploy.yml's `test` job is the gate (Pitfall 7 + CR-02 fix from Phase 5: deploy-frontend explicitly requires `test: success`). |
| T-12-03-02 | I (Information disclosure) | live API contract checks | accept | All probed endpoints are PUBLIC (no auth gate per d0ef452). The curl commands in Task 2a probe public-shape contract; no PII or secrets at risk. |
| T-12-03-03 | D (Denial of service) | manual 3-route spot check | accept | Operator-driven, single-shot route requests; cold-cache scenarios use jitter; total request volume <10 over the spot check. Phase 8's 12s SET LOCAL statement_timeout (db.py:97-98) bounds any pathological per-request behavior. |
| T-12-03-04 | E (Elevation of privilege) | live deploy via deploy.yml | mitigate | FLY_API_TOKEN is job-scoped; deploy-backend/deploy-frontend `if:` guards prevent deployment if `test` fails (CR-02 from Phase 5). The locked anti-pattern (DDL via flyctl ssh, NOT proxy) was Plan 12-01 only — no DDL in this plan. |
| T-12-03-05 | R (Repudiation) | who closed Phase 12 | mitigate | Final commit captures operator git author identity + ISO timestamp; Section 9 of the delta report records explicit sign-off; deploy.yml run URL is server-side traceable to the trigger commit SHA. |
| T-12-03-06 | I | disclaimer copy roundtripping | mitigate | Task 2a row 4 explicitly verifies the em dash byte sequence (`e2 80 94`) survives the build pipeline. Phase 11 disclaimer.test.tsx + the live grep are the two-layer defense. Phase 11 was D-11-04 EXACT-STRING locked; mismatch is rollback BREAKING (D-12-27). |
</threat_model>

<output>
Operator commits the populated Sections 6-9 under a single FINAL Phase 12 commit:

```
docs(12-03): close 12-DELTA-REPORT — GH Actions URL, 3-route spot check, deviations + sign-off (REQ-crash-cloud-deploy AC #5, AC #6, AC #7, AC #8, AC #9)
```

Optional `12-03-SUMMARY.md` for symmetry with autonomous plans (the populated
delta report is the de-facto summary).

**Hand-off to milestone-close:** Phase 12 is closed. The orchestrator can now invoke
`gsd-verify-phase 12` (which reads 12-DELTA-REPORT.md as its primary input per
D-12-21), then `gsd-complete-milestone v0.4.0` to ship M2.
</output>
