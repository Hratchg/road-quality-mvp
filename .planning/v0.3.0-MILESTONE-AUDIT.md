---
milestone: v0.3.0
audited: 2026-05-08
status: tech_debt
scores:
  requirements: 6/7
  phases: 7/8
  integration: 11/13 wiring checks pass
  flows: 2/3 e2e flows pass
gaps:
  requirements:
    - id: REQ-trained-la-detector
      status: unsatisfied
      phase: 07
      classification: documented_negative
      rationale: |
        Both training iterations failed the D-11 win-check on the held-out test split.
        Iter-1 (af7af59a): full collapse, 0 predictions on every split.
        Iter-2 (84a874c2): val P=0.184 but 0 predictions on test (operator labeling-style drift).
        D-13 contingency executed: close as documented negative, keep keremberke baseline in production.
      verification_status: documented_negative
      evidence: 07-SUMMARY.md, 07-06-WIN-CHECK.md, FUTURE-FINETUNE-OPTIONS.md, docs/DETECTOR_EVAL.md v0.3.0
    - id: REQ-user-auth
      status: doc_drift
      phase: 04
      classification: superseded_by_commit_d0ef452
      rationale: |
        Phase 4 originally satisfied REQ-user-auth at close (04-VERIFICATION.md status: passed).
        Commit d0ef452 (2026-04-28, "feat: remove sign-in feature — demo is fully public")
        removed auth gating from /route and /cache/*, deleted frontend SignInModal + api/auth.ts.
        Backend auth modules (tokens.py, passwords.py, dependencies.py) remain in tree but
        unmounted in main.py.
        REQUIREMENTS.md, PROJECT.md, and 04-VERIFICATION.md were NOT amended to reflect this
        product decision.
      verification_status: stale
      evidence: |
        Live: POST /route returns 200 without Authorization header (verified)
        Live: POST /auth/register returns 404 (auth router unmounted)
        Stale: 04-VERIFICATION.md cites Depends(get_current_user_id) at routing.py:44 — line no longer exists
        Stale: 04-VERIFICATION.md frontend Key Link table cites 5 wires evaluating to 0 imports in HEAD
      remediation: |
        Either (a) amend REQUIREMENTS.md row for REQ-user-auth: status SUPERSEDED-by-d0ef452,
        rationale "demo is fully public per operator decision; auth backend retained for future
        re-mount as env-toggle"; OR (b) restore auth as env-var-gated middleware (auth modules
        still in-tree, ~1 hr work).
  integration: []
  flows:
    - flow: auth_signin
      status: FAIL
      classification: superseded
      rationale: Auth flow removed by commit d0ef452 — demo is intentionally public. See REQ-user-auth above.
tech_debt:
  - phase: "04"
    severity: warning
    items:
      - "REQ-user-auth doc-vs-code drift (root cause; remediated by amending REQUIREMENTS.md + PROJECT.md or restoring auth as env-toggle)"
  - phase: "07"
    severity: info
    items:
      - "2 trained model artifacts public on HF (af7af59a collapse, 84a874c2 drift) — preserved for traceability per FUTURE-FINETUNE-OPTIONS.md"
  - phase: "08"
    severity: warning
    items:
      - "ROUTE_FILTER_BUFFER_DEG + ROUTE_FILTER_WIDEN_FACTOR env vars not documented in README/.env.example"
  - phase: "03"
    severity: warning
    items:
      - "5 Mapillary integration tests hang on subprocess.run selectors.poll (pre-existing; surfaced in Phase 8 UAT out-of-scope section)"
  - phase: "general"
    severity: info
    items:
      - "Untracked: data/eval_la/labels/test.cache (YOLO label cache — should be gitignored)"
      - "Untracked: .planning/phases/03-mapillary-ingestion-pipeline/.Rhistory (R history — should be gitignored)"
      - "README:312 stale prose: seed-on-demand description says 'inside the deployed backend container' but actual workflow is host-venv + flyctl proxy (Phase 5 CR-01 fix)"
nyquist:
  compliant_phases: 0
  partial_phases: 1  # Phase 8 has 08-VALIDATION.md unfilled scaffold
  missing_phases: 7  # 1, 2, 3, 4, 5, 6, 7
  overall: not_blocking
  notes: |
    nyquist_validation enabled per config but VALIDATION.md scaffold introduced late
    in M1 (only Phase 8). Earlier phases predate the convention; their plans carry
    inline <automated> verify blocks instead. Not a blocker for M1 close.
---

# v0.3.0 (M1 Post-MVP + Public Demo) — Milestone Audit

**Audited:** 2026-05-08
**Status:** `tech_debt` (no flow-breaking gaps; doc-vs-code drift on REQ-user-auth is the one real issue)

## Verdict

**M1 ships a working public demo.** Live URLs return 200, real Mapillary data flows through scoring to `/segments`, cross-LA route returns under 5s with both fastest + best paths. End-to-end user-observable flows (map view, route finder) work as advertised. Phase 7 closed honestly as documented negative; Phase 8 ships verified.

The verdict is `tech_debt` rather than `passed` because of one cross-phase doc-drift contradiction (REQ-user-auth shows SATISFIED in docs but the auth gate was removed in commit `d0ef452` for public demo). This is a documentation hygiene issue, not a runtime gap — the demo works as users would expect.

## Requirements Coverage

| REQ-ID | Assigned | Status | Evidence |
|--------|----------|--------|----------|
| REQ-mvp-integrity-cleanup | Phase 1 | ✅ SATISFIED | 4/4 plans complete; BIGINT verified, seed-radius reconciled |
| REQ-real-data-accuracy | Phase 2 | ✅ SATISFIED (tooling) | 4/4 SCs verified; numbers TBD per Phase 6 D-09 deferral to Phase 7 |
| REQ-mapillary-pipeline | Phase 3 | ✅ SATISFIED | 5/5 SCs verified; live ingest working in production |
| REQ-user-auth | Phase 4 | ⚠ SUPERSEDED | Originally satisfied at Phase 4 close; commit d0ef452 (2026-04-28) removed auth gate for public demo; docs not amended |
| REQ-prod-deploy | Phase 5 | ✅ SATISFIED | 9/9 SCs verified; live tri-app Fly deploy confirmed |
| REQ-public-demo | Phase 6 | ✅ SATISFIED | 4/4 SCs PASS; live URL serves real LA data with public-baseline detector per D-09 |
| REQ-trained-la-detector | Phase 7 | ❌ DOCUMENTED NEGATIVE | Both training iterations failed D-11 win-check; D-13 contingency executed; production retains keremberke baseline |
| (no formal REQ — Phase 8) | Phase 8 | ✅ SATISFIED | PERF-01/02/03 verified; live cross-LA routing in 2.5s |

**Score: 6/7 formal REQs satisfied + Phase 8 SCs met. 1 documented-negative (Phase 7) + 1 doc-drift (Phase 4).**

## E2E Flows

| Flow | Status | Verified by |
|------|--------|-------------|
| Map view (open URL → colored segments) | ✅ PASS | Live /segments returns 7724 features for DTLA bbox, 2336 with non-zero pothole_score_total |
| Route finder (click 2 points → fastest+best routes) | ✅ PASS | Phase 8 UAT (08-UAT.md) Tests 2-5 all PASS; live POST /route returns 200 in 0.5s for DTLA |
| Auth (sign in → JWT → authenticated route) | ⚠ SUPERSEDED | Removed by commit d0ef452 — demo is intentionally public |

## Cross-Phase Wiring (11/13 pass)

✅ Phase 3 → 6 → 8 data pipeline intact: ingest_mapillary → segment_defects → compute_scores → segment_scores → /segments + /route
✅ Phase 5 tri-app Fly deploy: db internal → backend public → frontend public with VITE_API_URL baked into bundle
✅ Phase 6 _DEFAULT_HF_REPO unchanged from baseline (Phase 7 D-13 intentional)
✅ Phase 8 Pitfalls A and G defended in code (outer GiST filter, btree on temp table)
✅ Live frontend bundle contains correct backend URL (no localhost leakage)
✅ FastAPI router mounting: health, segments, routing, cache (auth router unmounted per d0ef452)
⚠ Phase 4 auth gating absent from /route and /cache/* (intentional per d0ef452)
⚠ Phase 4 frontend auth UI absent (api/auth.ts deleted, SignInModal deleted, UnauthorizedError unimported)

## Recommended Pre-Close Cleanup (~10 min)

The audit allows `/gsd-complete-milestone v0.3.0` to proceed under `tech_debt` status. Two minor remediations would push verdict from `tech_debt` to `passed`:

1. **Amend REQUIREMENTS.md row for REQ-user-auth**: change status from "Pending" to "Superseded by d0ef452" with rationale "Public demo per operator decision; auth backend retained for future re-mount as env-toggle"
2. **Amend PROJECT.md `Active` requirements**: move REQ-user-auth from `[x]` SATISFIED to a new "Superseded" subsection with the same rationale

Optional (nice-to-have, not required for milestone close):
3. Update 04-VERIFICATION.md frontmatter `status: superseded` with note linking to d0ef452 (preserves verification history without claiming false coverage)
4. Add ROUTE_FILTER_BUFFER_DEG + ROUTE_FILTER_WIDEN_FACTOR to .env.example or README env-var list (Phase 8 tech debt)
5. Add data/eval_la/labels/*.cache and .planning/phases/*/.Rhistory to .gitignore

## What Ships Net-Net

8 phases. 39 plans across 5 waves of execution. ~205,000 segments + 74,000 vertices + 12 zones of real Mapillary detections. Live demo at https://road-quality-frontend.fly.dev/ working for the M1 public-demo target. Phase 7 documented negative is part of the honest portfolio narrative (DETECTOR_EVAL.md v0.3.0 documents both failure modes). Phase 8 perf fix (cross-LA 20-90s → 2.5s) ships verified.

The doc-drift on REQ-user-auth is the only real cleanup before milestone archive. Post-archive, M2 can pick up crash-data integration with a clean slate.
