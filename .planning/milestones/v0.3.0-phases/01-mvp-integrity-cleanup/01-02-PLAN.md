---
phase: 01-mvp-integrity-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/PRD.md
  - .planning/PROJECT.md
autonomous: true
requirements: [REQ-mvp-integrity-cleanup]

must_haves:
  truths:
    - "scripts/seed_data.py and docs/SETUP.md agree on the seed radius literal"
    - "README.md quick-start text states the same seed radius as scripts/seed_data.py"
    - "docs/PRD.md status bullet states the same seed radius as scripts/seed_data.py"
    - "PROJECT.md Key Decisions row for seed radius reflects the actual code value and is marked Good (no longer pending)"
  artifacts:
    - path: "README.md"
      provides: "User-facing quick-start documentation whose seed-radius claim matches code"
      contains: "20km"
    - path: "docs/PRD.md"
      provides: "Feature status doc whose seed-radius claim matches code"
      contains: "20km"
    - path: ".planning/PROJECT.md"
      provides: "Project decisions table with resolved seed-radius row"
      contains: "20 km"
  key_links:
    - from: "scripts/seed_data.py (DIST = 20000, line 21)"
      to: "docs/SETUP.md, README.md, docs/PRD.md, .planning/PROJECT.md"
      via: "authoritative value propagated from code to all user-facing docs and the project decision log"
      pattern: "20\\s*km|20000"
---

<objective>
Resolve the ingest-conflict drift on seed radius (INGEST-CONFLICTS.md INFO row 1) by making all user-facing docs and the project decision log reflect the actual value in `scripts/seed_data.py`.

**Current state (verified during planning):**
- `scripts/seed_data.py` line 21: `DIST = 20000` (20 km) — **authoritative**
- `docs/SETUP.md` line 153: "~20 km radius from downtown" — **already matches code**
- `README.md` line 38: "(~10km radius)" — **DRIFT, must be updated**
- `docs/PRD.md` line 19: "(osmnx LA 10km + synthetic IRI/potholes)" — **DRIFT, must be updated**
- `.planning/PROJECT.md` Key Decisions row for "Seed radius = 10 km": **flagged ⚠️ Revisit**, must be resolved to the actual code value (20 km) with a Good outcome.

**Phase 1 Success Criterion #2 (ROADMAP, authoritative):** "`scripts/seed_data.py` and `docs/SETUP.md` agree on the seed radius literal. SPEC says 10 km. Read both; if they diverge, update whichever is wrong so both state the same value. **Pick the seed script's literal as authoritative if there's ambiguity — docs should reflect code.**"

Code is 20 km. Both seed_data.py and SETUP.md already agree at 20 km, so the minimal SC #2 work is verification only. However, the broader intent of the phase — "reconcile ingest-vs-code drift before building on top" — requires also fixing README.md and PRD.md (which still say 10 km) and promoting PROJECT.md's pending decision to resolved.

**Purpose:** Prevent ongoing documentation drift from poisoning Phase 2-6 planning. Every future reader should see one consistent seed-radius value sourced from code.

**Output:**
- README.md says "20 km" where it previously said "10km"
- docs/PRD.md says "20 km" where it previously said "10km"
- PROJECT.md's seed-radius decision row is resolved (not ⚠️ Revisit)
- A SUMMARY file records the drift that was fixed and confirms post-change agreement

**Non-goals (do NOT do these in this plan):**
- Do NOT change `scripts/seed_data.py` — its literal IS the authoritative value.
- Do NOT change `docs/SETUP.md` — it already matches code.
- Do NOT change historical design docs under `docs/plans/` (they are dated artifacts; rewriting history is out of scope).
- Do NOT change `.planning/STATE.md` — orchestrator owns that file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INGEST-CONFLICTS.md
@.planning/intel/constraints.md

<interfaces>
<!-- Drift locations the executor must fix, with exact current text. -->

From scripts/seed_data.py (the authoritative source, lines 19-22):
```python
# LA center, 20km radius — covers Glendale, Westwood, Santa Monica, Pasadena
CENTER = (34.0522, -118.2437)
DIST = 20000
SEED = 42
```

From docs/SETUP.md (already matches code, line 153 — DO NOT EDIT, included for context only):
```
1. Downloads the LA road network (~20 km radius from downtown) via OSMnx
```

From README.md (DRIFT — current text at line 38):
```
This downloads the LA road network (~10km radius) via OSMnx and generates synthetic IRI + pothole data. Takes a few minutes on first run.
```

From docs/PRD.md (DRIFT — current text at line 19):
```
- [x] Seed data script (osmnx LA 10km + synthetic IRI/potholes)
```

From .planning/PROJECT.md (pending row in the Key Decisions table — exact current text):
```
| Seed radius = 10 km around (34.05, -118.24) | SPEC wins over SETUP's "~20 km" claim; verify literal in seed_data.py during M1 | ⚠️ Revisit (INFO item from ingest) |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify seed_data.py and SETUP.md already agree (SC #2 core)</name>
  <read_first>
    - /Users/hratchghanime/road-quality-mvp/scripts/seed_data.py (just lines 15-25 for the DIST literal; full file is 163 lines, do NOT re-read whole file)
    - /Users/hratchghanime/road-quality-mvp/docs/SETUP.md (just the "Seeding the Database" section around line 143-165 — under the "## Seeding the Database" heading)
  </read_first>
  <files>(none — read-only verification)</files>
  <action>
    Confirm that `scripts/seed_data.py` has `DIST = 20000` and `docs/SETUP.md` says "~20 km radius". These two files are the explicit subject of Phase 1 Success Criterion #2 and must agree before this plan finishes.

    If EITHER file has drifted between planning and execution time (e.g., someone has edited seed_data.py since this plan was drafted), STOP — do NOT proceed to Tasks 2-4. Surface the drift in the SUMMARY and let the planner reassess. The authoritative value must always come from `scripts/seed_data.py`, per the ROADMAP SC #2 instruction "pick the seed script's literal as authoritative."

    Capture the exact literal value observed in seed_data.py (expected: `20000` meters → `20 km`) — this value is what Tasks 2, 3, and 4 propagate.
  </action>
  <verify>
    <automated>grep -nE "^DIST\s*=\s*20000\s*$" /Users/hratchghanime/road-quality-mvp/scripts/seed_data.py &amp;&amp; grep -nE "~20\s*km\s+radius" /Users/hratchghanime/road-quality-mvp/docs/SETUP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^DIST\s*=\s*20000\s*$" scripts/seed_data.py` returns exactly one match.
    - `grep -nE "~20\s*km\s+radius" docs/SETUP.md` returns at least one match under the seeding section.
    - The executor has recorded the authoritative literal ("20 km") that subsequent tasks will write into README.md, docs/PRD.md, and PROJECT.md.
  </acceptance_criteria>
  <done>SC #2 is affirmatively verified: seed_data.py and SETUP.md already agree at 20 km. The authoritative value for downstream tasks is "20 km".</done>
</task>

<task type="auto">
  <name>Task 2: Fix seed-radius claim in README.md</name>
  <read_first>
    - /Users/hratchghanime/road-quality-mvp/README.md (just lines 30-50 around the seed section; full file is 148 lines)
  </read_first>
  <files>README.md</files>
  <action>
    Edit `README.md` to replace the drifted seed-radius claim with the authoritative value from seed_data.py (20 km).

    **Exact replacement** (single line change on line 38):
    - FROM: `This downloads the LA road network (~10km radius) via OSMnx and generates synthetic IRI + pothole data. Takes a few minutes on first run.`
    - TO:   `This downloads the LA road network (~20 km radius) via OSMnx and generates synthetic IRI + pothole data. Takes a few minutes on first run.`

    Rationale to include in commit: code (`scripts/seed_data.py` `DIST = 20000`) is authoritative per ROADMAP Phase 1 SC #2.

    Do NOT edit any other content in README.md. Do NOT reflow paragraphs. Do NOT touch the heading or surrounding context — this is a surgical one-line fix.

    Implements Success Criterion #2 of Phase 1 (ROADMAP) — drift-fix portion.
  </action>
  <verify>
    <automated>grep -nE "~20\s*km\s+radius" /Users/hratchghanime/road-quality-mvp/README.md &amp;&amp; ! grep -nE "~10\s*km\s+radius" /Users/hratchghanime/road-quality-mvp/README.md</automated>
  </verify>
  <acceptance_criteria>
    - After edit, `grep -nE "~20\s*km\s+radius" README.md` returns at least one line.
    - After edit, `grep -nE "~10\s*km\s+radius" README.md` returns ZERO lines.
    - `git diff README.md` shows exactly one line changed — the OSMnx download description at line 38.
  </acceptance_criteria>
  <done>README.md quick-start section states the LA road network is downloaded at ~20 km radius, matching `scripts/seed_data.py`.</done>
</task>

<task type="auto">
  <name>Task 3: Fix seed-radius claim in docs/PRD.md</name>
  <read_first>
    - /Users/hratchghanime/road-quality-mvp/docs/PRD.md (find the line with "osmnx LA 10km" — line 19 during planning; full file length is moderate, read only the immediate section around the drifted line)
  </read_first>
  <files>docs/PRD.md</files>
  <action>
    Edit `docs/PRD.md` to replace the drifted seed-radius claim with the authoritative 20 km value.

    **Exact replacement** (single line change on line 19):
    - FROM: `- [x] Seed data script (osmnx LA 10km + synthetic IRI/potholes)`
    - TO:   `- [x] Seed data script (osmnx LA 20km + synthetic IRI/potholes)`

    Do NOT edit any other content in PRD.md. Do NOT reword, just update the literal `10km → 20km` inside the parentheses. Preserve the rest of the bullet verbatim (including the `[x]` checkbox).

    If `grep -n "osmnx LA 10km" docs/PRD.md` returns 0 matches (meaning the file changed since planning), STOP — do NOT guess at the new location. Surface the drift in SUMMARY and let the planner reassess.

    Implements Success Criterion #2 of Phase 1 (ROADMAP) — drift-fix portion.
  </action>
  <verify>
    <automated>grep -nE "osmnx LA 20km" /Users/hratchghanime/road-quality-mvp/docs/PRD.md &amp;&amp; ! grep -nE "osmnx LA 10km" /Users/hratchghanime/road-quality-mvp/docs/PRD.md</automated>
  </verify>
  <acceptance_criteria>
    - After edit, `grep -nE "osmnx LA 20km" docs/PRD.md` returns at least one line.
    - After edit, `grep -nE "osmnx LA 10km" docs/PRD.md` returns ZERO lines.
    - `git diff docs/PRD.md` shows exactly one line changed — the seed-data bullet at line 19.
  </acceptance_criteria>
  <done>docs/PRD.md "Implemented" bullet for the seed script states 20km, matching `scripts/seed_data.py`.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve PROJECT.md seed-radius decision row</name>
  <read_first>
    - /Users/hratchghanime/road-quality-mvp/.planning/PROJECT.md (just the "Key Decisions" table section at the bottom — lines ~110-122; do not re-read the whole file)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Update the Key Decisions table row for the seed radius. The row currently has a ⚠️ Revisit outcome because the ingest flagged a SPEC-vs-DOC drift. Phase 1 has now resolved it by inspection (code says 20 km, SETUP.md agrees, README/PRD drift is fixed in this plan). Promote the row from pending to resolved.

    **Exact replacement** (single row change in the Key Decisions table):
    - FROM: `| Seed radius = 10 km around (34.05, -118.24) | SPEC wins over SETUP's "~20 km" claim; verify literal in seed_data.py during M1 | ⚠️ Revisit (INFO item from ingest) |`
    - TO:   `| Seed radius = 20 km around (34.05, -118.24) | Code is authoritative per ROADMAP Phase 1 SC #2; `scripts/seed_data.py` `DIST = 20000` confirmed; SETUP.md already agreed; README.md and docs/PRD.md updated in Phase 1 Plan 02 | ✓ Resolved (Phase 1, 2026-04-23) |`

    Also update the trailing `---` footer line if it carries a timestamp that should reflect today. Specifically: update `*Last updated: 2026-04-23 after ingest + roadmap initialization*` → `*Last updated: 2026-04-23 after Phase 1 Plan 02 (seed radius drift resolved)*`. If the existing timestamp text does not exactly match, leave the footer alone — do not invent new footer syntax.

    **Important scope guard:** Do NOT touch the `road_segments.source/target = BIGINT` decision row below it — that's Plan 01's concern and it is still technically "pending verification" from PROJECT.md's perspective until Plan 01 ships its SUMMARY. Leave that row unchanged.

    Do NOT touch any other section of PROJECT.md.

    Implements Success Criterion #2 of Phase 1 (ROADMAP) — project-decision-log update.
  </action>
  <verify>
    <automated>grep -nE "Seed radius = 20 km" /Users/hratchghanime/road-quality-mvp/.planning/PROJECT.md &amp;&amp; ! grep -nE "⚠️ Revisit.*seed_data\.py" /Users/hratchghanime/road-quality-mvp/.planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - After edit, `grep -nE "Seed radius = 20 km" .planning/PROJECT.md` returns exactly one line (the updated row).
    - After edit, `grep -nE "Seed radius = 10 km" .planning/PROJECT.md` returns ZERO lines.
    - After edit, the updated row contains the literal string "Resolved" and references "Phase 1 Plan 02".
    - The row for `road_segments.source/target = BIGINT` is unchanged (grep for its exact current text still finds it).
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions row for seed radius is updated to 20 km, marked Resolved, with Phase 1 Plan 02 traceability. The BIGINT row is left untouched.</done>
</task>

<task type="auto">
  <name>Task 5: Write plan SUMMARY</name>
  <read_first>(none — SUMMARY is created from scratch, prior tasks' outputs are the evidence)</read_first>
  <files>.planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md</files>
  <action>
    Create the plan SUMMARY. Record:
    - **SC #2 verified:** seed_data.py and SETUP.md agreed at 20 km before this plan ran.
    - **Drift fixed:** README.md line 38 (10km → 20 km) and docs/PRD.md line 19 (10km → 20km).
    - **PROJECT.md decision resolved:** the seed-radius row moved from ⚠️ Revisit to ✓ Resolved (Phase 1).
    - **INGEST-CONFLICTS INFO #1 (seed radius) — RESOLVED.**
    - **Files modified:** README.md, docs/PRD.md, .planning/PROJECT.md (three files, small surgical diffs).
    - **Files intentionally NOT modified:** scripts/seed_data.py (authoritative source), docs/SETUP.md (already correct), docs/plans/*.md (historical artifacts), .planning/STATE.md (orchestrator-owned).
    - **Evidence:** grep outputs proving post-edit state for all three files.
  </action>
  <verify>
    <automated>test -f /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md &amp;&amp; grep -iE "INFO #1.*RESOLVED" /Users/hratchghanime/road-quality-mvp/.planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md` exists.
    - File contains the literal string "INFO #1" AND "RESOLVED".
    - File contains the literal string "20 km" or "20km" (the authoritative value).
    - File contains an explicit "Files modified" section listing exactly three files: README.md, docs/PRD.md, .planning/PROJECT.md.
    - File contains an explicit "Files NOT modified (intentional)" section listing scripts/seed_data.py and docs/SETUP.md.
  </acceptance_criteria>
  <done>SUMMARY exists, records drift-fix evidence, enumerates modified and intentionally-not-modified files, and closes INGEST-CONFLICTS INFO row 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation/decision-log update plan. Two trust boundaries touched:
1. **Repo-root user-facing docs → public readers of the repo.** README.md and docs/PRD.md are read by anyone who clones the repo. A drifted radius claim misleads contributors trying to reproduce the seed step.
2. **.planning/PROJECT.md → downstream planning phases.** A stale or incorrect decision row causes future planners to re-open a settled question.

No untrusted input, no runtime code changes, no new network surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | README.md + docs/PRD.md content | mitigate | Every edit is constrained to an exact string replacement (see Tasks 2-3). The executor MUST NOT reflow paragraphs or edit surrounding text. Git diff review at acceptance time catches any out-of-scope change. |
| T-01-02-02 | Information Disclosure | All modified docs | accept | New content is "20 km" — a number that is already in the public seed script and was already in SETUP.md. No new information is disclosed. |
| T-01-02-03 | Repudiation | PROJECT.md decision-log row | mitigate | Task 4 acceptance criteria require the updated row to reference "Phase 1 Plan 02", giving future readers an audit trail back to this plan's SUMMARY. |
| T-01-02-04 | Integrity (drift regression) | Future planning docs | mitigate | SUMMARY records the authoritative literal and the files NOT to modify, so later phases that touch seed data inherit a single source of truth. |
</threat_model>

<verification_criteria>

```bash
# Full plan verification — run from repo root

# Authoritative value is still code (no one should have touched seed_data.py)
grep -nE "^DIST\s*=\s*20000\s*$" scripts/seed_data.py
grep -nE "~20\s*km\s+radius" docs/SETUP.md

# README.md drift fixed
grep -nE "~20\s*km\s+radius" README.md
! grep -nE "~10\s*km\s+radius" README.md

# docs/PRD.md drift fixed
grep -nE "osmnx LA 20km" docs/PRD.md
! grep -nE "osmnx LA 10km" docs/PRD.md

# PROJECT.md decision row resolved
grep -nE "Seed radius = 20 km" .planning/PROJECT.md
! grep -nE "Seed radius = 10 km" .planning/PROJECT.md
grep -nE "Phase 1 Plan 02" .planning/PROJECT.md

# BIGINT row in PROJECT.md left alone (Plan 01's concern, still pending per PROJECT.md)
grep -nE "road_segments.*source.*target.*BIGINT" .planning/PROJECT.md

# SUMMARY written with correct closure statement
test -f .planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md
grep -qE "INFO #1.*RESOLVED" .planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md
```

</verification_criteria>

<success_criteria>
- `scripts/seed_data.py` is unchanged (authoritative source).
- `docs/SETUP.md` is unchanged (already matched code).
- `README.md` line 38 now says "~20 km radius" (was "~10km radius").
- `docs/PRD.md` line 19 now says "osmnx LA 20km" (was "osmnx LA 10km").
- `.planning/PROJECT.md` Key Decisions row for seed radius now reads "Seed radius = 20 km" with "✓ Resolved (Phase 1, 2026-04-23)" outcome and references "Phase 1 Plan 02".
- `.planning/PROJECT.md` BIGINT decision row is unchanged (Plan 01's concern).
- SUMMARY file documents the drift fix, enumerates modified and intentionally-not-modified files, and closes INGEST-CONFLICTS INFO row 1.
</success_criteria>

<output>
After completion, `.planning/phases/01-mvp-integrity-cleanup/01-02-SUMMARY.md` must exist with drift-fix evidence, file-change inventory, and explicit closure of INGEST-CONFLICTS INFO row 1. Modified files: README.md, docs/PRD.md, .planning/PROJECT.md.
</output>
